# F1TV 繁中字幕 — 完整教學

> 對應版本 `v4.4.0`。程式碼有變動時本文件必須同步更新。
> 專案背景與架構決策請看 [`handoff.md`](./handoff.md)，後端部署看 [`backend/README.md`](./backend/README.md)。

---

# 第一部分：安裝

## 步驟 1：Tampermonkey 前置設定

1. Chrome 線上應用程式商店安裝 **Tampermonkey**
2. 網址列輸入 `chrome://extensions`
3. 右上角開啟「**開發人員模式**」
4. 找到 Tampermonkey → 點「**詳細資料**」→ 開啟「**允許存取檔案網址**」

> ⚠️ **這兩個開關不開，Manifest V3 下的 userscript 不會執行。** 這是最常見的「裝了沒反應」原因。

## 步驟 2：安裝腳本

1. 點 Tampermonkey 圖示 → **新增腳本**
2. 編輯區內容**全選刪除**
3. 貼上 `f1tv-zh-subtitles.user.js` 的完整內容
4. `Ctrl + S` 儲存

## 步驟 3：取得 Anthropic API Key

1. 前往 [console.anthropic.com](https://console.anthropic.com) 註冊
2. **Settings → Billing** 儲值（最低 US$5，以目前成本可看約 16 場正賽）
3. **API Keys → Create Key**，複製 `sk-ant-...`
4. 建議在 Console 為這把 key 設定每月消費上限

## 步驟 4：設定 Key

1. 開啟 `https://f1tv.formula1.com`
2. 網址列的 Tampermonkey 圖示 → **🔑 設定 Anthropic API key**
3. 貼上 → 確定 → **重新整理頁面**

## 步驟 5：驗證

在任何 F1TV 頁面開 `F12` → Console，執行：

```js
__f1zh.test("Massive lock-up into the hairpin, he's flat-spotted the front left")
```

預期畫面下方出現：

> **髮夾彎前嚴重鎖死，左前胎磨出平斑了**
> <sub>Massive lock-up into the hairpin, he's flat-spotted the front left</sub>

同時 Console 出現綠字 `✅ Prompt caching 生效`。

---

# 第二部分：日常使用

## 每次觀看的標準流程

1. 進入播放頁面（網址含 `/detail/`）
2. **在播放器設定裡開啟英文字幕（CC）** — 腳本靠它取得文字，即使畫面上被我們隱藏也必須是開啟狀態
3. 開始播放
4. 觀察 Console，重播應該出現：
   ```
   🎯 找到字幕播放清單（eng，...），開始整軌預抓
   字幕分段共 XXX 個，開始抓取
   ✅ 整軌預抓完成：成功 XXX 段、失敗 0 段
   ```
5. 中文字幕自動出現

## 選單功能一覽

點 Tampermonkey 圖示可看到：

| 選單 | 用途 | 何時用 |
|---|---|---|
| 🔑 設定 Anthropic API key | 設定／更換金鑰 | 首次安裝、換 key |
| ☁ 設定共用譯文後端 | 填後端網址與權杖 | 部署後端後 |
| ☁ 測試後端連線 | 驗證可連線與本片快取狀況 | 設定後、除錯 |
| ☁ 立即上傳本片譯文 | 手動貢獻譯文到共用快取 | 收割完成後 |
| ⏯ 開 / 關字幕翻譯 | 總開關 | 想看純英文時 |
| 🚀 開 / 關前瞻預譯 | 關掉會退回逐句模式 | 除錯用，平時別關 |
| 🎯 立即整軌預抓 | 手動觸發 | 換了影片、或自動沒觸發 |
| 🧬 開 / 關 Worker 注入 | 注入播放器 worker | **影片播不出來時關掉這個** |
| 🔤 切換雙語 / 純中文 | 顯示樣式 | 依喜好 |
| 👁 顯示 / 隱藏原生英文字幕 | 原生 CC 可見度 | 除錯用 |
| 🩺 掛載狀態自我診斷 | 完整健康檢查 | **出問題第一個看這個** |
| 🧮 檢查 system prompt token 數 | 確認是否跨過快取門檻 | **改術語表後必看** |
| 📊 顯示本場統計 | 成本與效能 | 想知道花多少錢 |
| 🌐 顯示攔截到的網址 | 網路除錯 | 進階除錯 |
| 📜 顯示攔截到的 manifest | 看 HLS 清單 | 進階除錯 |

## 統計數字怎麼看

執行 **📊 顯示本場統計**：

| 欄位 | 健康值 | 意義 |
|---|---|---|
| 逐句呼叫 | 越低越好，理想 < 10 | 走最貴路徑的次數 |
| 批次呼叫 | — | 預譯的呼叫次數 |
| **平均批次大小** | **> 10 句/次** | 低於 5 代表批次沒聚合，成本偏高 |
| 實測提前量中位數 | 重播約 45~55 秒 | 自適應的預算來源 |
| **命中率** | **> 90%** | 低於 70% 代表預譯沒跟上 |
| 錯誤 | 0 | 非 0 要看 Console |
| 實際花費 | 一小時內容約 $0.17 | 讀 API 回傳的真實用量，非估算 |

**🩺 診斷新增的欄位（v4.1.0）：**

| 欄位 | 健康值 | 意義 |
|---|---|---|
| 距上次看到字幕 | 播放中 < 30 秒 | 超過 45 秒會跳提示，通常是 CC 被關掉 |
| 目前 contentId | 與網址一致 | 換影片時會自動重置預抓狀態 |

---

# 第二點五部分：共用譯文後端

> 完整部署步驟見 [`backend/README.md`](./backend/README.md)。這裡只講 userscript 端怎麼接。

## 這是什麼

所有人看同一支影片拿到的是**同一份英文字幕**，所以譯文可以共用。第一次有人看就翻一次存起來，之後所有人直接下載，**零 API 呼叫、零延遲**。

**不設定也完全能用**——後端是選配，沒設定就是純本機模式，行為和 v4.2.0 一樣。

## 設定

部署好後端後，選單 → **☁ 設定共用譯文後端**，依序填入：

| 欄位 | 說明 |
|---|---|
| 後端網址 | `https://f1zh-api.xxx.workers.dev`。**留空 = 停用** |
| CLIENT_TOKEN | 讀取與翻譯用，所有使用者都需要 |
| ADMIN_TOKEN | **上傳用，只有你要填**。一般使用者留空即可 |

填完重整頁面，再跑 **☁ 測試後端連線** 確認。

## 運作流程

```
換影片／首次載入
   → 自動向後端要整支譯文，灌進本機快取
   → Console：「☁ 從共用快取取得 N 句譯文，這些不會再花錢」

整軌預抓完成、佇列清空後
   → 若有 ADMIN_TOKEN，自動把本片譯文上傳
   → Console：「☁ 已上傳 N 句到共用快取」
```

**連不上後端時全自動退回本機翻譯**，只會在 Console 留一行記錄，不影響觀看。

## 怎麼確認共用生效了

看 🩺 診斷的「共用譯文後端」區塊：

```
本次下載：1101 句 ／ 上傳：0 句 ／ 錯誤：0
```

下載數字 > 0 就代表命中了。這時再看 📊 統計，**批次呼叫應該接近 0**——因為根本不需要翻。

## 給一般使用者的說明

如果你要把腳本分享出去，告訴對方：

- **填後端網址 + CLIENT_TOKEN 就好，ADMIN_TOKEN 留空**
- 熱門內容多半已經有人翻過，會直接命中、不花他們的錢
- 冷門內容才會用到他們自己的 Anthropic key

---

# 第三部分：疑難排解

## 決策樹

```
沒有中文字幕
   ├─ Console 有 [f1zh] 載入訊息嗎？
   │    否 → Tampermonkey 兩個開關沒開，或 @match 不符
   │    是 ↓
   ├─ 執行 🩺 診斷，看「翻譯：」
   │    關閉 → 用 ⏯ 打開
   │    開啟 ↓
   ├─ 看「容器原始文字長度」
   │    0 字   → 播放器真的沒送字幕，去設定裡開 CC
   │    > 0 字 ↓
   ├─ 看「目前抓到的英文」
   │    （空） → 抽取邏輯有問題，回報
   │    有文字 ↓
   └─ 看「API key」與 Console 錯誤
        401 → key 無效或沒儲值
        429 → 觸發速率限制，把 batchSize 調小
```

## 常見問題對照表

| 症狀 | 原因 | 解法 |
|---|---|---|
| Console 沒有 `[f1zh]` 載入訊息 | Tampermonkey 開關沒開 | 回第一部分步驟 1 |
| `__f1zh is not defined` | 沙箱隔離 | 確認腳本有 `@grant unsafeWindow`；或在 Console 左上把執行環境從 `top` 改成腳本名稱 |
| 有載入但完全沒字幕 | 播放器沒開 CC | 播放器設定裡開啟英文字幕 |
| 中英字幕重疊 | 原生 CC 沒被隱藏 | 確認 `hideNativeCC: true` |
| 字幕位置跑掉 | 全螢幕／多視角 | 應自動處理；仍有問題回報使用的全螢幕方式 |
| **切換車手鏡頭後字幕不見** | 播放器重建 DOM，觀察者失聯 | **v4.1.0 已修**（改為 250ms 主動輪詢）。若仍發生，看 🩺 診斷的「距上次看到字幕」秒數 |
| 換了一支影片後預抓沒跑 | contentId 未偵測到 | v4.1.0 已自動偵測並重置。可用 🎯 手動觸發 |
| 疊字層整個消失 | 播放器重建時被一起移除 | v4.1.0 已在輪詢中自動重掛 |
| 畫面出現「⚠ 未偵測到字幕」 | 播放中 45 秒沒有任何字幕 | 到播放器設定重新開啟英文 CC（切換視角後可能被關掉） |
| **影片完全播不出來** | **Worker 注入破壞了播放器** | **🧬 關閉 Worker 注入 → 重整**（設定會記住） |
| HTTP 401 | key 無效或未儲值 | 重設 key、檢查 Console 餘額 |
| HTTP 429 | 速率限制 | `CFG.batchSize` 調小；新帳號 Tier 較低 |
| 整軌預抓 HTTP 400 | 授權 token 或網址問題 | 看 Console 印出的清單網址並回報 |
| 後端 401 | CLIENT_TOKEN／ADMIN_TOKEN 不符 | 確認與 `wrangler secret put` 設的一致 |
| 後端 429 | 每 IP 每分鐘 120 次上限 | 正常使用不會碰到；碰到代表有迴圈 |
| 後端下載 0 句 | 該影片還沒有人翻過 | 正常。翻完會自動上傳，下次就有了 |
| 上傳沒反應 | 沒設 ADMIN_TOKEN | 一般使用者本來就不該有，只有你需要 |
| 平均批次只有 2~3 句 | 提前量太短或聚合失效 | 看「實測提前量中位數」，過短就是播放器緩衝不足 |
| 翻譯品質怪 | 術語表缺詞 | 見第四部分 |

## 進階除錯指令

```js
__f1zh.peek()          // 現在畫面上抓到什麼英文
__f1zh.stats           // 完整統計物件
__f1zh.lead()          // 提前量樣本與目前採用的等待
__f1zh.subPlaylist()   // 字幕清單狀態（走哪條路徑、body 在不在手上）
__f1zh.netlog()        // 攔截到的網址表
__f1zh.manifests()     // 攔到的 manifest 全文
__f1zh.fullPrefetch()  // 強制重跑整軌預抓
__f1zh.on() / .off()   // 開關翻譯
__f1zh.cfg.debug = true   // 開啟詳細日誌（每句的原文/譯文/耗時）
```

---

# 第四部分：維護術語表（最重要的日常工作）

**成本已經不是問題了，翻譯品質才是你每場都會感受到的東西。而術語表改一行就見效。**

## 位置

腳本第 2 區塊的 `SYSTEM_PROMPT` 常數。

## 新增詞條

找到對應的分類區塊，加一行：

```
英文原文 → 中文譯法
```

範例：

```
tyre warmer → 暖胎毯
sighting lap → 場勘圈
```

## 分類區塊（v4.2.0）

```
【輸出規則】【批次模式】【專有名詞總則】

人物類
  【2026 賽季車隊與車手】   ← 含備援與測試車手
  【歷代車手】              ← 世界冠軍、近年退役、經典人物
  【車隊與賽事人物】        ← 領隊高層、賽事工程師、轉播團隊、贊助品牌

技術類
  【2026 技術規則新名詞】【動力單元】【輪胎】【空力與底盤】【駕駛與圈速】

賽事類
  【策略與進站】【賽會、旗號與處罰】【賽制與統計】

地點類
  【2026 賽季賽道】         ← 24 站完整清單
  【彎角俗名】              ← 各賽道彎角＋歷史賽道

情境類
  【纏鬥與駕駛動作】【天候與賽道狀況】【賽季與合約用語】
  【車隊無線電常用語】

【翻譯風格範例】            ← 改語氣改這裡最有效
```

**人物與地點一律「保留英文原文」，不是翻譯對照。** 列出來的目的是讓模型認得出那是專有名詞，不要誤譯（例如把 `Bono` 當成 U2 主唱、把 `Village` 當成「村莊」）。

## ⚠️ 改完必做

執行選單的 **🧮 檢查 system prompt token 數**，確認 **≥ 4,096**。

低於門檻，prompt caching 會**靜默失效**，成本立刻變成 10 倍，而且不會有任何錯誤訊息。

## 加詞的成本（放心加）

Prompt 變長只影響「快取讀取」那筆，而它是 0.1 倍計價：

```
每增加 1,000 tokens ≈ 每場正賽多花 US$0.008（約新台幣 0.25 元）
```

**術語表加一倍也只多幾毛錢。** 翻譯品質的價值遠高於此，看到缺詞就加，不用省。

唯一的真實限制是**別讓規則被淹沒**——術語表可以長，但最上方的【輸出規則】要保持精簡，那才是模型最需要遵守的部分。

## 修改的優先順序

1. **翻錯的專有名詞**（車手、車隊、賽道）— 影響最大
2. **翻反或翻錯意思的術語**（如 undercut / overcut 搞混）
3. **語氣不對**（太書面）— 改「翻譯風格範例」比加規則有效
4. 罕見詞 — 低優先

## 小技巧

**正例比禁令有效。** 想改語氣，與其寫「不要太書面」，不如在【翻譯風格範例】加一組實際的對照。模型會照著範例的長度和語氣走。

---

# 第五部分：開發者指南

## 程式碼結構

| 區塊 | 行為 |
|---|---|
| 1. 設定區 `CFG` | 所有可調參數 |
| 2. `SYSTEM_PROMPT` | 翻譯規則與術語表 |
| 3. 狀態 | 快取、佇列、統計 |
| 4. 文字正規化 | `clean()` `normKey()` `memoSet/Get()` |
| 5. 疊字層 | 定位、顯示 |
| 6. API | `apiPost()` `tallyUsage()` |
| 7. 逐句翻譯 | 第 3 層降級路徑 |
| 8. 整軌預抓 | 第 1 層（重播） |
| 9. Worker 注入 | 第 2 層 |
| 10. DOM 字幕來源 | 顯示觸發器 |
| 11. 啟動 | hook 安裝順序 |
| 12. 選單 | Tampermonkey 選單 |
| 13. 除錯把手 | `__f1zh` |

## 重要參數

```js
CFG.batchSize        = 15     // 湊滿就送
CFG.flushDelayMs     = 8000   // 尚未量到提前量前的預設等待
CFG.adaptiveFlush    = true   // 用實測提前量的一半
CFG.fetchConcurrency = 3      // 整軌預抓的並行數
CFG.model            = 'claude-haiku-4-5'

// 韌性（v4.1.0 新增）
CFG.captionPollMs    = 250    // 主動輪詢字幕的間隔，這是偵測主力
CFG.noCaptionWarnMs  = 45000  // 播放中這麼久沒字幕就提示
CFG.showCaptionHint  = true   // 在疊字層顯示提示
```

## 修改時的鐵則

1. **文字一律用 `textContent`，不用 `innerText`** — 見 handoff 坑 #1
2. **網址絕不截斷** — 見坑 #3
3. **任何輪詢裡的重試都要有上限** — 見坑 #4
4. **不要相信「註冊一次就永遠有效」** — SPA 會重建 DOM，任何 observer／事件監聽都要有主動輪詢當備援。見 handoff 決策 4.6 與坑 #8
4. **改 `SYSTEM_PROMPT` 後檢查 token 數** — 見決策 4.2
5. **worker 內只 clone 小的文字類回應** — 否則持續 clone 影片分段會吃爆記憶體
6. **絕不碰 `MediaSource`** — `createObjectURL` 的 patch 要用 `instanceof Blob` 過濾

## 支援新網站的步驟

1. 用「文字變動嗅探器」找出字幕容器選擇器（方法見 handoff 第 2 節）
2. 更新 `CFG.captionRoot` / `CFG.captionLabel`
3. 加 `@match` 規則
4. 驗證 `__f1zh.peek()` 讀得到文字

## 修改後的檢查清單

- [ ] 程式碼可正常載入（Console 有版本訊息）
- [ ] `__f1zh.test()` 能翻譯並顯示
- [ ] 🩺 診斷各項正常
- [ ] 🧮 token 數 ≥ 4,096（若動過 prompt）
- [ ] 實際播放一段，命中率 > 90%
- [ ] **更新 `handoff.md` 的版本、更新紀錄**
- [ ] **更新本文件對應段落**

---

# 附錄：安全注意事項

- **API key 存在 Tampermonkey 的 `GM_setValue`**，不在程式碼裡。**不要把設定好的腳本分享給別人**
- 專案資料夾若有存放金鑰的檔案（例如 `c api.txt`），**建 repo 前務必加進 `.gitignore`**
- 建議在 Anthropic Console 為此 key 設定每月消費上限
- 腳本只讀取字幕文字，不碰 DRM、不下載影音
