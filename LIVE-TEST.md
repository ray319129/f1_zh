# 直播實測計畫

> **這是整個專案最大的未知數**（`AUDIT-2026-08-16.md` 的 U1）。
> 直播路徑的滑動視窗補抓、`isVod` 判定、直播下的免費層計時、DO 並發去重，
> **全部只有程式碼，從未跑過一場真的直播**。
>
> 賽季開始前一定要跑完這份。測完再決定要不要做 DO 步驟 4~5。

---

## 為什麼需要一份清單

直播只有在真的直播時才能測，而一個賽季只有 23 個週末。
**測錯或漏測，下一次機會是一週後。** 所以事前把要看的東西全部列出來，
現場只負責照著做，不要臨時想。

---

## 事前準備（比賽開始前 30 分鐘）

### 1. 確認版本

```
擴充功能　chrome://extensions → PitLingo 應為 v0.19.0 以上
後端　　　curl https://api.pitlingo.com/v1/health
```

後端必須已經部署帶 DO 的版本（v4.0+）。確認方式：Cloudflare 後台
Workers → f1zh-api → Settings → Bindings 應該看得到 **ROOM (Durable Object)**。

### 2. 建立三個獨立的「使用者」

**一台電腦就夠。** 每個 Chrome 設定檔有獨立的 `chrome.storage`，
也就是**獨立的 installId** —— 對後端而言就是三個不同的使用者。

```
chrome.exe --user-data-dir=C:\tmp\pl1
chrome.exe --user-data-dir=C:\tmp\pl2
chrome.exe --user-data-dir=C:\tmp\pl3
```

每個設定檔都要：
- 載入擴充功能（`chrome://extensions` → 載入未封裝項目 → `extension/`）
- 登入 F1TV
- **其中一個啟用授權碼，另外兩個保持免費** —— 兩種路徑都要測到

### 3. 記下起始數字

```
Cloudflare 後台 → Workers & Pages → KV → SUBS → 目前的 puts 用量
Cloudflare 後台 → Workers → f1zh-api → Durable Objects 的請求數
curl "https://api.pitlingo.com/v1/admin/stats?days=1" -H "x-admin-token: <TOKEN>"
```

**沒有起始值就算不出增量。** 這一步最容易忘。

---

## 開播後：五個要量的東西

### ① 提前量 —— **最關鍵的未知數**

重播實測是 **47~53 秒**。直播受限於 live edge，一定更短，
但**短到什麼程度決定整個設計成不成立**：

| 提前量 | 意義 |
|---|---|
| > 10 秒 | 前瞻預譯成立，DO 讓路機制也成立 |
| 3~10 秒 | 勉強成立，但讓路的人可能來不及 |
| < 3 秒 | **前瞻預譯在直播上不成立**，去重設計要重做 |

**怎麼量**：開播 5 分鐘後匯出診斷，看

```
──── 提前量來源：Worker 注入 ────
收到的 VTT 分段：N 份
```

再對照畫面上正在顯示的句子與 `cue 時間軸` 的最大值。
或直接在 Console 打 `__pitlingo.diag()` 看「字幕時機微調」區塊的
`校準` 那一行 —— 基準值就是提前量的量測結果。

### ② DO 並發去重是否生效

```
curl "https://api.pitlingo.com/v1/admin/stats?days=1" -H "x-admin-token: <TOKEN>"
```

看 **模型呼叫次數 vs 翻譯句數**。

| 結果 | 判讀 |
|---|---|
| 呼叫次數 ≈ 句數 ÷ 15 | ✅ 去重生效（三個人只翻了一份） |
| 呼叫次數 ≈ 句數 ÷ 15 × 3 | ❌ 去重失效，三個人各翻各的 |

模擬測試（`tools/check-live-concurrency.js`）顯示 10 並發降到 2 次，
但那是假的 DO。**這一項是真實環境的驗收標準。**

### ③ KV 寫入量

直播結束後看 KV puts 的增量。預估值：

| 來源 | 每場約 |
|---|---|
| bundle 回寫 | 約 147（與觀眾數無關，DO 去重後只有一個人翻） |
| 免費額度計數 | 約 1.7 × 免費使用者數 × 觀看批次 |
| 產品數據 | 約 10 |

**如果實測遠高於估計，代表某個地方在重複寫**，那是做 DO 步驟 4~5 的訊號。

### ④ 免費層計時是否正確

用兩個免費設定檔各測一種：

- **設定檔 2**：連續看 20 分鐘不暫停 → 第 15 分鐘應停止顯示中文、原生英文字幕恢復
- **設定檔 3**：看 5 分鐘 → 暫停 10 分鐘 → 再看 → **暫停期間不可計時**，
  總共應該還有 10 分鐘可用

直播用的是「累計實際觀看秒數」（重播用播放位置），這條路從未實測。
狀態卡上的 `免費試看　剩餘 M:SS` 就是驗證點。

### ⑤ 播放品質

**開著擴充功能與關掉，各看 10 分鐘。**

- 有沒有卡頓、有沒有自動降解析度
- 診斷報告的「讓路給影片」有沒有動：

```
讓路給影片：暫停 N 次共 X 秒　目前節奏 40ms
播放器緩衝：24.3 秒
```

全部是 0 代表退讓機制根本沒被觸發（可能是好事，也可能是判斷條件寫錯）。

---

## 同時要留意的四件事

| 項目 | 怎麼看 |
|---|---|
| `isVod` 判定 | 診斷的「型態」欄應為**直播**，不是重播 |
| 滑動視窗補抓 | 事件時間軸應每 20 秒左右出現「直播：清單新增 N 段」 |
| 多視角切換 | 切換主畫面後字幕是否還在（選擇器可能整組換掉） |
| 中途缺漏 | 診斷的「中途缺漏」欄，`silent` 是正常（賽後訪問），其餘都要查 |

---

## 結束後要收集的東西

1. **三個設定檔各匯出一份診斷**（擴充功能圖示 → 回報問題 → 傳送診斷資訊）
2. `curl "https://api.pitlingo.com/v1/admin/stats?days=1"` 的完整輸出
3. Cloudflare 的 KV puts 與 DO 請求數增量
4. 主觀感受：字幕有沒有慢、有沒有掉、播放順不順

**這四樣湊齊才有辦法判斷要不要做步驟 4~5。**

---

## 判斷準則

| 觀察 | 結論 |
|---|---|
| 提前量 > 10 秒、去重生效、KV 增量符合估計 | **不用做 4~5**，現況足夠 |
| 提前量 < 3 秒 | **去重設計要重做**，4~5 先不要碰 |
| KV 增量遠超估計 | **做 4~5**，落地節流是必要的 |
| 播放卡頓且「讓路」全是 0 | 退讓的判斷條件寫錯，先修那個 |

---

## 最後提醒

**這是一次性的機會。** 開播前把這份文件打開放在旁邊，
量測用的指令先貼好在終端機裡。現場只做兩件事：照著跑、把數字記下來。
