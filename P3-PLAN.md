# P3 商品化技術規劃

> 對應 `handoff.md` 路線圖的 **P3**。這份談的是「怎麼做成一個成熟的付費軟體」，
> 不是功能，而是**維運、金流、可觀測性、以及不傷害使用者與開發者的機制**。
>
> `GO-TO-MARKET.md` 談社群與推廣，這份談技術與制度。

---

## 1. 分階段推送 —— 最優先，因為現在完全沒有

### 現在的風險（使用者自己指出的，正確）

| 動作 | 現在的後果 |
|---|---|
| `wrangler deploy` | **所有使用者立刻換版**，沒有灰度、沒有回滾按鈕 |
| `POST /v1/config` | **所有使用者三分鐘內套用**，推錯選擇器等於全站字幕停擺 |
| Chrome Web Store 上架新版 | 幾小時內推給所有人 |

三條路都是「一改就全中」。**一次失誤 = 所有付費使用者同時故障**，而他們付了錢。
這在自用階段無所謂，在收費之後是重大事故。

### 四層防護（由外而內）

#### 1.1 Chrome Web Store 的部分推送（免費、內建）

上架時選 **Partial rollout**，先推給 10% 的安裝數，觀察 24 小時再拉到 50% → 100%。
出事就停在原地，未更新的 90% 不受影響。

> ⚠️ 只對「已安裝的使用者更新」有效，新安裝一律拿最新版。所以它是**降低爆炸半徑**，
> 不是保險。真正的保險是下面三層。

#### 1.2 Cloudflare Workers 的漸進式部署

```bash
wrangler versions upload            # 上傳但不啟用
wrangler versions deploy            # 指定版本與流量比例，例如 新版 10% / 舊版 90%
```

出事的回滾是把比例調回 0，**秒級生效**，不用重新部署。
上線後**禁止直接 `wrangler deploy`**（那等於 100% 立刻切換）。

#### 1.3 遠端設定加上「金絲雀」與「總開關」

現在的 `/v1/config` 是全有全無。要加三個欄位：

```jsonc
{
  "version": 2,
  "rollout": 10,          // 只有 10% 的用戶端套用這份新設定，其餘用上一版
  "killSwitch": false,    // true = 所有用戶端停用疊字，只留原生字幕
  "minClientVersion": "0.5.0",   // 低於此版的用戶端顯示「請更新」並停止翻譯
  "sites": [ /* ... */ ]
}
```

**分流方式**：用戶端第一次啟動時產生一個隨機且穩定的 `installId`（存 `chrome.storage`），
取雜湊後對 100 取模。同一個人永遠落在同一組，不會忽新忽舊。

`installId` **只用於分流，不上傳、不關聯任何個人資料**——這點要寫進隱私政策。

**`killSwitch` 是最後的救命索**：F1TV 大改版而我們一時修不好時，
與其讓使用者看到錯亂的疊字，不如乾淨地退回原生英文字幕並顯示說明。

#### 1.4 用戶端自我保護

- 遠端設定套用後**自我檢查**：30 秒內若一句字幕都沒抓到，自動退回上一版設定並回報
- 設定快取保留「上一版已知可用」的副本，新版失敗就用舊的
- 這樣即使推了壞設定，使用者最多壞 30 秒

### 實作順序

1. `installId` + `rollout` 分流（用戶端 + 後端）
2. `killSwitch` + `minClientVersion`
3. 用戶端的自我檢查與自動回退
4. 上線後改用 `wrangler versions deploy`，並在 README 明文禁止 `wrangler deploy`

---

## 2. 成本後台

### 現在的狀況

`/v1/translate` 的回應已經帶 `usage`（input / output / cache_read tokens），
但**沒有人在累計**。目前只能事後翻 Anthropic 的帳單，看不出「哪支影片花了多少」。

### 為什麼不能用 KV 累計

KV 免費額度是 **1,000 puts/天**（已經撞過一次，坑 #19）。
「每次翻譯寫一次計數」必定再撞牆。

### 正解：Cloudflare Analytics Engine

- **寫入不計入 KV 額度**，本來就是為高頻寫入設計的
- 免費方案可用，查詢走 SQL API
- 每次 `/v1/translate` 寫一筆：

```js
env.ANALYTICS.writeDataPoint({
  blobs: [cid, model],
  doubles: [inputTokens, outputTokens, cacheReadTokens, translatedCount],
  indexes: [cid],
});
```

### 後台要看到什麼

新增 `GET /v1/admin/stats`（需 `ADMIN_TOKEN`），回傳：

| 指標 | 為什麼要看 |
|---|---|
| 今日／本月 API 成本（美元） | 直接對得上帳單 |
| 每支影片的成本與句數 | 找出異常昂貴的內容 |
| **prompt cache 命中率** | 掉下來代表 4,096 門檻又出事了（坑 #21） |
| 共用快取命中率 | 這是整個商業模式的分母 |
| KV puts 今日用量 | 距離 1,000 還有多遠 |
| 每位使用者平均成本 | 定價的依據 |

前端做一頁靜態 HTML，直接打這個端點就好，不需要框架。

**成本警戒線**：超過設定值就在回應裡帶警告，並考慮暫停 `/v1/translate`（保護開發者）。

---

## 3. 金流

### 前提：Chrome Web Store 沒有金流

Google 已於 2020 年停止 Chrome Web Store 的付款與訂閱功能。
**授權必須自建**——這不是選擇題。

### 建議：台灣本地金流為主，PayPal 為輔

#### 綠界 ECPay / 藍新 NewebPay（主力）

| | |
|---|---|
| 費率 | 信用卡約 2.75%、ATM 轉帳約 NT$10~15/筆、超商代收約 NT$25/筆 |
| 優勢 | 台灣消費者信任度高、支援 ATM 與超商（沒信用卡的學生也能付）、金流直接進台灣銀行帳戶、發票可自動開立 |
| 限制 | **需要商業登記才能開通完整功能**（個人賣家額度低）、只收得到台灣的錢 |
| 定期扣款 | 綠界支援信用卡定期定額，適合訂閱制 |

#### PayPal（備援，不建議當主力）

**優點**
- 不需台灣商業登記就能開始收款
- 內建訂閱（Subscriptions API），開發成本低
- 海外使用者（港澳、留學生）唯一實際可行的選項

**缺點（這些才是重點）**
- **費率高**：跨境收款約 4.4% + 固定費，比綠界貴一倍
- **提領有匯損**：轉回台灣銀行帳戶會被 PayPal 的匯率吃一層，實際成本再高 1~3%
- **爭議處理明顯偏向買方**：對方申訴 chargeback 時，數位商品幾乎必輸——
  你無法提供「已寄送」的證明。這是數位訂閱用 PayPal 最大的風險
- **台灣普及率低**：多數台灣使用者沒有 PayPal 帳戶，轉換率會掉
- 帳戶可能因「高風險交易模式」被凍結，且申訴曠日費時

#### 結論

```
台灣使用者  → 綠界（信用卡定期定額 + ATM + 超商）
海外使用者  → PayPal，並在條款明訂「數位商品，開通後不退款」
```

**先只做台灣**（使用者已決定）。PayPal 等到真的有海外需求再加，
且要把 chargeback 風險算進定價。

### 授權機制（保護雙方）

- 付款成功 → 後端簽發 **JWT**，有效期至賽季結束，存在 `chrome.storage`
- 用戶端每次啟動驗證簽章（**離線可驗**），過期前 7 天提示續訂
- **後端掉線時不要立刻鎖住功能**——JWT 還在有效期內就繼續服務。
  伺服器出問題不該讓付費使用者看不到字幕，那是我們的錯不是他們的
- 退款／停權 → 加入撤銷清單，用戶端每天檢查一次

### 免費試用

企劃書寫的是「每場 GP 前 15 分鐘」。實作上比較穩的是：
**每個 `contentId` 前 500 句免費**，超過才要授權。
理由：不依賴時鐘、不依賴使用者何時開始看，而且對「只想試試」的人足夠。

---

## 4. 介面與體驗

現在的設定頁是功能性的，不是產品。要做的（依優先序）：

### 4.1 首次安裝的引導

現在使用者裝完看到的是一堆滑桿。應該是三步驟：
1. 貼上存取金鑰（或登入）
2. 「請在 F1TV 播放器開啟英文 CC」——**附截圖**，這是最常見的卡關點
3. 「打開任一支影片就會自動運作」

### 4.2 工具列彈出視窗＝狀態面板

打開就看到「現在是什麼狀況」，而不是設定：

```
● 運作中 · 共用快取 2,338 句 · 本場已省下 $0.31
  這支影片已完整翻譯，不會再花錢

[設定]  [匯出診斷]
```

狀態要有顏色：綠（正常）／黃（逐句模式，較慢）／紅（CC 沒開或後端異常）。

### 4.3 錯誤狀態要有出路

每個錯誤都要說「使用者現在能做什麼」，不是只報錯：
- CC 沒開 → 附播放器設定的位置說明
- 金鑰無效 → 直接給重新輸入的欄位
- 後端異常 → 說明「字幕仍可運作但較慢」，不要讓人以為壞了

### 4.4 設定項目分組

**基本**（字級、位置、雙語）／**進階**（停留時間、隱藏原生 CC）／
**疑難排解**（重新載入設定、匯出診斷、詳細日誌）。
現在全部平鋪，第一次看會不知道該動哪個。

### 4.5 疊字本身

- 字級隨影片寬度縮放（**已完成**）
- 加半透明底或文字陰影，白色字幕在淺色畫面上會看不清
- 全螢幕與多視角要驗過（多視角取最大容器**已完成**）

---

## 5. 上線前的檢查清單

| # | 項目 | 狀態 |
|---|---|---|
| 1 | **移除測試工具** `__pitlingo.t.*`（見 `CLAUDE.md`） | ⬜ |
| 2 | 輪換 `ADMIN_TOKEN`（曾在對話中外洩） | ⬜ |
| 3 | 圖示 16/32/48/128 | ✅ |
| 4 | 隱私政策（需網域託管） | ⬜ |
| 5 | 使用條款（含「數位商品不退款」與爭議處理） | ⬜ |
| 6 | `installId` 分流 + `killSwitch` | ⬜ |
| 7 | 成本後台 | ⬜ |
| 8 | 改用 `wrangler versions deploy`，禁用 `wrangler deploy` | ⬜ |
| 9 | 一場完整正賽的穩定性測試 | ⬜ |
| 10 | 直播（非重播）實測 —— **從未測過** | ⬜ |

**第 10 項是最大的未知數**：三個產物的直播路徑都沒有真正跑過一場。
賽季開始前一定要找一場直播驗證。
