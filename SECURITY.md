# 資安檢視

> 2026-08-16 對 `extension/`、`backend/`、`f1tv-zh-subtitles.user.js` 的完整攻擊面檢視。
> 上線前必須把「未修正」欄位全部清空。

## 已修正

| # | 問題 | 嚴重度 | 說明與修法 |
|---|---|---|---|
| S1 | **診斷報告外洩 CDN 存取權杖** | **高** | 字幕清單網址裡的 base64 解開含 `sessionId` 與 24 小時有效的存取權杖（`ttl:1440`）。報告是設計來貼給別人看的，**貼出去等於送出該串流的存取權**。v0.4.0 為了偵錯拿掉截斷時破壞了「報告不含權杖」的原設計，期間已產生 6 份以上外洩的報告。修法：`maskUrl()` 遮蔽權杖但保留長度與頭尾，仍能判斷網址有沒有被截斷 |
| S2 | **SW 可被誘導抓任意網址** | **高** | `fetchText()` 沒有任何網址驗證，且帶 `credentials: 'include'`。字幕清單網址來自 `window.postMessage`，而**頁面上任何腳本都能偽造那個訊息**——F1TV 頁面的第三方腳本（廣告、分析、被入侵的 CDN）就能讓擴充功能帶著使用者 cookie 去抓任意網址。修法：`FETCH_ALLOW` 網域白名單 + 強制 https |
| S3 | **成本放大攻擊** | 中 | `/v1/translate` 限制 200 句，但**沒有限制每句長度**。200 句 × 100KB = 20MB 進模型，一次請求就能燒掉大量 token。修法：單句上限 1,000 字元（真實字幕不超過 300） |
| S4 | `postMessage` 用 `'*'` | 低 | 頁面上任何 iframe 都收得到我們的訊息。字幕文字本身不敏感，但沒必要外送。修法：改用 `location.origin` |
| S5 | `mergeWrite` 沒擋 bundle 上限 | 低 | `BUNDLE_MAX_LINES` 原本只在 `handlePostSubs` 檢查，但擴充功能上線後絕大多數寫入走 `mergeWrite`。修法：兩邊都擋 |

## 未修正 —— 上線前必須處理

| # | 問題 | 嚴重度 | 為什麼還沒修 |
|---|---|---|---|
| **S6** | **`CLIENT_TOKEN` 是所有人共用的單一密鑰** | **極高** | 這是目前最大的架構問題。權杖隨擴充功能發給每一位使用者，**等於公開**——任何人解開 `.crx` 就能取得，然後無限呼叫 `/v1/translate` 燒你的 Anthropic 額度。搭配 `Access-Control-Allow-Origin: *`，任何網站都能直接打。**沒有 P3 的每人 JWT，這個洞補不起來** |
| **S7** | `ADMIN_TOKEN` 曾在對話中明文出現兩次 | **高** | 使用者決定上線前再換。**上線前務必執行** `wrangler secret put ADMIN_TOKEN` |
| S8 | 速率限制可被繞過 | 中 | 以 IP 為鍵，換 proxy 即可繞過；而且是**取樣寫入**（1/10 機率），攻擊者可能連續矇混。改用 JWT 的使用者 ID 當鍵才有意義（依賴 S6） |
| S9 | 共用快取可被 prompt injection 汙染 | 中 | `/v1/translate` 的 `lines` 直接進模型，攻擊者能構造輸入讓輸出受其控制，再寫進**他指定的 `cid`** 的 bundle。所有後續觀看者都會拿到那份被汙染的譯文。緩解方向：譯文長度／字元集合理性檢查、可疑內容不寫回 bundle、保留管理員覆寫能力 |
| S10 | `/v1/complete` 可被濫用 | 低 | 任何持有 CLIENT_TOKEN 的人都能把任一 `cid` 標記為完整。後果是別人跳過整軌預抓、退回 worker 攔截（仍可用），不會弄壞既有譯文。**刻意接受**，因為代價低於「沒有跳過機制」 |
| S11 | 權杖比對用 `!==` | 資訊性 | 理論上的時序攻擊。跨網路且有速率限制，實務上不可行。改用 JWT 後自然消失 |

## 資料處理

**收集什麼**：不收集。翻譯只送出畫面上的英文字幕文字。

**存什麼**：
- 本機（`chrome.storage.local`）：顯示設定、存取權杖、遠端設定快取
- 後端 KV：`cid` → 英文字幕的正規化鍵 → 中文譯文。**不含任何使用者識別資訊**

**不做什麼**：不讀瀏覽紀錄、不讀其他分頁、不上傳 F1TV 帳號資訊。
v0.4.0 已移除全部 cookie 讀取程式碼與 `cookies` 權限。

**權限最小化**：目前只有 `storage` 與 `clipboardWrite`，host 限三個網域。

## 上線前的資安檢查清單

| # | 項目 | 狀態 |
|---|---|---|
| 1 | 輪換 `ADMIN_TOKEN`（S7） | ⬜ |
| 2 | 每人 JWT 取代共用 `CLIENT_TOKEN`（S6） | ⬜ |
| 3 | CORS 收斂到擴充功能來源，不再用 `*` | ⬜ |
| 4 | 速率限制改以使用者為鍵，並改成每次都寫（S8） | ⬜ |
| 5 | 譯文寫回前的合理性檢查（S9） | ⬜ |
| 6 | 移除測試工具 `__pitlingo.t.*` | ⬜ |
| 7 | 成本警戒線與自動熔斷 | ⬜ |
| 8 | 隱私政策與使用條款上線 | ⬜ |
| 9 | 撤銷清單（退款／盜用時停權） | ⬜ |

## 金流相關（P3 實作時的硬性要求）

- **絕對不自行儲存信用卡號**。一律走綠界／PayPal 的託管頁面，我們只收 webhook
- webhook 必須驗簽，並防重放（記錄已處理的交易序號）
- JWT 用非對稱簽章，私鑰只在後端
- **後端掉線時不要鎖住已付費使用者**——JWT 還在有效期內就繼續服務。
  伺服器出問題是我們的錯，不該讓他們看不到字幕
- 退款／爭議 → 加入撤銷清單，用戶端每天檢查一次
