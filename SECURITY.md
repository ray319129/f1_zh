# 資安檢視

> 2026-08-16 對 `extension/`、`backend/`、`f1tv-zh-subtitles.user.js` 的完整攻擊面檢視。
> 上線前必須把「未修正」欄位全部清空。

## 已修正

| # | 問題 | 嚴重度 | 說明與修法 |
|---|---|---|---|
| S1 | **診斷報告外洩 CDN 存取權杖** | **高** | 字幕清單網址裡的 base64 解開含 `sessionId` 與 24 小時有效的存取權杖（`ttl:1440`）。報告是設計來貼給別人看的，**貼出去等於送出該串流的存取權**。v0.4.0 為了偵錯拿掉截斷時破壞了「報告不含權杖」的原設計，期間已產生 6 份以上外洩的報告。修法：`maskUrl()` 遮蔽權杖但保留長度與頭尾，仍能判斷網址有沒有被截斷 |
| S2 | **SW 可被誘導抓任意網址** | **高** | `fetchText()` 沒有任何網址驗證，且帶 `credentials: 'include'`。字幕清單網址來自 `window.postMessage`，而**頁面上任何腳本都能偽造那個訊息**——F1TV 頁面的第三方腳本（廣告、分析、被入侵的 CDN）就能讓擴充功能帶著使用者 cookie 去抓任意網址。修法：`FETCH_ALLOW` 網域白名單 + 強制 https |
| S3 | **成本放大攻擊** | 中 | `/v1/translate` 限制 200 句，但**沒有限制每句長度**。200 句 × 100KB = 20MB 進模型，一次請求就能燒掉大量 token。修法：單句上限 1,000 字元（真實字幕不超過 300） |
| S4 | `postMessage` 用 `'*'` | 低 | 頁面上任何 iframe 都收得到我們的訊息。字幕文字本身不敏感，但沒必要外送。修法：改用 `location.origin` |
| S5 | `mergeWrite` 沒擋 bundle 上限 | 低 | `BUNDLE_MAX_LINES` 原本只在 `handlePostSubs` 檢查，但擴充功能上線後絕大多數寫入走 `mergeWrite`。修法：兩邊都擋 |

## 已修正（第二輪：v2.0 / v0.7.0）

| # | 問題 | 修法 | 驗證 |
|---|---|---|---|
| **S6** | `CLIENT_TOKEN` 是所有人共用的單一密鑰 | **每個安裝一枚權杖**。`POST /v1/register` 匿名換發（不需帳號、不收個資），格式 `installId.exp.HMAC`，驗證只算 HMAC 不讀 KV。舊的共用權杖仍接受但標記 legacy，僅為相容 userscript | `check-backend.js`：竄改簽章／換 installId／過期／他人 secret 簽發，六種情境全部必須被拒 |
| S8 | 速率限制以 IP 為鍵可繞過 | 改以 `installId` 為鍵。換 proxy 不再有用，要繞過就得不斷重新註冊，而註冊另有 IP 限流 | — |
| S9 | 共用快取可被 prompt injection 汙染 | `plausibleTranslation()`：譯文暴長、含注入殘留、無中文、空值一律**不寫回 bundle**（仍回傳給請求者） | 六種汙染樣本全部必須被擋，正常譯文必須放行 |
| S11 | 權杖比對用 `!==` | `safeEqual()` 常數時間比對 | 行為正確性已驗 |
| — | CORS `*` | **整個拿掉**。擴充功能的 SW 有 host_permissions、userscript 用 GM_xhr，兩者都不受 CORS 限制——我們根本不需要它 | — |
| — | 沒有成本上限 | 每日美元熔斷（`DAILY_USD_CAP`，預設 $5）。超過就停掉 `/v1/translate`，**但共用快取的讀取照常**，已翻好的影片完全可用 | 成本計算已驗 |

## 授權模型 —— 綁人，不綁裝置

> **v2.0 把這件事做錯了。** 當時用「安裝權杖」同時當濫用防護與付費憑證，
> 結果是付費使用者換一台電腦就失效。這兩件事必須分開：

| | 綁什麼 | 用途 | 換裝置 |
|---|---|---|---|
| 安裝權杖 | 裝置 | 限額、撤銷、分階段推送分流 | 重新匿名換發即可 |
| **授權碼** | **人** | 證明付過錢 | **必須還在** |

授權碼 `PL-XXXX-XXXX-XXXX`，付款成功後寄到金流回傳的 email。
在任何裝置貼上就能啟用，最多 3 台，可自行解除，可用 email 補發。
**沒有密碼、沒有註冊流程、不另外收集個資**——就是獨立軟體最常見的做法。

幾個刻意的選擇：

- **撞到裝置上限時不自動踢掉別台。** 使用者可能正在另一台看比賽，
  靜默踢掉會變成兩台互相把對方擠下線。讓他自己決定解除哪一台。
- **解除裝置不需要該裝置的權杖。** 電腦壞了、賣掉了、重灌了都要能處理。
- **通行證的驗證在伺服器，不在用戶端。** 用戶端沒有 `TOKEN_SECRET`，
  它讀 `exp` 只是為了決定 UI 顯示什麼——那是體驗不是防護。
  真正的牆在 `/v1/translate`：偽造的通行證拿不到翻譯。
  好處是**後端掛掉時用戶端不會因為驗不了簽就把功能鎖起來**。

## 未修正 —— 上線前必須處理

| # | 問題 | 嚴重度 | 為什麼還沒修 |
|---|---|---|---|
| **S7** | `ADMIN_TOKEN` 曾在對話中明文出現兩次 | **高** | 使用者決定上線前再換。**上線前務必執行** `wrangler secret put ADMIN_TOKEN` |
| S10 | `/v1/complete` 可被濫用 | 低 | 任何持有 CLIENT_TOKEN 的人都能把任一 `cid` 標記為完整。後果是別人跳過整軌預抓、退回 worker 攔截（仍可用），不會弄壞既有譯文。**刻意接受**，因為代價低於「沒有跳過機制」 |

## 資料處理

**收集什麼**：不收集。翻譯只送出畫面上的英文字幕文字。

**存什麼**：
- 本機（`chrome.storage.local`）：顯示設定、存取權杖、遠端設定快取
- 後端 KV：`cid` → 英文字幕的正規化鍵 → 中文譯文。**不含任何使用者識別資訊**

**不做什麼**：不讀瀏覽紀錄、不讀其他分頁、不上傳 F1TV 帳號資訊。
v0.4.0 已移除全部 cookie 讀取程式碼與 `cookies` 權限。

**權限最小化**：目前只有 `storage` 與 `clipboardWrite`，host 限三個網域。

## 上線前的資安檢查清單

| # | 項目 | 狀態 |
|---|---|---|
| 1 | **輪換 `ADMIN_TOKEN`**（S7）—— 使用者決定上線前再做 | ⬜ |
| 2 | 設定 `TOKEN_SECRET` 與 `DAILY_USD_CAP` | ✅ 已設定 |
| 3 | 部署 backend v2.0，確認舊安裝能自動換發新權杖 | ⬜ |
| 4 | 每人權杖取代共用 `CLIENT_TOKEN`（S6） | ✅ |
| 5 | 拿掉 CORS `*` | ✅ |
| 6 | 速率限制改以 installId 為鍵（S8） | ✅ |
| 7 | 譯文寫回前的合理性檢查（S9） | ✅ |
| 8 | 成本警戒線與自動熔斷 | ✅ |
| 9 | 撤銷清單（`POST /v1/admin/revoke`） | ✅ |
| 10 | 移除測試工具 `__pitlingo.t.*` | ⬜ |
| 11 | 隱私政策與使用條款（`legal/`，由 `check-legal.js` 確保與程式碼一致） | ✅ 已撰寫，待部署到 pitlingo.com |
| 12 | 淘汰 legacy `CLIENT_TOKEN`（確認所有安裝都換發後） | ⬜ |

## 金流相關（P3 實作時的硬性要求）

- **絕對不自行儲存信用卡號**。一律走綠界／PayPal 的託管頁面，我們只收 webhook
- webhook 必須驗簽，並防重放（記錄已處理的交易序號）
- JWT 用非對稱簽章，私鑰只在後端
- **後端掉線時不要鎖住已付費使用者**——JWT 還在有效期內就繼續服務。
  伺服器出問題是我們的錯，不該讓他們看不到字幕
- 退款／爭議 → 加入撤銷清單，用戶端每天檢查一次
