# 全面審查報告

> 2026-08-16。以 production-ready 為標準，對 `extension/`、`backend/`、
> `f1tv-zh-subtitles.user.js` 做架構、安全、可靠性、成本、併發、資料一致性的完整檢視。
>
> **原則：每一項都實際查證過，不寫「建議考慮」這種沒有結論的東西。**
> 沒查證的一律標成「未驗證」。

---

## 一、本次審查修正的缺陷

全部都是**實際存在、已驗證、已修正**的，不是理論風險。

| # | 缺陷 | 類別 | 為什麼會發生 |
|---|---|---|---|
| A1 | `noteFreeUsage` 取樣數學錯誤 | 成本 + 正確性 | `if (Math.random() >= n/5) return;` 在 `n≥5` 時機率恆為 1 → **每批都寫一次 KV**（一支影片 100 puts，走回坑 #19）；且固定 `+5`，`n=20` 時只記 5 句 → 免費額度低估 4 倍 |
| A2 | 遠端的 `freeTier` 永遠不生效 | 靜默失效 | `freeTier` 掛在設定**根層**，用戶端卻讀 `site.freeTier`（`sites[i]` 底下），永遠 `undefined` → 一直用內建值，且不報錯 |
| A3 | 早鳥「限量 20」根本沒實作 | 商業 | `EARLY_LIMIT` 與 `earlyRemaining` 都寫了，**但沒有任何地方呼叫** |
| A4 | userscript 收割器會被免費額度擋死 | 功能性 | `checkEntitlement` 用 `auth.installId`，legacy 全體共用 `'legacy'` 一個桶。第一個人用完 800 句其他人全擋；而收割器一支影片就要翻上千句 → **共用快取的來源被掐死** |
| A5 | 早鳥會賣超 | 併發 | `讀 count → +1 → 寫回` 在 KV 上是 lost update。兩筆同時付款都讀到 19 → 發出 21 組。而且計數器會與實際發出的碼漂移，漂了對不回來 |
| A6 | manifest 最壞佔 14 MB 常駐記憶體 | 可靠性 | `MANIFEST_MAX` 只限**份數**，但每份最多 600,000 字元。而且 body 的唯一用途是找出字幕清單位址，找到後就沒用了 |

**A3 與 A4 的共同點**：兩個都是「寫了但沒接上」。這在這個專案已經是第三次
（第一次是後台換方案的 UI）。因此新增 `tools/check-admin.js` 的
「每個管理端點都要有 UI 在用」與後端的懸空引用檢查——**做了等於沒做，
比沒做更危險，因為你以為它在保護你**。

---

## 二、已知但**刻意接受**的風險

寫下來是因為「沒寫下來的接受」等於「忘記了」。

| # | 風險 | 為什麼接受 | 什麼時候要重新檢視 |
|---|---|---|---|
| R1 | 統計與指標的 lost update（`flushStats`／`flushMetrics`） | 成本與產品數據是**近似值**就夠用。精確化要 Durable Objects，複雜度與費用不划算 | 當數據要拿來對外報告時 |
| R2 | 速率限制取樣寫入，非精確 | 精確計數會撞爆 KV 1,000 puts/天。真正的成本防線是每日美元熔斷 | 遭遇實際濫用時 |
| R3 | 免費額度在併發下會低估 | 方向對使用者有利，且同一安裝很少併發 | 免費額度被大規模濫用時 |
| R4 | `revoked` 清單的 lost update | 管理員動作、頻率極低。但**後果是被封鎖的安裝仍然有效** | 有實際濫用需要批次封鎖時 |
| R5 | 刪除授權碼會連帶清掉 `order:` 去重鍵 | 這是刻意的——讓同一筆訂單可以重新發碼補救。副作用是舊 webhook 重放會發出新碼 | 若金流會重送超過 400 天前的通知 |
| R6 | 譯文汙染防護擋不了「語意正確但翻錯」 | 那需要人工判斷。目前靠管理員可覆寫 | 出現實際的惡意汙染時 |
| R7 | 用戶端的免費層閘門可被繞過 | **刻意分層**：用戶端是產品承諾，伺服器的每日 800 句才是成本防線 | 若伺服器閘門被繞過 |

---

## 三、尚未驗證的部分（**不要當成已完成**）

| # | 項目 | 為什麼還沒驗 |
|---|---|---|
| U1 | **直播路徑** | 三個產物的滑動視窗補抓、`isVod` 判定、直播下的免費層計時，**全部只有程式碼，沒有跑過一場真的直播** |
| U2 | 金流端到端 | 邏輯（驗簽／防重放／限量）已驗，但沒有真實綠界帳號跑過 |
| U3 | `killSwitch` 與 `rollout` 的實際推送 | 邏輯已驗，沒有對真實使用者推過 |
| U4 | 診斷回報的 email 通知 | 需要已驗證的寄件網域（`pitlingo.com` 尚未購買） |
| U5 | 長時間記憶體表現 | `memo` 上限 12,000 從未實際觸發過 |
| U6 | 多人同時使用同一支影片 | `mergeWrite` 的實測併發只到 3（單一用戶端） |

**U1 是最大的未知數。** 賽季開始前一定要找一場直播驗證。

---

## 四、架構層面的觀察

### 4.1 目前的分層是對的

```
用戶端  產品承諾與體驗（免費層 15 分鐘、授權 UI、狀態顯示）
伺服器  真正的閘門（每日句數、通行證驗簽、成本熔斷）
```

**用戶端的任何檢查都只是體驗**——它沒有 `TOKEN_SECRET`，使用者也能改自己的
storage。這個分層讓「後端掛掉時不鎖住已付費使用者」與「成本有上限」
可以同時成立。

### 4.2 最脆弱的地方是 KV 沒有 CAS

目前有 12 處 read-modify-write。已用 `mergeWrite` 處理最關鍵的譯文寫入
（實測回寫率 41% → 99.7%），其餘見上表 R1／R3／R4。

**真正要零損失只有 Durable Objects 一條路。** 目前的量級不值得，
但如果同時觀看人數上到三位數，`bundle` 的寫入衝突會重新變成問題。

### 4.3 三份 `normKey()` 與兩份 `SYSTEM_PROMPT` 是必要之惡

userscript 必須是單一檔案、backend 由 wrangler 打包，沒有共用模組的辦法。
靠 `check-normkey.js` 與 `check-prompt.js` 擋漂移。**這個取捨要保留**——
為了消除重複而改用建置流程，會讓 userscript 失去「複製貼上就能用」的優點。

### 4.4 遠端設定是最重要的單點

F1TV 改版時它是唯一能在幾分鐘內救回所有使用者的手段。已有的保護：
`rollout` 分流、`config:prev` 保留上一版、用戶端 30 秒自我檢查回退、
`killSwitch` 總開關。

**風險是它同時也是最容易一次推壞所有人的東西。** 上線後推設定必須從
`rollout: 5` 開始。

---

## 五、上線前必須完成（依阻擋程度排序）

| # | 項目 | 為什麼是阻擋項 |
|---|---|---|
| 1 | **輪換 `ADMIN_TOKEN`** | 已在對話與截圖中外洩多次。它能發碼、刪碼、看所有回報 |
| 2 | **移除測試工具** `__pitlingo.t.*` | `t.prefetch()` 繞過所有防護；`t.memo()` 洩漏內部狀態 |
| 3 | 設定 `TOKEN_SECRET` 與金流驗簽金鑰 | 未設定時 webhook 直接拒絕（刻意），但授權權杖會退回由 `ADMIN_TOKEN` 衍生 |
| 4 | 隱私政策與使用條款 | 商店審核硬性要件，且需要網域託管 |
| 5 | 一場完整正賽 + **一場直播**的實測 | U1 |
| 6 | 改用 `wrangler versions deploy` | 上線後 `wrangler deploy` 等於 100% 立即切換，沒有回頭路 |

---

## 六、這次審查沒有做的重構

以下都是「看起來可以更乾淨，但沒有明確理由」的，**刻意不動**：

- `backend/src/index.js` 已經 2,400 行，可以拆檔——但 wrangler 打包後沒有差別，
  拆檔會讓「一個檔案就是全部」這個優點消失，而那對交接很有價值
- `main.js` 的顯示邏輯與收割邏輯可以分離——但它們共用大量狀態，
  分離後要傳遞的參數比現在的耦合更難維護
- 錯誤處理可以統一成 Result 型別——現在的 try/catch 雖然重複，
  但每一處的復原策略都不同，統一反而會掩蓋差異

**重構的門檻是「解決一個實際發生過的問題」，不是「看起來更好」。**
