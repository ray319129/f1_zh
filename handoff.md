# F1TV 繁中即時字幕 — 專案交接文件

> **這份文件是專案的單一事實來源。每次修改程式碼都必須同步更新這裡與 `TUTORIAL.md`。**

| 項目 | 內容 |
|---|---|
| 目前版本 | `v4.8.0`（userscript）／ `v1.7`（backend）／ `v0.5.4`（擴充功能） |
| 最後更新 | 2026-08-16 |
| 狀態 | 個人自用版完成並驗證；**P1 後端已部署並驗證共用生效** |
| 主檔案 | `f1tv-zh-subtitles.user.js`、`backend/` |

---

## 1. 專案是什麼

在 Chrome 觀看 F1TV 時，攔截官方的英文 CC，透過 Claude Haiku 4.5 即時翻譯成台灣用語的繁體中文，以雙語疊字顯示在影片上。

**核心設計原則：播放器負責「何時顯示」，我們只負責「顯示什麼」。** 顯示時機完全交給 F1TV 播放器的 DOM 更新，我們不碰時間軸對齊——這是同步永遠正確的原因。

---

## 2. 已驗證的技術事實（本專案最有價值的資產）

以下全部是實測確認，不是推測。這些是好幾輪逆向偵錯換來的。

### 2.1 F1TV 播放器架構

| 項目 | 事實 |
|---|---|
| 串流協定 | **HLS**（API 端點 `WEB_HLS/ALL/CONTENT/PLAY?...&player=player_tm`） |
| 播放器 | 自訂播放器，CSS class 前綴 `tm-`。**不是** Shaka / Bitmovin / hls.js |
| 核心 | 約 **7 MB 的 WASM** 模組 |
| 網路層 | **全部在 blob: Web Worker 內執行** |
| 原生文字軌 | **`video.textTracks.length === 0`** — 沒有原生字幕軌可用 |
| 字幕渲染 | 由 worker 解析後交給主執行緒畫進 DOM |

### 2.2 關鍵 DOM 結構

```
div.tm-subtitle-region-container      ← 監聽這個節點（MutationObserver）
  └─ div.tm-container-wrapper
       └─ span.tm-ui-subtitle-label   ← 讀這個的文字
            └─ span                    ← 實際文字節點
```

### 2.3 串流資源

- **Master playlist 網域**：`ott-video-fer-cf.formula1.com`
- **路徑形式**：`/v2/pa_<很長的 base64 授權 token>/<hash>/<hash>/index_N.m3u8`
- **token 內含**：path、kid、exp（到期時間戳）、ttl（1440 分鐘 = 24 小時）、geo、streamType
- **字幕軌宣告**：master 內以標準 HLS 的 `#EXT-X-MEDIA:TYPE=SUBTITLES` 標記，帶 `LANGUAGE="eng"` 與相對路徑的 `URI`
- **重播判定**：字幕 media playlist 含 `#EXT-X-ENDLIST` → VOD；沒有 → 直播

### 2.4 實測數量級（2026 Pre-Season Testing Day 1，約 1 小時）

| 指標 | 數值 |
|---|---|
| 字幕分段數 | 622 段（每段約 6 秒） |
| 總字幕句數 | 約 1,101 句 |
| 字幕出現頻率 | 約每 3~4 秒一句 |
| **cue 提前量中位數** | **47,000 ~ 53,000 ms** |

> **「提前量」= 從 VTT 分段抵達，到該句真正顯示在畫面上的時間差。**
> 這是自適應批次的預算來源。播放器的字幕預載遠比直覺深。

---

## 3. 腳本架構

### 3.1 三層取得字幕的路徑（自動降級）

```
第 1 層  整軌預抓（僅重播）
         攔 master m3u8 → 找字幕清單 → 抓全部 VTT 分段 → 一次全翻
              ↓ 失敗
第 2 層  Worker 即時攔截
         注入 hook 到 blob worker → 攔它的 fetch/XHR → BroadcastChannel 回傳
              ↓ 失敗
第 3 層  DOM 逐句翻譯
         字幕出現在畫面上才翻（最貴，但永遠可用）
```

**任何一層失效都只是變貴，不會壞掉。**

### 3.2 顯示路徑（唯一，且獨立於上述三層）

```
每 250 ms 主動輪詢 collectCaption()          ← 主力，不可能失效
   ↑ MutationObserver 只是加速器（有效時瞬間反應）
   → 讀取主畫面容器內的 .tm-ui-subtitle-label 的 textContent
   → 正規化後查本地快取
   → 命中：0 ms 顯示 ／ 未命中：送逐句翻譯
```

**設計原則：不要相信 hook，要驗證輸出。** 見決策 4.6。

### 3.3 Worker 注入原理

F1TV 的串流網路請求全在 blob worker 內，主執行緒 patch `fetch`/`XHR` 完全攔不到。解法：

```
攔截 URL.createObjectURL
   → 判斷是 Blob 且看起來是 worker 腳本
   → 同步 XHR 讀出原始碼（blob: 讀取無網路成本）
   → 把 hook 前置後產生新 Blob，回傳新的 blob URL
   → hook 在 worker 內部 patch 它自己的 fetch/XHR
   → 用 BroadcastChannel（不是 postMessage）送回主執行緒
```

**為什麼用 BroadcastChannel 而不是 postMessage**：postMessage 會把我們的訊息塞進播放器自己的訊息通道，它的 handler 若對未知格式拋錯，播放會直接壞掉。BroadcastChannel 是完全獨立的頻道。

**為什麼 MSE 不會被誤傷**：`URL.createObjectURL(mediaSource)` 的參數是 MediaSource 不是 Blob，`instanceof Blob` 檢查直接放行。

---

## 4. 關鍵決策紀錄

> 記錄「為什麼這樣做」，避免未來重蹈覆轍。

### 4.1 不使用 VTT 的時間軸來決定顯示時機

對齊 HLS 分段 VTT 的 `X-TIMESTAMP-MAP` 到 `video.currentTime` 很脆弱，容易做出「翻譯正確但時機歪掉」的體驗。改由播放器的 DOM 更新當顯示觸發器，同步永遠正確。

**但商品版應該把時間軸存進快取當備胎**——見第 8 節。

### 4.2 System prompt 刻意寫長到超過 4,096 tokens

**Haiku 4.5 的 prompt caching 最小前綴是 4,096 tokens。** 未達門檻時加了 `cache_control` 也會靜默失敗。

卡在門檻下是**最貴的位置**。跨過去之後每次呼叫的輸入只收 0.1 倍。目前 system prompt 約 5,050 tokens。

**修改術語表時務必用選單的「🧮 檢查 token 數」確認仍在門檻之上。**

> **這條規則一度只套用在 userscript 上。** 後端從 v1.0 到 v1.3 都放精簡版（約 650 tokens），`cache_control` 因此**從未生效**，而擴充功能的翻譯 100% 走後端。
> v1.4 起兩份 prompt 逐字相同，由 `node tools/check-prompt.js` 把關。詳見坑 #21。

### 4.3 逐句與批次共用同一份 system prompt

兩種模式共用同一份，才能共用同一份 prompt cache。批次模式的指示寫在 prompt 內部，靠使用者訊息的格式區分。

### 4.4 放棄「漸進顯示」（英文先上、中文後補）

實作過但移除。原因：語速快時 `displayRaw` 已換到下一句，舊句譯文被判定過期而不顯示，結果變成**整段只看得到英文**，比原本更糟。程式碼保留但 `CFG.progressive = false`。

### 4.5 自適應批次等待

固定等待時間是瞎猜：等太短批次湊不大，等太長漏接率飆升反而更貴（漏接一句要付一次完整的逐句呼叫）。

改為量測「cue 入列 → 畫面顯示」的實際間隔，取中位數的一半再扣掉翻譯耗時。**只用兩個牆鐘時間相減，不碰時間軸對齊。**

### 4.6 顯示偵測改為「輪詢為主、observer 為輔」

**這是回應「切換車手鏡頭後字幕不再出現」的一般性修法。**

原本的寫法是 `if (node.__f1zh) return;` —— 掛上 MutationObserver 後就相信它永遠有效。但 F1TV 是 SPA，切換視角、換畫質、換影片都會**銷毀並重建播放器 DOM**，observer 隨之靜默失聯：不報錯、不再觸發，字幕就這樣停了。

改法：

| 角色 | 機制 | 性質 |
|---|---|---|
| **主力** | 每 250 ms 主動 `collectCaption()` 比對 | **不依賴任何註冊狀態，不可能失效** |
| 加速器 | MutationObserver | 有效時瞬間反應；失效時由輪詢在 250 ms 內接手 |

同時處理的四件事：

1. **節點集合變動就整組重掛** observer（不再用一次性標記）
2. **字幕清空時重置 `lastRaw`** —— 否則切回主訊號時，若第一句與切走前相同會被去重擋掉（這正是原始症狀的成因之一）
3. **`mount()` 加入輪詢** —— 疊字層可能隨播放器重建被移除
4. **contentId 變動偵測** —— SPA 換影片時重置預抓狀態、manifest、提前量樣本

**通用原則：任何「註冊一次然後假設它持續有效」的機制，在 SPA 裡都要有主動驗證的備援。**

---

## 5. 踩過的坑（重要，別再犯）

| # | 坑 | 症狀 | 原因與修法 |
|---|---|---|---|
| 1 | **`innerText` vs `visibility:hidden`** | 元素存在但讀到空字串，永遠不觸發翻譯 | `innerText` 是版面感知的，元素被隱藏時回傳 `""`。**一律用 `textContent`**，隱藏改用 `opacity:0`（同時保留版面供定位用） |
| 2 | **Tampermonkey 沙箱隔離** | `__f1zh is not defined` | 沙箱的 `window` 不等於頁面的。要 `@grant unsafeWindow` 並同時掛上去 |
| 3 | **網址截斷** | 整軌預抓 HTTP 400 | worker 回傳網址時做了 `.slice(0,300)`，把 base64 授權 token 切掉。相對路徑解析時 token 那段被吃掉。**網址絕不截斷** |
| 4 | **失敗後無限重試** | Console 洗版、反覆打 CDN | `startFullPrefetch` 掛在 1.5 秒輪詢裡，失敗重設旗標等於永不停止。**要加重試上限** |
| 5 | **偵錯工具被自己過濾** | netlog 永遠是空的 | 只記錄「通過 `isVttish` 的網址」，等於把偵錯工具擋在要偵錯的過濾器後面。**探索期要記錄全部** |
| 6 | **批次沒有聚合** | 平均批次只有 2 句，省不到錢 | 每個 VTT 分段一到就 drain，而每段只含約 2 句。**要用「最長等待」聚合，不是 debounce**（debounce 會因持續有新資料而無限延後） |
| 7 | **manifest 內容被截斷** | 字幕清單解析不完整 | 兩小時正賽的字幕清單有上千段，40,000 字元不夠。已提高到 600,000 |
| 8 | **切換車手鏡頭後字幕不再出現** | 切回主轉播訊號要「重新點一次」才有字幕 | 兩個原因疊加：(a) 播放器重建 DOM，掛在舊節點上的 observer 靜默失聯；(b) `lastRaw` 去重把「切回來第一句 == 切走前最後一句」的情況擋掉。修法見決策 4.6 |
| 9 | **單句批次的編號協定失效** | `批次對應率偏低（0/1）`，整批作廢 | prompt 內部衝突：批次只有 1 句時，模型照【輸出規則】「只輸出譯文本身」直接給譯文而不加編號。修法：(a) `lines.length === 1` 走逐句路徑；(b) prompt 明寫「即使只有一句也要編號」；(c) 解析失敗時若回應行數等於輸入行數就按位置對應 |
| 11 | **contentId 只認一種網址** | 從 `/page/<id>/...` 進去看影片時完全不上傳，整支的翻譯成果全丟 | F1TV 有多種網址形式，只有 `/detail/<id>/` 帶 contentId。修法：從經過的網址中抓 `contentId=(\d+)`（PLAY API 一定會帶），網址解析失敗時用觀察值 |
| 12 | **手動觸發沒有併發鎖** | 1,056 段被抓了 2,112 次 | 選單的「立即整軌預抓」重設 `fullPrefetchStarted` 就再跑一條，與進行中的那條並行。修法：`harvestInFlight` 鎖 + `harvestGen` 世代編號（換影片時遞增，跑到一半的迴圈自行中止） |
| 13 | **只在全部翻完才上傳** | 1,669 句要翻約 5 分鐘，中途關掉分頁就全白費 | 改為增量上傳：只送差集（`uploadedKeys`），每 60 秒一次，加上 `pagehide` 用 `fetch(keepalive)` 做最後一搏 |
| 14 | **無旁白片段被誤判為故障** | 開場動畫、車手宣傳片、純音樂剪輯時跳出「未偵測到字幕」 | 「播放 45 秒沒字幕」無法分辨「壞了」與「這段本來就沒人說話」。修法：以 `everSawCaption` 為關鍵——**看過任何一句就永不再提示**；從未看過時再依有無 VTT 資料決定訊號強度（有資料 = CC 沒開，高訊號值得提示；無資料 = 可能真的沒字幕，只記 Console）。**寧可漏報也不要誤報** |
| 15 | **收割中止只看 contentId** | 點播放器返回鍵時網址沒變，收割繼續跑完上千段 | 播放器被拆掉但 SPA 網址不變。修法：加 `harvestShouldStop()`，以「`video` 元素消失超過 5 秒」為訊號（5 秒緩衝避開播放器重建時的短暫消失） |
| 16 | **跨影片 memo 污染導致 bundle 缺角** | 顯示正常，但伺服器上該片的譯文是缺的，下一個全新觀看者要重翻 | 收割時 `memo.has(k)` 就整句跳過，既不進佇列也不進 `sessionKeys`。切換影片時 `memo` 刻意不清（重看可受惠），於是前一支影片翻過的重複句子在新影片就被吃掉。**完全不會報錯，只會讓別人默默多花錢。** 修法：已有譯文時仍 `sessionKeys.add(k)`，因為那句確實出現在這支影片的 VTT 裡 |
| 17 | **手動觸發預抓會默默重抓整支** | 按一下 🎯 就對 CDN 再發近千個請求，畫面完全沒提示 | `force: true` 是**刻意**繞過所有自動防護的（手動就該能強制），但按鈕沒有在「已經抓完」時攔住使用者。診斷報告裡出現的兩次重複收割都是這個原因，不是自動路徑的 bug。修法：(a) 收割完成時把成果登記成本機的 `bundleSegCount`／`bundleLineCount`，任何重新進入都會走既有的「跳過收割」判斷；(b) 按鈕在已完整時跳確認對話框；(c) 強制觸發會在時間軸留下 `⚠ 手動強制整軌預抓` |
| 18 | **過時的譯文會重新彈出** | 字幕停止後，同一句舊字幕在不同時間斷斷續續重複出現 | `flushPending` 用 `currentEn` 判斷要不要「補顯示」，但那個變數只反映「最後一次看到的字幕」，且從未清空。翻譯慢回來時畫面早已換過，於是把過時的句子重新畫一次。**正常運作時也會發生**（講話空檔、切換視角後）。修法：(a) 字幕清空時一併清 `currentEn`；(b) 補顯示前**當場重讀 DOM 比對**，只有那句此刻仍在畫面上才顯示 |
| 19 | **KV 寫入爆量，一場就超額** | 播到一半連續 `HTTP 500`，Cloudflare 通知 `exceeded the daily Workers KV free tier limit of 1000 put operations` | `/v1/translate` 每翻一句寫一個 `line:` key，加上每次請求寫一次速率限制計數。實測 550 句 = 550+ puts；一場正賽約 2,000 句 = **超額 2 倍**，之後所有寫入回 429，用戶端表現為 500。修法：(a) 改成「讀一次 bundle、寫一次 bundle」，一次請求固定 1 put；(b) 速率限制改取樣，每 10 次才寫一次並直接加 10 補回。一場正賽從 2,100 puts 降到 110 |
| 20 | **分頁被節流時輪詢整個停擺** | 看到一半字幕停了一小段，動一下滑鼠就恢復 | 推測是 Chrome 對被遮蔽／背景分頁的計時器節流，`setInterval` 被拉長到每分鐘一次，250ms 輪詢與 1.5 秒結構檢查全部失效。修法：`checkPollStall()` 偵測「上次輪詢到現在超過 8 倍間隔」就強制整組重掛觀察者並清除去重狀態；另在 `focus`／`visibilitychange` 立刻重檢。**不論真正原因為何，處理方式都一樣——偵測到自己被凍結過，就不要假設狀態還新鮮** |
| 10 | **收割期間批次被計時器切碎** | 整軌預抓時平均批次只有 2.7 句 | 分段會在幾十秒內全部到齊，但 8 秒的 flush 計時器把零星新句切成一堆迷你批次。修法：收割期間停用計時器 flush（`harvesting` 旗標），只在湊滿 `batchSize` 或收割結束時才送 |
| 21 | **後端的 prompt 快取從來沒生效過** | 沒有任何症狀。不報錯、不失敗，只是每次都全額計費 | 後端刻意放「精簡版」`SYSTEM_PROMPT`（34 條術語，約 650 tokens），但 Haiku 的最小可快取前綴是 **4,096 tokens**，低於門檻時 `cache_control` **靜默失效**。同一個坑 userscript 早就踩過並寫進鐵則，後端卻沒套用。連帶第二個損害：擴充功能的翻譯 100% 走後端，所以快取沒命中時使用者拿到的是精簡術語表的譯文，比 userscript 差。修法：把 userscript 那份完整 prompt（273 條術語、16KB）整份搬進後端，兩個問題一起解決；新增 `tools/check-prompt.js` 擋日後漂移。**教訓：一條規則在某個檔案上學到，要主動去看它在別的檔案上成不成立** |

| 22 | **刪死碼時連帶刪掉還在用的宣告** | `ReferenceError: manifests is not defined` 洗版，連診斷都匯不出來 | 用行號區間刪掉 PLAY API 死碼時，區間起點算錯 8 行，把剛加上的 `const MANIFEST_MAX` / `let manifests = []` 一起帶走。**`node --check` 完全沒反應**——少一個宣告在語法上完全合法，要到執行期才炸。而且錯在 async `boot()` 裡，表現成 `Uncaught (in promise)`，頁面其他功能看起來還在動，更難察覺。修法：(a) 補回宣告；(b) 新增 `tools/smoke-extension.js`——在 Node 裡用假 DOM／假 `chrome.*` 真的把 content script 跑一遍並觸發實際路徑。**已驗證：拿掉宣告時這支會失敗，補回後會通過** |

| 23 | **bundle 記憶體快取沒有 TTL** | 同一支影片預抓完 836 句後重整，共用快取仍只回報 87 句，整支又重翻一次 | `sw.js` 的 `getBundle` 寫成 `if (cached) return cached;`——存了 `at` 卻從來不檢查，等於永久快取。重整頁面時 SW 沒被回收，回的是開場那份舊 bundle。**沒有任何徵兆**：畫面一模一樣，只是帳單多一份、伺服器的譯文永遠追不上。修法：(a) 90 秒 TTL；(b) `getBundle` 支援 `force`；(c) **預抓完成 20 秒後主動回頭查核後端有幾句**，本機與後端差太多就明確警告。與遠端設定那兩層快取是同一類錯——**寫了 `at` 就要有人去讀它** |

| 24 | **並行請求把彼此的譯文覆蓋掉** | 一輪翻了 363 句，後端只多 148 句（41%）。每個請求自己都成功，沒有任何錯誤 | `/v1/translate` 是「讀 bundle → 呼叫模型（1~2 秒）→ 整份寫回」。用戶端同時最多 3 個請求在飛，三個都讀到同一份舊快照再依序覆蓋，只有最後寫的活下來。**經典的 read-modify-write lost update**，而 KV 沒有 CAS。修法：寫回前**重讀再合併**（只補不覆蓋），把競爭視窗從「模型呼叫的 1~2 秒」縮到「讀+寫的幾十毫秒」。做不到零損失，但差了好幾個數量級 |
| 25 | **擴充功能每次都重抓整支，不會跳過** | 每個人開同一支影片都再向 CDN 發 383 個請求、花約 80 秒，而且幾乎抓不到新句子 | userscript 早有 `bundleSegCount === segs.length` 的跳過判斷（坑 #17），擴充功能完全沒有。原因是 `segCount` 只能由 `POST /v1/subs`（ADMIN_TOKEN）寫入，而使用者不該持有 ADMIN_TOKEN，所以這個欄位在擴充功能的世界裡永遠是 0。修法：新增 `POST /v1/complete`（CLIENT_TOKEN，**只能寫 segCount 且只准往上加**），收割完成且零失敗才回報。最壞情況只是別人略過預抓、退回 worker 攔截（仍有提前量），不會弄壞既有譯文 |

| 26 | **HTTP 快取讓剛寫進去的值一分鐘看不到** | 第一次成功「標記完整收割」，第二次開同一支片卻顯示「後端完整度：記錄 0 段」，照樣重抓 383 段 | `GET /v1/subs` 帶著 `cache-control: public, max-age=60`。v1.2 時 bundle 只有管理員會寫，一分鐘陳舊無所謂；現在每個使用者都會改它。**瀏覽器的 HTTP 快取是用戶端清不掉的一層**——`memCache.bundles.delete()` 清得掉自己的記憶體，清不掉底下的 HTTP 快取。時間軸完全吻合：01:03:06 查核存進快取（segCount 0）→ 01:03:11 標記完整 → 01:03:48 重開，42 秒仍在快取內。第二次的 bundle 只花 22ms 回來（真連線要 50~200ms）也證實是快取。修法：後端改 `no-store`，用戶端加 `cache: 'no-store'` 當第二道保險 |

| 27 | **疊字靠固定計時器消失，不跟著來源** | 使用者持續回報「擴充功能比 userscript 慢一拍」，但量測顯示我們只加 1ms | 疊字只靠 `holdMs` 計時器收起，**不管原生字幕還在不在**。F1 一句常在畫面上停 3.5~4 秒：`holdMs=3000` 會讓中文先消失、空白半秒才換下一句（讀起來是「慢一拍」）；`holdMs=7000` 則是講完很久還掛著上一句（讀起來是「對不起來」）。**兩種都像延遲，而且調參數只能二選一。** 使用者的擴充功能是 3000、userscript 預設 7000，差異就從這裡來。修法：輪詢每 100ms 就知道原生字幕清空了，那一刻收起疊字（350ms 寬限避開換句空窗）；`holdMs` 退化成純安全網 |

| 28 | **事件時間軸被同一行洗版** | 跑三小時後，400 筆的時間軸有 150 筆是「⚙ 遠端設定已更新 v1 → v1」，真正重要的紀錄全被擠掉 | `applyRemoteConfig` 每 60 秒判定「設定變了」並整組重掛觀察者、清空去重狀態，但版本號其實沒變。**根因未確認**——加了冪等性測試（同樣內容第二次必須回 `false`）卻無法重現，代表真實環境兩次拿到的設定內容確實不同（可能是 SW 快取層之間的漂移）。既然追不到單一來源，就改成**不論原因為何都不會再發生**：(a) `logEvent` 對連續相同訊息折疊成 `×N`；(b) 版本號沒變時降級成詳細日誌；(c) `window.__pitlingoBooted` 擋重複注入。**診斷報告是唯一的回報管道，被洗版等於瞎了——在出口擋一次，勝過追無窮盡的來源** |
| 29 | **翻譯失敗的句子被永久丟棄** | 沒有症狀。只有 `state.errors++`，畫面正常，共用快取默默缺一塊 | `flushPending` 的 catch 只做 `requested.delete(k)`，那批 20 句既沒放回 `pending` 也沒記錄，**永遠消失**。一次 429（後端每 IP 每分鐘 120 次，同一家用 IP 兩人同時收割就會逼近）或一次網路抖動就是 20 句沒了，下一個人得重新付費。更嚴重的是 `markComplete` 只看 `segFailed`（分段抓取），翻譯失敗完全不擋——**可能把殘缺的 bundle 標成「完整」，之後所有人都跳過預抓、永遠拿到缺角的譯文，而且沒有任何機制會去補**。修法：(a) 失敗排回佇列，每句最多 3 次（無上限會讓 429 更嚴重，鐵則 #3）；(b) 有放棄的句子就不標記完整 |
| 30 | **memo 淘汰會砍掉正在看的那支** | 連看幾支長影片後，畫面上突然開始重翻已經翻過的句子 | `MEMO_MAX` 淘汰時從最舊的砍，但 memo 跨影片共用——連看幾支之後，**正在看的這支的譯文可能就是「最舊的」**。不報錯，只是變慢又變貴。修法：淘汰時跳過 `sessionKeys`。尚未實際觸發（實測連看兩支才 4,308 句，上限 12,000），屬預防 |
| 31 | **診斷報告外洩 CDN 存取權杖** | 沒有症狀。報告看起來正常，只是多了一長串網址 | 字幕清單網址的 base64 解開含 `sessionId` 與 24 小時有效的存取權杖。**報告是設計來貼給別人看的**，原設計刻意不含任何權杖——v0.4.0 為了偵錯拿掉截斷時把這個原則一起破壞了，期間已產生 6 份以上外洩的報告。修法：遮蔽權杖但保留長度與頭尾，仍能判斷網址有沒有被截斷。**教訓：為了偵錯放寬輸出時，要回頭確認有沒有踩到既有的安全設計** |

> **坑 #29／#30 的通則：跨物件共用的容器，不能拿單一情境的規則去管它。**
> `memo` 刻意跨影片共用（重複用語互相受惠），於是它同時被三種邏輯使用：
> 查核的分母、淘汰的對象、命中的來源。每一種都要問「這裡的『全部』指的是哪個範圍」。
> 回寫查核就是因為拿 `memo.size` 當單片分母而誤報過（本機 4,308／後端 1,946
> 看似只有 45%，真實回寫率是 99.7%）。

> **坑 #27 的通則：量測範圍不對，結論就是錯的。**
> 先前只量「偵測 → `render()` 返回」得到 1ms，就下結論「延遲不是我們造成的」。
> 那個數字沒錯，但**它不含版面計算與繪製，更完全沒涵蓋「什麼時候消失」**——
> 而消失的時機同樣構成使用者感受到的同步性。
> v0.5.1 起兩個產物都量到「兩次 rAF 之後真的上畫面」，數字可直接對照。
> **使用者說慢，就是慢；量不到只代表量錯地方。**

> **坑 #26 的通則：又是鐵則 #11（傳播延遲是所有快取層的總和）。**
> 這已經是**第三次**在同一件事上跌倒——先是遠端設定的 6 小時、再是 SW 的 bundle 永久快取，
> 現在是 HTTP 快取。每次都以為「已經處理過快取了」，每次都漏掉底下還有一層。
> **加任何快取時，先把該路徑上所有快取層列出來。**

> **坑 #25 的通則：權限模型會悄悄砍掉功能。**
> 「這個欄位只有管理員能寫」在 userscript 時代沒問題（你就是管理員），
> 到了擴充功能就等於「這個功能不存在」——而且不會有任何錯誤訊息。
> 把 userscript 的能力移植過去時，要逐項問「這件事在沒有 ADMIN_TOKEN 的世界裡還成立嗎」。

> **坑 #23 的通則：共用快取的貢獻端沒有任何使用者可見的回饋。**
> 存失敗、存到舊的、根本沒存——畫面全都一模一樣，只有下一個人要重新付費。
> 這類「靜默的利他機制」必須自己長出查核步驟，不能等使用者回報。

> **坑 #22 的通則：驗證強度要配得上改動性質。**
> 改邏輯時 `node --check` 加上眼睛還算夠；**大量刪除**時不夠，因為刪錯的東西不會留下語法痕跡。
> 凡是「按行號批次刪除」這類操作，一定要有能實際執行程式碼的驗證。

> **坑 #21 的通則**：這個專案已經有兩個「跨檔案複製、靠檢查工具維持一致」的契約——`normKey()`（三份）與 `SYSTEM_PROMPT`（兩份）。
> 兩者都是**靜默失效**型：壞掉不會報錯，只會讓品質或成本默默劣化。新增這類複製時，一併加檢查工具。

---

## 6. 成本演進（實測）

| 版本 | 機制 | 每句成本 | 2 小時正賽推估 |
|---|---|---|---|
| v2.2 | 純逐句翻譯 | $0.00083 | $1.66 |
| v3.2 | Worker 攔截 + 批次（平均 2 句） | $0.00050 | $1.00 |
| v3.3 | 聚合批次（平均 3.9 句） | — | $0.75 |
| v3.4 | 自適應批次（平均 5.1 句） | $0.000258 | $0.52 |
| **v4.0.1** | **整軌預抓（平均 14.2 句）** | **$0.000155** | **約 $0.31** |

**相對起點降低 81%。**

### 目前的成本結構（v4.0.1）

| 項目 | 佔比 |
|---|---|
| 輸出 tokens | **65%** ← 真正的地板 |
| 快取讀取 | 24% |
| 未快取輸入 | 11% |

**輸入端已無優化空間。** 輸出佔 65% 是物理下限——1,100 句中文就是要產生約 22,000 個輸出 token，除非讓翻譯變短變差。

---

## 6.1 Cloudflare 免費額度的真實邊界（實測觸發過）

| 資源 | 免費額度 | 實測消耗 |
|---|---|---|
| Workers 請求 | 100,000／天 | 寬鬆 |
| **KV puts** | **1,000／天** | **最先撞牆的就是這個** |
| KV reads | 100,000／天 | 寬鬆 |

**KV 寫入是唯一的瓶頸。** 初版設計一場正賽會寫 2,100 次，實測看 38 分鐘就把
當日額度用光，之後所有寫入回 429、用戶端連續拿到 HTTP 500。

修正後一場約 110 puts，也就是**每天約 9 場**的餘裕。若要超過，
Workers Paid 方案 US$5/月含 100 萬 puts——以商業化的規模來說這筆錢無論如何都要付，
但**架構上不該依賴「加錢解決」**，所以寫入量本身必須壓低。

**通則：免費額度的瓶頸往往不在最明顯的那個指標上。** 部署前要逐一算過每個
資源的消耗量，而不是只看請求數。

---

## 7. 商業化計畫

> **產品定位、品牌、定價、Landing Page、社群、Launch 時程以
> [`F1TV_即時繁體中文字幕工具_產品與Launch整合企劃.md`](./F1TV_即時繁體中文字幕工具_產品與Launch整合企劃.md) 為準。**
> 本節只記錄**技術架構決策**與**與企劃書不一致之處**。

### 7.0 已定調的商業決策（摘自企劃書）

| 項目 | 決定 |
|---|---|
| 品牌 | **PitLingo — 已定案**（2026-08-16）。網路搜尋未發現同名產品或商標衝突；正式註冊前仍須完成第 7.0.2 的查核清單 |
| 定位 | **F1TV 的繁體中文化工具**，不是「中文轉播」。不與緯來／愛爾達競爭 |
| 核心客群 | 已經在用或願意用 F1TV，但英文字幕造成障礙的人 |
| 定價 | Weekend NT$79、**Season NT$799（主推）**、Early Access 前 100 位 NT$599 |
| 月費 | 暫不做（避免與低價中文 F1 服務直接比價） |
| 免費層 | 每場 GP 前 15 分鐘，**後端記錄，不可只靠 localStorage** |
| 金流 | 綠界或藍新（台灣優先） |
| 社群 | Threads、Instagram、TikTok、Facebook |
| 法務 | 列為 P0，Launch 前請律師檢視 |

### 7.0.2 PitLingo 品牌查核清單

初步搜尋（2026-08-16）**未發現** 同名產品、公司或商標。但以下仍須逐項確認：

| 項目 | 狀態 | 備註 |
|---|---|---|
| 網路搜尋同名產品 | ✅ 未發現 | — |
| `pitlingo.com` / `.app` / `.tw` | ⬜ | 一併把 `.io` `.co` 買起來防搶註 |
| Instagram `@pitlingo` | ⬜ | — |
| Threads `@pitlingo` | ⬜ | 與 IG 帳號綁定 |
| TikTok `@pitlingo` | ⬜ | — |
| Facebook 粉專 | ⬜ | — |
| X `@pitlingo` | ⬜ | 即使不經營也先卡位 |
| Chrome Web Store 名稱 | ⬜ | 上架時才知道，但可先搜尋 |
| 台灣智慧財產局商標檢索 | ⬜ | 第 9 類（軟體）、第 42 類（SaaS） |
| USPTO 檢索 | ⬜ | 若打算賣海外 |

**一個要留意的點**：`-lingo` 字尾在語言／翻譯領域已有 Duolingo、Witlingo、Vlingo 等既有品牌。`lingo` 本身是英文通用字、單獨難以主張專屬，但 Duolingo 在商標維護上相當積極。**這不是不能用，而是列入律師檢視清單的項目之一**（企劃書已把法務列為 P0）。

**現在就該做的**：網域與四個社群帳號一次註冊完。加起來不到 NT$1,000，但被別人搶註的代價是整個品牌重來。

### 7.0.1 ⚠️ 企劃書與現況的三個落差

**① 「產品已經做好」是高估了。**
目前有的是 **Tampermonkey userscript**，不是 Chrome Web Store 上的 MV3 擴充功能。兩者差距是真實的工程量（見 7.5）：

| 項目 | 現況 | 上架需要 |
|---|---|---|
| 執行環境 | Tampermonkey | MV3 擴充功能 |
| `unsafeWindow` | 可用 | 改用 `"world": "MAIN"` |
| `GM_xmlhttpRequest` | 可用 | 改用 service worker `fetch` |
| `GM_setValue` | 可用 | 改用 `chrome.storage` |
| 狀態常駐 | 可以 | **SW 隨時被殺，狀態必須外置** |

**這是 P2，估 2 週。** 企劃書的 Week 1 商業化基礎、Week 2 Beta 都預設擴充功能已存在——**時程要往後推 2 週，或把 P2 併進 Week 1。**

**② 金流的地域範圍要先決定。**
綠界／藍新對台灣使用者體驗最好，但**只收得到台灣的錢**。若目標包含香港與海外華人，需要另一條國際金流（Paddle／LemonSqueezy 這類 Merchant of Record，稅務他們處理）。
**建議：第一版純台灣、用綠界；有海外需求再加。** 但要在 Landing Page 講清楚。

**③ 企劃書直接進入付費 Launch，跳過了免費驗證（P0）。**
這是可以的選擇，但要知道代價：**你會在不知道市場規模的情況下投入 4 週。**
現在其實有折衷方案——**發布免費版時附上公用後端**，一般使用者連 Anthropic key 都不用申請就能看熱門內容。安裝門檻極低，驗證訊號乾淨，而且那批人就是 Beta 名單與首批付費者的來源。**成本幾乎為零，我仍建議做。**

### 7.1 最關鍵的架構決策：伺服器共用譯文快取

### 7.1 最關鍵的架構決策：伺服器共用譯文快取

所有人看同一支影片拿到的是**同一份英文字幕**，所以譯文可以共用。

```
第一次有人看  → 翻譯一次 → 存進伺服器（key = contentId + 單句雜湊）
之後所有人    → 直接下載譯文，零 API 呼叫
```

| | 各自翻譯 | 共用快取 |
|---|---|---|
| 每用戶每月 API 成本 | $0.62 ~ $2.40 | **趨近 $0** |
| 整季全站 API 成本 | 隨用戶數線性成長 | **固定約 US$40** |

一季 24 站 × 5 個時段 = 120 個時段 × $0.30 = **全站總計約 US$36**，不管幾個用戶。

**沒有這個設計，商業模型不成立。**

### 7.2 直播怎麼處理（無法賽前預抓）

兩層機制：

**第一層｜以單句雜湊為 key 的快取 —— 自動成群**
直播觀眾的緩衝深度天生不一致，總有人比別人早幾秒。第一個碰到某句的人替所有人付錢。**架構自帶，不需要任何額外元件。**

**第二層｜專屬收割器 —— 讓「第一個」永遠是你**
不需要播放影片，只要：認證 → 取得字幕清單網址 → 每 3 秒重抓清單 → 下載新分段 → 小批次翻譯 → 寫入快取。

**HLS 直播的觀眾天生落後 live edge 約 20~40 秒**（播放器緩衝），收割器站在 live edge，因此有約 15~25 秒餘裕。

> 實際餘裕待測。有直播時用現有腳本量「提前量中位數」即可得知。

**直播的批次策略要反過來**：用小批次（3~5 句）換低延遲，因為成本已被全站均攤（一場約 $0.40，不管幾人觀看）。

### 7.3 擴充功能應該做什麼、不做什麼

**不要做**：worker 注入、網路攔截。理由：有伺服器快取就不需要；修改頁面 Worker 是審核紅旗；維護成本高。

**只要做**：讀 contentId → 向後端要譯文 → DOM 監聽 → 查表 → 疊字。

**「第一次收割」由你自己做**——現有的 userscript 當管理員工具，賽前跑一遍灌進快取。

### 7.4 技術選型

| 項目 | 選擇 | 理由 |
|---|---|---|
| 後端 | Cloudflare Workers + KV + D1 | 邊緣節點延遲低、免費額度夠、KV 適合寫少讀多 |
| 金流 | **Paddle 或 LemonSqueezy** | Merchant of Record，全球稅務他們處理 |
| 認證 | Email magic link | UX 遠優於授權碼 |

> **Chrome Web Store 的付費／應用內購買在 2020 年關閉且未恢復，必須自建收款。**

### 7.5 MV3 遷移要點

| Tampermonkey | MV3 |
|---|---|
| `unsafeWindow` | manifest 的 `"world": "MAIN"`（Chrome 111+） |
| `GM_xmlhttpRequest` | service worker 的 `fetch` + `host_permissions` |
| `GM_setValue` | `chrome.storage.local` |
| 常駐記憶體 | **SW 隨時被殺，狀態一律放 `chrome.storage`** |

**禁止遠端程式碼**：可以下載資料（譯文 JSON、選擇器設定），不可下載 JS 執行。

### 7.6 定價

| SKU | 價格 | 定位 |
|---|---|---|
| 月費 | NT$99 | 降低嘗試門檻 |
| **2026 賽季通行證** | **NT$799** | **主打，預期佔營收 80%** |

錨定：NT$799 ÷ 24 站 = 一場 33 元。

**免費層：每場比賽前 15 分鐘免費。** 比「7 天試用」好，因為每一場都是一次轉換機會，而且用戶正在看的當下動機最強。因 COGS 趨近零，可以大方。

300 位付費用戶的年淨利約 **NT$22 萬**，毛利率約 91%。

### 7.7 市場現實

繁中圈同時是 F1TV 訂閱者的人，樂觀估 5,000–30,000。附加服務轉換率 1–3% → **50–900 付費用戶**。

**300 人是中間偏樂觀，不是保守估計。** 獲客管道基本上只有 PTT F1 版、FB 台灣 F1 社團、Discord、YouTube 中文頻道合作。

---

## 7.7 擴充功能架構（P2）

`extension/`，MV3，`v0.4.0` — **三層架構已與 userscript 對等**。
詳見 [`extension/README.md`](./extension/README.md)。

### 最終架構：雙 world

| World | 檔案 | 職責 |
|---|---|---|
| **MAIN**（`document_start`） | `inject.js` | patch `URL.createObjectURL`，把 hook 注入 blob worker |
| **ISOLATED**（`document_idle`） | `main.js` 等 | 只有這裡有 `chrome.*` API：批次翻譯、共用快取、疊字顯示 |

需要兩個 world 是因為：**注入必須在頁面環境，但 `chrome.*` API 只存在隔離環境。**

資料流：

```
worker 內攔到 VTT
  → BroadcastChannel（不是 postMessage，避免污染播放器的訊息通道）
  → MAIN world
  → window.postMessage
  → ISOLATED world → 批次翻譯 → 共用快取 → 顯示
```

**實測驗證**：注入的 worker 腳本是 707,103 bytes，與 userscript 攔到的完全一致。

### ⚠️ PLAY API 這條路走不通（v0.1.0 → v0.2.9，八個版本）

原本的設計是「擴充功能不做注入，改自己呼叫 F1TV 的 PLAY API 取得串流位址」。
**這條路失敗了。** 完整的排查軌跡（負面知識同樣有價值）：

| 版本 | 伺服器回應 | 解決了什麼 |
|---|---|---|
| v0.2.2 | `400 Missing parameter … Token` | header 名稱錯 → 確認是 `ascendontoken` / `entitlementtoken` |
| v0.2.3 | `401 signature validation failed` | 送的是 URL-encoded 的 JWT，要先解碼 |
| v0.2.5 | 同上 | 兩個是**不同的權杖**，要各自配對來源一起送 |
| v0.2.6 | 同上 | 候選被 `ABTastyData`（18KB）擠掉，改成依可信度排序 |
| v0.2.7 | `500 Failed to evaluate stream rule` | **認證通過了**，改剔除不能當 header 值的候選 |
| v0.2.9 | 同上 | **原封不動重放播放器自己的網址仍然 500** |

**結論**：還缺播放器會帶的裝置／平台 header，而那個清單無法窮舉。停損。

### 後記：整軌預抓的入口其實一直都在（v0.4.0）

PLAY API 失敗之後，v0.3.x 認定「拿不到 master m3u8 就做不了整軌預抓」，
於是把整段預抓程式碼用 `USE_PLAY_API = false` 關掉——**但那個前提是錯的**。

**worker 本來就會抓 m3u8**（它就是播放器的網路層）。v0.3.x 的 hook 只是
`if (t.indexOf('-->') !== -1)` 一行就 return，只送 VTT 不送 manifest。
入口一直在手上，只是沒去拿。

v0.4.0 的修法：hook 也送 manifest，然後比照 userscript 用兩條路徑找字幕清單
（直接攔到 > 從 master 解析）。下游的分段抓取、批次翻譯全部沿用既有程式碼。

同時修掉三件事：

| 問題 | 說明 |
|---|---|
| **網址截斷復發** | `inject.js` 的 `url: String(u).slice(0,300)` —— 就是坑 #3。當時只拿來記錄所以沒事，一旦要用它去抓東西就會 400 |
| **manifest 未隨影片清空** | 換片後會拿上一支的字幕清單去抓，整支翻錯且不報錯（坑 #16 那類） |
| **收割鎖直接放棄** | 切換影片時前一次收割還在等待階段（最長 45 秒），新影片的 `startPrefetch` 撞上鎖就 return，**永遠不會再有整軌預抓**。改成有上限的重試 |

**順帶移除 PLAY API 的全部死碼（-339 行）與 `cookies` 權限**，商店審查面縮小。

> **教訓：一個被否決的方案會連帶埋掉它周邊的東西。**
> PLAY API 死的時候，整軌預抓被一起關掉了，而那兩件事其實沒有必然關係。
> 停用一條路徑時，要分清楚「真的依賴它的」和「只是剛好寫在一起的」。

程式碼保留，由 `USE_PLAY_API = false` 控制。

### ⚠️ F1TV 有 Imperva 機器人防護（重要，影響商業架構）

cookie 清單裡的 **`reese84` 是 Imperva Advanced Bot Protection**。
實測連續快速的 API 重試會開始被擋（表現為 `Failed to fetch`）。

**這對商品化的影響很大：**

1. **任何需要大量呼叫 F1TV API 的設計都不可行** —— 例如「每個用戶端各自去抓 manifest」
2. **共用譯文快取的價值因此更高** —— 全站一次收割，其他人只跟我們自己的後端說話
3. **Worker 注入反而是最安全的方案** —— 它搭播放器自己的請求便車，**零額外流量**

原本以為 Worker 注入的審核風險是缺點，實測後發現它在「不觸發對方防護」這點上
反而優於自行呼叫 API。

### ⚠️ 7.3 的「擴充功能只做 DOM」是錯的（v0.2.0 修正）

實測發現：**擴充功能跟不上語速，userscript 卻可以，即使兩者都沒預先收割。**

差別不在效能調校，在**提前量**：

| | 資料來源 | 提前量 |
|---|---|---|
| userscript | Worker 注入攔截 VTT 分段 | **47~53 秒**（實測） |
| 擴充功能 v0.1.x | 只讀畫面 DOM | **0 秒** |

播放器會提前約 50 秒下載字幕分段，userscript 的第二層攔截等於在提前翻譯。
DOM-only 沒有這個，逐句翻譯（1~3 秒來回 vs 每 3~4 秒一句）**物理上追不上**。
對直播與未收割的重播都不可用。

**解法不是 Worker 注入**：master 網址本來就來自主執行緒的 PLAY API，
擴充功能有 `host_permissions`，可以自己發同一個請求拿到串流位址，
再自行抓字幕清單與 VTT 分段。提前量拿回來了，而且完全不用注入任何東西。

```
PLAY API → master m3u8 → 字幕清單 → VTT 分段 → 批次翻譯 → 共用快取
```

重播一次抓完整支；直播用滑動視窗每 20 秒重抓（觀眾落後 live edge 20~40 秒，有餘裕）。

**新增 host permission**：`ott-video-fer-cf.formula1.com`（CDN）。

### 擴充功能的三個實測修正（v0.1.1）

| 問題 | 原因 | 修法 |
|---|---|---|
| **中英字幕並排而非上下** | 兩個子元素都是 `inline-block`，userscript 版本靠 `<br>` 分隔，移植時拿掉了 `<br>` 卻沒改排版 | 父層改 `display:flex; flex-direction:column`，比 `<br>` 乾淨且不受子元素 display 影響 |
| **HTTP 401 之後永不重試** | `loadBundle()` 只在換影片時呼叫。啟動時金鑰還沒填就 401，整場都走最慢的逐句路徑 | 指數退避重試 5 次；並監聽 `clientToken` 變更，一填好就立刻重抓 |
| **逐句翻譯跟不上語速** | 等 1.2 秒才送出，且一次只允許一個請求在飛，實際延遲疊到 4 秒以上 | 聚合縮到 300ms、允許 3 個請求並行、加「翻譯中…」暫時顯示。**但這條路本來就追不上 F1 語速，正解是預先收割** |

### ⚠️ `normKey()` 是三份程式碼的硬性契約

`extension/src/shared/normalize.js`、`backend/src/index.js`、`f1tv-zh-subtitles.user.js`
三處必須**行為完全一致**，否則同一句話算出不同的鍵，共用快取整個失效——
**不會報錯，只會默默重翻**。

已做成可重複執行的檢查：

```bash
node tools/check-normkey.js
```

**修改任何一處的 normKey 之後都要跑這個。**

---

## 8. 風險與防禦

| 破壞面 | 可能性 | 衝擊 | 防禦 |
|---|---|---|---|
| **DOM 選擇器改變** | 高 | 致命 | 遠端設定檔（選擇器陣列） |
| 網址結構改變 | 中 | 致命 | 同上（contentId pattern 也放設定檔） |
| 播放器整個換掉 | 低 | 致命 | 嗅探器自動找新選擇器 |
| CDN 網域改變 | 中 | 只影響收割器 | 設定檔 |
| API 版本升級 | 中 | 只影響收割器 | 設定檔 |
| **字幕改成加密** | 低 | 致命 | **無法防禦**，保留 DOM 路徑當底線 |
| **F1TV 自推繁中** | 中 | 產品歸零 | 賽季通行證讓現金提前入袋 |

### 防禦優先順序

| 順位 | 項目 | 說明 |
|---|---|---|
| 1 | **遠端設定檔** | ✅ **已實作並實測**（見 8.1）。選擇器用**陣列**依序嘗試（灰度推送期新舊版同時可用）。改設定幾分鐘內生效，不用等 1~3 天審核 |
| 2 | **主動驗證 + 自我診斷回報** | 見下方說明。**已在 v4.1.0 於本地實作，商品版需加上回報後端** |
| 3 | **雙顯示路徑 + 自動校準** | 快取同時存時間軸。平時用 DOM，同時偷量 DOM 顯示時刻與時間軸標記的偏移；DOM 失效時用時間軸接手並套用已校準的偏移。**把 DOM 改版從「致命」降為「幾乎無感」** |
| 4 | 嗅探器自動找選擇器 | 失效時跑有界限的文字變動嗅探，回報候選路徑 |
| 5 | 收割器賽前金絲雀 | 每時段開播前健康檢查，比用戶早幾小時知道 |
| 6 | 來源抽象層 | `SubtitleSource` 介面，F1TV 只是其中一個實作 |

**1、2、3 必須在上架前完成。**

### 8.1 遠端設定熱修 — 實測結果（2026-08-16）

**機制**

| 端點 | 用途 |
|---|---|
| `GET /v1/config` | 優先讀 KV，讀不到才用 Worker 內建預設 |
| `POST /v1/config` | 熱修入口（需 `ADMIN_TOKEN`），寫入 KV。**不用重新部署** |
| `DELETE /v1/config` | **還原鍵**，刪掉 KV 回到內建預設 |

用戶端：SW 快取 2 分鐘，content script 每 60 秒重讀並**就地套用**（重掛觀察者、
重套隱藏樣式、清除去重狀態）。選項頁有「立即重新載入設定」可跳過等待。

**已驗證**

```
[22:09:53] PitLingo v0.3.3 已啟動 | 設定版本 3
[22:10:27] ⚙ 遠端設定已更新 v3 → v1，選擇器已就地套用
```

- 後端 POST / DELETE 改得動設定 ✅
- 用戶端 **34 秒內**自動收到並套用 ✅
- 影片全程未中斷、不需重新整理 ✅

**行為層面也已驗證**：推 v2（`.pitlingo-does-not-exist`）之後字幕確實停止，
換回正確選擇器後恢復。**熱修機制完整成立。**

> 這次測試順帶逼出坑 #18：選擇器壞掉時畫面仍會「斷斷續續重複同一句舊字幕」，
> 那不是選擇器失效的正常表現，是補顯示邏輯的缺陷。

**踩過的兩個坑（傳播延遲比想像中難算）**

| 版本 | 問題 |
|---|---|
| 初版 | SW 快取 6 小時 —— 比賽日出事等於整場報銷 |
| v0.3.1 | 改成 10 分鐘，但 content script 的 60 秒重讀只會問到 SW 的快取，**實際傳播仍是 10 分鐘** |
| v0.3.3 | SW 2 分鐘 + 重讀 60 秒 → 最壞約 3 分鐘；另加手動立即套用 |

**通則：傳播延遲 = 所有快取層的總和，不是最短那層。** 診斷面板只寫「每 60 秒重讀」
會誤導自己，必須標明實際上限。

### 為什麼「主動驗證」值得排到第 2

v4.1.0 的「切換車手鏡頭後字幕消失」暴露了一個比 DOM 改版更陰險的失效模式：**時好時壞**。

| 失效模式 | 用戶行為 | 對生意的影響 |
|---|---|---|
| 完全壞掉 | 回報給你 | 你會知道，能修，用戶會等 |
| **時好時壞** | **默默流失** | **你永遠不知道發生過，也沒機會補救** |

**時好時壞比完全壞掉更傷。** 而且商品版的用戶不會看 Console，他們只會覺得「這東西不太可靠」然後在續訂時不續。

通則：**任何「註冊一次然後假設它持續有效」的機制，在 SPA 裡都必須有主動驗證的備援。** MutationObserver、事件監聽、播放器 API 掛勾——全部適用。

商品版要做的兩件事：
1. **主動輪詢驗證輸出**（已實作，見決策 4.6）
2. **靜默失效自動回報後端** —— 播放中 + 容器存在 + N 秒零字幕，就送一筆遙測（只需網域、播放器版本、失敗的選擇器）。**你要在用戶抱怨之前就知道。**

---

## 9. 路線圖

| 階段 | 內容 | 估時 | 狀態 |
|---|---|---|---|
| — | 個人自用版 | — | ✅ 完成 |
| **P0** | **免費開源驗證需求**（整理 README、發文、看裝機數） | 1 週 | ⬜ **仍建議補做** |
| P1 | 後端（Workers + KV 快取 + 上傳／下載 API） | 2 週 | ✅ **已部署並驗證**（見 10.1） |
| P2 | MV3 擴充功能 | 2 週 | 🟢 **功能面完成**：Worker 注入提前量、命中率 99.5%、遠端設定熱修已驗證 |
| P3 | 商業化（金流、登入、配額、免費層） | 3 週 | ⬜ |
| P4 | 上架（隱私政策、審核、著陸頁） | 1 週 | ⬜ |

**P0 是最重要且最便宜的一步。** 若免費版只有 50 人用，付費版不可能有 300 人買，該停在那裡。

---

## 9.1 下一步（接手時從這裡開始）

### 立刻要做（環境層面）

| # | 事項 | 說明 |
|---|---|---|
| 1 | **部署 backend v1.3** | `cd backend && wrangler deploy`。修的是 KV 寫入爆量（坑 #19），不部署會持續超額 |
| 2 | **等 KV 額度重置** | 每日 UTC 00:00（台灣早上 8 點）。在那之前寫入會失敗，但**讀取正常**，已翻好的內容照常能用 |
| 3 | **換掉 `ADMIN_TOKEN`** | 曾在對話中明文出現。`wrangler secret put ADMIN_TOKEN`。換完只需更新自己的 PowerShell 變數，一般使用者不受影響 |
| 4 | **`git push`** | 有 20 幾個 commit 未推送 |

### P2 收尾（剩下的都是形式要件，非技術風險）

| # | 事項 | 判準 |
|---|---|---|
| 5 | 完整看完一場正賽 | 命中率維持 95%+、無字幕重複、記憶體不持續成長、切換視角／全螢幕正常 |
| 6 | 確認坑 #20 的推測 | 若再出現「字幕停一下」，Console 應有 `⚠ 偵測到輪詢停擺 N 秒`。有那行就證實是瀏覽器節流 |
| 7 | 圖示 16/32/48/128 png | 上架必備 |
| 8 | 隱私權政策 | **必須說明 `cookies` 權限的用途**（目前只有 PLAY API 路徑用得到，而那條已停用——**可考慮直接移除該權限**，審核會更順） |

> ⚠️ **第 8 點值得先處理**：`USE_PLAY_API = false` 之後，`cookies` 與 `ott-video-fer-cf.formula1.com`
> 的 host permission 其實都不再需要。移除能大幅降低審核摩擦，也少一項要在隱私政策裡解釋的東西。

> **P3 的技術規劃見 [`P3-PLAN.md`](./P3-PLAN.md)**——分階段推送、成本後台、
> 金流比較（綠界 vs PayPal）、介面規劃、上線前檢查清單。
>
> **共用快取寫入遺失（約 30%）已決定採 B 案**：維持現狀不開放用戶端上傳。
> 理由是不願把「誰能寫共用快取」放寬到所有使用者（汙染風險）。
> 代價是成本不會歸零，但每多一個人看就補一點，會收斂。

### P3 之前必須先決定的事

| # | 決策 | 現況 |
|---|---|---|
| 9 | **網域** `pitlingo.com` 等 | **尚未購買**。Landing Page、Chrome Store 官網欄位、隱私政策託管都需要它 |
| 10 | 商標檢索（TIPO 第 9、42 類 / USPTO） | 未做。註意 `-lingo` 字尾已有 Duolingo 等既有品牌（見 7.0.2） |
| 11 | 金流地域範圍 | 綠界／藍新只收得到台灣的錢。若含港澳海外需另一條國際金流（見 7.0.1） |
| 12 | 法務檢視 | 企劃書列為 P0，Launch 前請律師看過 |

### 仍然建議補做的 P0

免費開源版 + 公用後端，成本幾乎為零，但能換到**真實的市場規模數字**。
目前所有推估（50~900 付費用戶）都是紙上談兵，安裝數是唯一可靠訊號。

---

## 10.1 共用快取實測結果（2026-08-16）

測試內容：`post-qualifying-show-japan`（cid 1000010205，407 個字幕分段）

**第一次觀看**（冷快取）：整軌預抓 407 段 → 全部翻譯 → 上傳 845 句

**第二次觀看**（重整後）：

| 指標 | 數值 |
|---|---|
| 從共用快取取得 | **845 句** |
| 命中率 | **100%** |
| 逐句呼叫 | **0 次** |
| 該次成本 | **約 US$0.01**（若無共用快取約 $0.13） |
| system prompt | 7,895 tokens（caching 生效） |

**共用架構驗證成功。** 第二位以後的觀看者成本趨近於零，這正是商業模型成立的前提。

---

## 10.2 整軌預抓與跳過收割實測（2026-08-16）

測試內容：`2026-monaco-gp-practice-3`（cid 1000010262，1,042 個字幕分段，約 1,855 句）

**第一次開啟（冷快取）**

| 指標 | 數值 |
|---|---|
| 收割 | 1,042 段成功、0 失敗 |
| 平均批次 | **14.3 句/次**（上限 15） |
| 逐句呼叫 | **0 次** |
| 增量上傳 | 82 → 371 → 669 → 947 → 1227 → 1511 → 1793 → 1823（每 60 秒） |
| 完整度標記 | ✅ `已標記完整：1042 段` |
| 成本 | **US$0.28** |

**第二次開啟（熱快取）**

```
⏭ 共用快取已涵蓋整支影片（1823 句 / 1042 段），跳過整軌預抓
```

**1 個請求取代 1,042 個。**

**新量到的數字**：整軌預抓下的提前量中位數為 **276,592 ms（4.6 分鐘）**，遠大於逐段攔截時的 47 秒——因為整支一次載入。自適應等待因此撞到 30 秒上限，屬預期行為。

---

## 10. 待驗證事項

- [ ] **直播情境**（三個產物都沒測過）：提前量、命中率、直播滑動視窗補抓
- [ ] 2026 車手陣容核對（腳本內名單需人工確認，我對部分席次沒把握）
- [ ] 整軌預抓在 2 小時正賽（約 1,200+ 分段）的表現與耗時
- [ ] 坑 #20 的推測（瀏覽器節流）需要 Console 出現 `輪詢停擺` 才算證實
- [ ] 譯文本地持久化（重看同一支影片免費）— 尚未實作
- [ ] 擴充功能的長時間記憶體表現（memo 上限 5,000 句，未實測過上限行為）

---

## 11. 更新紀錄

| 日期 | 版本 | 內容 |
|---|---|---|
| 2026-08-16 | **backend v1.7 + extension v0.5.4** | 後端補上 userscript 的坑 #9：**單句不再走批次編號協定**（實測回覆率只有 50%——送 2 句回 1 句，且完全不報錯，沒對上的下次再翻一遍白花錢）。批次解析加上「行數相同就按位置對應」的備援。另修正誤導訊息：「開始批次預譯」改成「已從播放器的字幕分段取得提前量」——使用者掛著幾小時後看到它再次出現，會誤以為整支要重翻 |
| 2026-08-16 | **extension v0.5.3** | 修坑 #28（事件時間軸被同一行洗版三小時）。加上**圖示 16/32/48/128**（由 `logo.png` 縮圖產生），P2 的形式要件只剩隱私政策。煙霧測試新增遠端設定冪等性檢查 |
| 2026-08-16 | **extension v0.5.2** | 補齊 userscript 的防呆機制：**播放中沒字幕的提示**（只在 video 真的在播、且從未看過字幕時才提示，五分鐘一次）、`memo` 上限 12,000／`pending` 上限 3,000、`pagehide` 把還沒送出的句子用 keepalive 做最後一搏。實測批次效率 19.8 句/批（v0.4.3 是 4.8，userscript 是 14.9）、到上畫面中位數 4.3ms |
| 2026-08-16 | **extension v0.5.1 + userscript v4.8.0** | 修坑 #27（疊字靠固定計時器消失，不跟著原生字幕走——這才是「比 userscript 慢一拍」的真正成因）。兩個產物都加上**「到上畫面」量測**（兩次 rAF），數字可直接對照，不用再各說各話。驗證：跳過整軌預抓已生效（後端記錄 383 段、本次已抓 0 段） |
| 2026-08-16 | **extension v0.5.0 + backend v1.6** | 修坑 #26（HTTP 快取讓 segCount 一分鐘看不到，跳過判斷永遠不成立）。補上 userscript 的坑 #10（收割期間停用計時器 flush——實測批次 4.8 句/批 vs userscript 14.9）與坑 #15（收割中止偵測，播放器返回鍵不改網址）。**新增測試工具 `__pitlingo.t.*`**，集中在一個區塊，上線前整段刪除；`__pitlingo.help()` 列得出來的東西正式版一律不該存在 |
| 2026-08-16 | **extension v0.4.3 + backend v1.5** | 修坑 #24（並行請求互相覆蓋，實測只有 41% 的譯文存活）與坑 #25（擴充功能不會跳過已收割的影片）。新增 `POST /v1/complete` 讓一般用戶端也能標記完整度。**量測結果：擴充功能替顯示延遲加了 1ms**（observer 佔 100%、繪製中位數 1ms），使用者感受到的 0.5 秒全部來自 F1TV 自己畫字幕的時機 |
| 2026-08-16 | **extension v0.4.2** | 修坑 #23（bundle 快取無 TTL，重整後共用快取永遠停在舊值）。新增**預抓後回寫查核**，主動比對本機與後端句數。新增**詳細日誌模式**（設定頁勾選或 `__pitlingo.debug(true)`），逐句與逐批都印。顯示延遲改為**量測而非猜測**：診斷報告新增「偵測來源」（observer vs 輪詢）與繪製耗時，用來分離「我們的延遲」與「F1TV 自己的延遲」。輪詢 250ms → 100ms，停擺門檻改用絕對值 2500ms（原本是 POLL_MS 的倍數，調快輪詢會連帶誤報） |
| 2026-08-16 | **extension v0.4.1** | 修坑 #22：v0.4.0 清死碼時誤刪 `manifests` 宣告，實測一開頁就 `ReferenceError` 洗版、診斷無法匯出。新增 `tools/smoke-extension.js`（假 DOM 執行 content script 並觸發實際路徑），已驗證能抓到這類 `node --check` 抓不到的錯 |
| 2026-08-16 | **extension v0.4.0** | **整軌預抓補上，三層架構與 userscript 對等。** 入口不是 PLAY API 而是 worker 本來就會抓的 m3u8——v0.3.x 的 hook 只送 VTT 不送 manifest，所以一直以為拿不到 master。同時：修好 inject.js 裡重新出現的網址截斷（坑 #3）；換影片時清空 manifest（防坑 #16 式污染）；收割鎖改成重試而非直接放棄。**移除 PLAY API 全部死碼（-339 行）與 `cookies` 權限**，減少商店審查面 |
| 2026-08-16 | **backend v1.4** | 修坑 #21（後端 prompt 快取從未生效）。把 userscript 的完整 `SYSTEM_PROMPT` 整份搬進後端：術語 34 → 273 條，token 數跨過 4,096 門檻，`cache_control` 才真的啟動。連帶修正擴充功能「快取未命中時譯文品質較差」的問題。新增 `tools/check-prompt.js` 擋兩份 prompt 漂移 |
| 2026-08-16 | **extension v0.3.5 + backend v1.3** | 修坑 #19（KV 寫入爆量，一場正賽超額 2 倍，實測已觸發 Cloudflare 封鎖）與 #20（分頁被節流時輪詢停擺）。38 分鐘穩定性測試：命中率 99.5%、VTT 218 份、零字幕重複 |
| 2026-08-16 | **extension v0.3.4** | 修坑 #18（過時的譯文會重新彈出）。遠端設定熱修的行為層面驗證完成——壞選擇器確實讓字幕停止、換回即恢復 |
| 2026-08-16 | **extension v0.3.3 + backend v1.2** | **遠端設定熱修完成並實測**（見 8.1）。後端設定移到 KV，`POST`/`DELETE /v1/config` 可熱修與還原，不用重新部署。用戶端 34 秒內自動套用、影片不中斷。過程修掉兩個傳播延遲的坑（6 小時 → 10 分鐘 → 3 分鐘），並修正「事件日誌第二個參數被靜默丟掉」。新增 `tools/check-logcalls.js` |
| 2026-08-16 | **extension v0.3.0** | **P2 核心障礙解除**。放棄 PLAY API（八個版本、詳見 7.7），改用 MAIN world 注入 blob worker 取得提前量——與 userscript 相同的機制，實測注入的腳本同為 707,103 bytes，命中率 83%。發現 F1TV 掛有 Imperva 機器人防護（`reese84`），這反而讓 Worker 注入成為最安全的方案（零額外 API 流量） |
| 2026-08-16 | **extension v0.2.0** | **推翻 7.3 的 DOM-only 決策**。實測擴充功能跟不上語速而 userscript 可以，根因是提前量 50 秒 vs 0 秒。改為直接呼叫 PLAY API 取得 master m3u8 → 字幕清單 → VTT 分段預抓，拿回提前量且不需注入。直播用滑動視窗。移除會分心的「翻譯中…」佔位字。新增事件時間軸與「匯出診斷」按鈕 |
| 2026-08-16 | **extension v0.1.0（P2 起步）** | MV3 擴充功能骨架。關鍵決策：**不做 Worker 注入，因此不需要 MAIN world，跑在 ISOLATED world**；contentId 改用 `performance.getEntriesByType('resource')` 取得（回溯、免 webRequest 權限）；所有網路請求集中在 service worker；`/v1/config` 遠端設定讓 F1TV 改版可熱修不用送審。新增 `tools/check-normkey.js` 驗證三份 `normKey()` 一致 |
| 2026-08-16 | **v4.7.2** | 修坑 #17（手動 🎯 會默默重抓整支）。收割完成時登記本機覆蓋率，讓重新進入自動走「跳過」路徑；按鈕在已完整時要求確認；強制觸發留下時間軸紀錄 |
| 2026-08-16 | **v4.7.1** | 修坑 #16（跨影片 memo 污染導致共用 bundle 缺角，從診斷報告的時間軸發現）。加 `prefetchDoneForCid` 以 contentId 為鍵的預抓決策保險。診斷面板改顯示目前觀察中的容器數 |
| 2026-08-16 | **v4.7.0** | **可觀測性**：新增事件時間軸（所有狀態變化都印到 Console 並存入環形緩衝）、階段指示、收割與翻譯的 10% 進度回報、**「📋 匯出完整診斷」一鍵複製完整報告**。修坑 #14（無旁白片段誤判為故障）、#15（收割中止只看 contentId，播放器返回鍵無效） |
| 2026-08-16 | **v4.6.0** | **韌性：使用者亂按也不會出錯**。修坑 #11（contentId 只認一種網址，導致 `/page/` 進來的影片完全不上傳）、#12（手動觸發無併發鎖，1056 段抓了 2112 次）、#13（只在全部翻完才上傳，中途關掉全白費）。新增 `harvestGen` 世代中止、`uploadedKeys` 增量上傳、60 秒定時上傳、`pagehide` keepalive 收尾 |
| 2026-08-16 | **v4.5.0 + backend v1.1** | **快取完整時跳過整軌預抓**：bundle 新增 `segCount`（僅在「整軌抓完且零失敗」時標記），用戶端比對字幕清單分段數，相同就完全跳過收割——省下 400+ 次對 F1 CDN 的請求。品牌定案 PitLingo，腳本 `@name` 更新，新增品牌查核清單（7.0.2） |
| 2026-08-16 | v4.4.0 | 修正坑 #9（單句批次編號協定失效）與坑 #10（收割期批次被計時器切碎）。記錄共用快取實測結果（10.1 節） |
| 2026-08-16 | **v4.3.1** | 修正「掛在非播放頁也會跳前瞻字幕警告」——改為累計實際播放秒數才判定。新增 `CLAUDE.md`（跨機器 AI 接手）、`README.md`（repo 門面）。整合產品與 Launch 企劃書，記錄三個落差。git repo 建立 |
| 2026-08-15 | **v4.3.0 + backend v1** | **共用譯文後端（P1）**：Cloudflare Workers + KV。`GET/POST /v1/subs`、`POST /v1/translate`、`GET /v1/config`（遠端設定，防禦第 1 名的實作）。userscript 加上換片自動下載、收割後自動上傳、後端選單與統計。連不上時全自動退回本機翻譯 |
| 2026-08-15 | v4.2.0 | **術語表大幅擴充**：新增歷代車手、車隊與賽事人物、2026 完整賽道清單、各賽道彎角俗名（含歷史賽道）、纏鬥與駕駛動作、天候與賽道狀況、賽季與合約用語。防禦順位調整：主動驗證＋自我診斷提到第 2 |
| 2026-08-15 | v4.1.0 | **韌性強化**：顯示偵測改為輪詢為主、observer 為輔；節點集合變動整組重掛；字幕清空重置 `lastRaw`；contentId 切換偵測與狀態重置；疊字層自動重掛；字幕長時間缺席提示。修正「切換車手鏡頭後字幕不再出現」 |
| 2026-08-15 | v4.0.1 | 建立本文件。修正網址截斷、無限重試、manifest 截斷；改用直接攔到的字幕清單優先 |
| 2026-08-15 | v4.0.0 | 整軌預抓（重播）。成本降至約 $0.31/場 |
| 2026-08-15 | v3.4.0 | 自適應批次等待、manifest 擷取 |
| 2026-08-15 | v3.3.0 | 聚合批次（最長等待而非 debounce） |
| 2026-08-15 | v3.2.0 | blob worker 注入成功，命中率 95% |
| 2026-08-15 | v3.0.0 | VTT 攔截架構（主執行緒，未成功） |
| 2026-08-15 | v2.2.0 | 修正 `innerText` 讀不到隱藏元素 — 字幕首次成功顯示 |
| 2026-08-15 | v2.0.0 | 2026 賽季擴充術語表 + prompt caching |
| 2026-08-15 | v1.0.0 | 初版 |
