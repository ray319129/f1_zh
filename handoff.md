# F1TV 繁中即時字幕 — 專案交接文件

> **這份文件是專案的單一事實來源。每次修改程式碼都必須同步更新這裡與 `TUTORIAL.md`。**

| 項目 | 內容 |
|---|---|
| 目前版本 | `v4.8.0`（userscript）／ `v1.7`（backend）／ `v0.5.4`（擴充功能） |
| 最後更新 | 2026-08-16 |
| 狀態 | 個人自用版完成並驗證；**P1 後端已部署並驗證共用生效** |
| 主檔案 | `f1tv-zh-subtitles.user.js`、`backend/` |

---

## 1. 專案是什麼

在 Chrome 觀看 F1TV 時，攔截官方的英文 CC，透過 Claude Haiku 4.5 即時翻譯成台灣用語的繁體中文，以雙語疊字顯示在影片上。

**核心設計原則：播放器負責「何時顯示」，我們只負責「顯示什麼」。** 顯示時機完全交給 F1TV 播放器的 DOM 更新，我們不碰時間軸對齊——這是同步永遠正確的原因。

---

## 2. 已驗證的技術事實（本專案最有價值的資產）

以下全部是實測確認，不是推測。這些是好幾輪逆向偵錯換來的。

### 2.1 F1TV 播放器架構

| 項目 | 事實 |
|---|---|
| 串流協定 | **HLS**（API 端點 `WEB_HLS/ALL/CONTENT/PLAY?...&player=player_tm`） |
| 播放器 | 自訂播放器，CSS class 前綴 `tm-`。**不是** Shaka / Bitmovin / hls.js |
| 核心 | 約 **7 MB 的 WASM** 模組 |
| 網路層 | **全部在 blob: Web Worker 內執行** |
| 原生文字軌 | **`video.textTracks.length === 0`** — 沒有原生字幕軌可用 |
| 字幕渲染 | 由 worker 解析後交給主執行緒畫進 DOM |

### 2.2 關鍵 DOM 結構

```
div.tm-subtitle-region-container      ← 監聽這個節點（MutationObserver）
  └─ div.tm-container-wrapper
       └─ span.tm-ui-subtitle-label   ← 讀這個的文字
            └─ span                    ← 實際文字節點
```

### 2.3 串流資源

- **Master playlist 網域**：`ott-video-fer-cf.formula1.com`
- **路徑形式**：`/v2/pa_<很長的 base64 授權 token>/<hash>/<hash>/index_N.m3u8`
- **token 內含**：path、kid、exp（到期時間戳）、ttl（1440 分鐘 = 24 小時）、geo、streamType
- **字幕軌宣告**：master 內以標準 HLS 的 `#EXT-X-MEDIA:TYPE=SUBTITLES` 標記，帶 `LANGUAGE="eng"` 與相對路徑的 `URI`
- **重播判定**：字幕 media playlist 含 `#EXT-X-ENDLIST` → VOD；沒有 → 直播

### 2.4 實測數量級（2026 Pre-Season Testing Day 1，約 1 小時）

| 指標 | 數值 |
|---|---|
| 字幕分段數 | 622 段（每段約 6 秒） |
| 總字幕句數 | 約 1,101 句 |
| 字幕出現頻率 | 約每 3~4 秒一句 |
| **cue 提前量中位數** | **47,000 ~ 53,000 ms** |

> **「提前量」= 從 VTT 分段抵達，到該句真正顯示在畫面上的時間差。**
> 這是自適應批次的預算來源。播放器的字幕預載遠比直覺深。

---

## 3. 腳本架構

### 3.1 三層取得字幕的路徑（自動降級）

```
第 1 層  整軌預抓（僅重播）
         攔 master m3u8 → 找字幕清單 → 抓全部 VTT 分段 → 一次全翻
              ↓ 失敗
第 2 層  Worker 即時攔截
         注入 hook 到 blob worker → 攔它的 fetch/XHR → BroadcastChannel 回傳
              ↓ 失敗
第 3 層  DOM 逐句翻譯
         字幕出現在畫面上才翻（最貴，但永遠可用）
```

**任何一層失效都只是變貴，不會壞掉。**

### 3.2 顯示路徑（唯一，且獨立於上述三層）

```
每 250 ms 主動輪詢 collectCaption()          ← 主力，不可能失效
   ↑ MutationObserver 只是加速器（有效時瞬間反應）
   → 讀取主畫面容器內的 .tm-ui-subtitle-label 的 textContent
   → 正規化後查本地快取
   → 命中：0 ms 顯示 ／ 未命中：送逐句翻譯
```

**設計原則：不要相信 hook，要驗證輸出。** 見決策 4.6。

### 3.3 Worker 注入原理

F1TV 的串流網路請求全在 blob worker 內，主執行緒 patch `fetch`/`XHR` 完全攔不到。解法：

```
攔截 URL.createObjectURL
   → 判斷是 Blob 且看起來是 worker 腳本
   → 同步 XHR 讀出原始碼（blob: 讀取無網路成本）
   → 把 hook 前置後產生新 Blob，回傳新的 blob URL
   → hook 在 worker 內部 patch 它自己的 fetch/XHR
   → 用 BroadcastChannel（不是 postMessage）送回主執行緒
```

**為什麼用 BroadcastChannel 而不是 postMessage**：postMessage 會把我們的訊息塞進播放器自己的訊息通道，它的 handler 若對未知格式拋錯，播放會直接壞掉。BroadcastChannel 是完全獨立的頻道。

**為什麼 MSE 不會被誤傷**：`URL.createObjectURL(mediaSource)` 的參數是 MediaSource 不是 Blob，`instanceof Blob` 檢查直接放行。

---

## 4. 關鍵決策紀錄

> 記錄「為什麼這樣做」，避免未來重蹈覆轍。

### 4.1 不使用 VTT 的時間軸來決定顯示時機

對齊 HLS 分段 VTT 的 `X-TIMESTAMP-MAP` 到 `video.currentTime` 很脆弱，容易做出「翻譯正確但時機歪掉」的體驗。改由播放器的 DOM 更新當顯示觸發器，同步永遠正確。

**但商品版應該把時間軸存進快取當備胎**——見第 8 節。

### 4.2 System prompt 刻意寫長到超過 4,096 tokens

**Haiku 4.5 的 prompt caching 最小前綴是 4,096 tokens。** 未達門檻時加了 `cache_control` 也會靜默失敗。

卡在門檻下是**最貴的位置**。跨過去之後每次呼叫的輸入只收 0.1 倍。目前 system prompt 約 5,050 tokens。

**修改術語表時務必用選單的「🧮 檢查 token 數」確認仍在門檻之上。**

> **這條規則一度只套用在 userscript 上。** 後端從 v1.0 到 v1.3 都放精簡版（約 650 tokens），`cache_control` 因此**從未生效**，而擴充功能的翻譯 100% 走後端。
> v1.4 起兩份 prompt 逐字相同，由 `node tools/check-prompt.js` 把關。詳見坑 #21。

### 4.3 逐句與批次共用同一份 system prompt

兩種模式共用同一份，才能共用同一份 prompt cache。批次模式的指示寫在 prompt 內部，靠使用者訊息的格式區分。

### 4.4 放棄「漸進顯示」（英文先上、中文後補）

實作過但移除。原因：語速快時 `displayRaw` 已換到下一句，舊句譯文被判定過期而不顯示，結果變成**整段只看得到英文**，比原本更糟。程式碼保留但 `CFG.progressive = false`。

### 4.5 自適應批次等待

固定等待時間是瞎猜：等太短批次湊不大，等太長漏接率飆升反而更貴（漏接一句要付一次完整的逐句呼叫）。

改為量測「cue 入列 → 畫面顯示」的實際間隔，取中位數的一半再扣掉翻譯耗時。**只用兩個牆鐘時間相減，不碰時間軸對齊。**

### 4.6 顯示偵測改為「輪詢為主、observer 為輔」

**這是回應「切換車手鏡頭後字幕不再出現」的一般性修法。**

原本的寫法是 `if (node.__f1zh) return;` —— 掛上 MutationObserver 後就相信它永遠有效。但 F1TV 是 SPA，切換視角、換畫質、換影片都會**銷毀並重建播放器 DOM**，observer 隨之靜默失聯：不報錯、不再觸發，字幕就這樣停了。

改法：

| 角色 | 機制 | 性質 |
|---|---|---|
| **主力** | 每 250 ms 主動 `collectCaption()` 比對 | **不依賴任何註冊狀態，不可能失效** |
| 加速器 | MutationObserver | 有效時瞬間反應；失效時由輪詢在 250 ms 內接手 |

同時處理的四件事：

1. **節點集合變動就整組重掛** observer（不再用一次性標記）
2. **字幕清空時重置 `lastRaw`** —— 否則切回主訊號時，若第一句與切走前相同會被去重擋掉（這正是原始症狀的成因之一）
3. **`mount()` 加入輪詢** —— 疊字層可能隨播放器重建被移除
4. **contentId 變動偵測** —— SPA 換影片時重置預抓狀態、manifest、提前量樣本

**通用原則：任何「註冊一次然後假設它持續有效」的機制，在 SPA 裡都要有主動驗證的備援。**

---

## 5. 踩過的坑（重要，別再犯）

| # | 坑 | 症狀 | 原因與修法 |
|---|---|---|---|
| 1 | **`innerText` vs `visibility:hidden`** | 元素存在但讀到空字串，永遠不觸發翻譯 | `innerText` 是版面感知的，元素被隱藏時回傳 `""`。**一律用 `textContent`**，隱藏改用 `opacity:0`（同時保留版面供定位用） |
| 2 | **Tampermonkey 沙箱隔離** | `__f1zh is not defined` | 沙箱的 `window` 不等於頁面的。要 `@grant unsafeWindow` 並同時掛上去 |
| 3 | **網址截斷** | 整軌預抓 HTTP 400 | worker 回傳網址時做了 `.slice(0,300)`，把 base64 授權 token 切掉。相對路徑解析時 token 那段被吃掉。**網址絕不截斷** |
| 4 | **失敗後無限重試** | Console 洗版、反覆打 CDN | `startFullPrefetch` 掛在 1.5 秒輪詢裡，失敗重設旗標等於永不停止。**要加重試上限** |
| 5 | **偵錯工具被自己過濾** | netlog 永遠是空的 | 只記錄「通過 `isVttish` 的網址」，等於把偵錯工具擋在要偵錯的過濾器後面。**探索期要記錄全部** |
| 6 | **批次沒有聚合** | 平均批次只有 2 句，省不到錢 | 每個 VTT 分段一到就 drain，而每段只含約 2 句。**要用「最長等待」聚合，不是 debounce**（debounce 會因持續有新資料而無限延後） |
| 7 | **manifest 內容被截斷** | 字幕清單解析不完整 | 兩小時正賽的字幕清單有上千段，40,000 字元不夠。已提高到 600,000 |
| 8 | **切換車手鏡頭後字幕不再出現** | 切回主轉播訊號要「重新點一次」才有字幕 | 兩個原因疊加：(a) 播放器重建 DOM，掛在舊節點上的 observer 靜默失聯；(b) `lastRaw` 去重把「切回來第一句 == 切走前最後一句」的情況擋掉。修法見決策 4.6 |
| 9 | **單句批次的編號協定失效** | `批次對應率偏低（0/1）`，整批作廢 | prompt 內部衝突：批次只有 1 句時，模型照【輸出規則】「只輸出譯文本身」直接給譯文而不加編號。修法：(a) `lines.length === 1` 走逐句路徑；(b) prompt 明寫「即使只有一句也要編號」；(c) 解析失敗時若回應行數等於輸入行數就按位置對應 |
| 11 | **contentId 只認一種網址** | 從 `/page/<id>/...` 進去看影片時完全不上傳，整支的翻譯成果全丟 | F1TV 有多種網址形式，只有 `/detail/<id>/` 帶 contentId。修法：從經過的網址中抓 `contentId=(\d+)`（PLAY API 一定會帶），網址解析失敗時用觀察值 |
| 12 | **手動觸發沒有併發鎖** | 1,056 段被抓了 2,112 次 | 選單的「立即整軌預抓」重設 `fullPrefetchStarted` 就再跑一條，與進行中的那條並行。修法：`harvestInFlight` 鎖 + `harvestGen` 世代編號（換影片時遞增，跑到一半的迴圈自行中止） |
| 13 | **只在全部翻完才上傳** | 1,669 句要翻約 5 分鐘，中途關掉分頁就全白費 | 改為增量上傳：只送差集（`uploadedKeys`），每 60 秒一次，加上 `pagehide` 用 `fetch(keepalive)` 做最後一搏 |
| 14 | **無旁白片段被誤判為故障** | 開場動畫、車手宣傳片、純音樂剪輯時跳出「未偵測到字幕」 | 「播放 45 秒沒字幕」無法分辨「壞了」與「這段本來就沒人說話」。修法：以 `everSawCaption` 為關鍵——**看過任何一句就永不再提示**；從未看過時再依有無 VTT 資料決定訊號強度（有資料 = CC 沒開，高訊號值得提示；無資料 = 可能真的沒字幕，只記 Console）。**寧可漏報也不要誤報** |
| 15 | **收割中止只看 contentId** | 點播放器返回鍵時網址沒變，收割繼續跑完上千段 | 播放器被拆掉但 SPA 網址不變。修法：加 `harvestShouldStop()`，以「`video` 元素消失超過 5 秒」為訊號（5 秒緩衝避開播放器重建時的短暫消失） |
| 16 | **跨影片 memo 污染導致 bundle 缺角** | 顯示正常，但伺服器上該片的譯文是缺的，下一個全新觀看者要重翻 | 收割時 `memo.has(k)` 就整句跳過，既不進佇列也不進 `sessionKeys`。切換影片時 `memo` 刻意不清（重看可受惠），於是前一支影片翻過的重複句子在新影片就被吃掉。**完全不會報錯，只會讓別人默默多花錢。** 修法：已有譯文時仍 `sessionKeys.add(k)`，因為那句確實出現在這支影片的 VTT 裡 |
| 17 | **手動觸發預抓會默默重抓整支** | 按一下 🎯 就對 CDN 再發近千個請求，畫面完全沒提示 | `force: true` 是**刻意**繞過所有自動防護的（手動就該能強制），但按鈕沒有在「已經抓完」時攔住使用者。診斷報告裡出現的兩次重複收割都是這個原因，不是自動路徑的 bug。修法：(a) 收割完成時把成果登記成本機的 `bundleSegCount`／`bundleLineCount`，任何重新進入都會走既有的「跳過收割」判斷；(b) 按鈕在已完整時跳確認對話框；(c) 強制觸發會在時間軸留下 `⚠ 手動強制整軌預抓` |
| 18 | **過時的譯文會重新彈出** | 字幕停止後，同一句舊字幕在不同時間斷斷續續重複出現 | `flushPending` 用 `currentEn` 判斷要不要「補顯示」，但那個變數只反映「最後一次看到的字幕」，且從未清空。翻譯慢回來時畫面早已換過，於是把過時的句子重新畫一次。**正常運作時也會發生**（講話空檔、切換視角後）。修法：(a) 字幕清空時一併清 `currentEn`；(b) 補顯示前**當場重讀 DOM 比對**，只有那句此刻仍在畫面上才顯示 |
| 19 | **KV 寫入爆量，一場就超額** | 播到一半連續 `HTTP 500`，Cloudflare 通知 `exceeded the daily Workers KV free tier limit of 1000 put operations` | `/v1/translate` 每翻一句寫一個 `line:` key，加上每次請求寫一次速率限制計數。實測 550 句 = 550+ puts；一場正賽約 2,000 句 = **超額 2 倍**，之後所有寫入回 429，用戶端表現為 500。修法：(a) 改成「讀一次 bundle、寫一次 bundle」，一次請求固定 1 put；(b) 速率限制改取樣，每 10 次才寫一次並直接加 10 補回。一場正賽從 2,100 puts 降到 110 |
| 20 | **分頁被節流時輪詢整個停擺** | 看到一半字幕停了一小段，動一下滑鼠就恢復 | 推測是 Chrome 對被遮蔽／背景分頁的計時器節流，`setInterval` 被拉長到每分鐘一次，250ms 輪詢與 1.5 秒結構檢查全部失效。修法：`checkPollStall()` 偵測「上次輪詢到現在超過 8 倍間隔」就強制整組重掛觀察者並清除去重狀態；另在 `focus`／`visibilitychange` 立刻重檢。**不論真正原因為何，處理方式都一樣——偵測到自己被凍結過，就不要假設狀態還新鮮** |
| 10 | **收割期間批次被計時器切碎** | 整軌預抓時平均批次只有 2.7 句 | 分段會在幾十秒內全部到齊，但 8 秒的 flush 計時器把零星新句切成一堆迷你批次。修法：收割期間停用計時器 flush（`harvesting` 旗標），只在湊滿 `batchSize` 或收割結束時才送 |
| 21 | **後端的 prompt 快取從來沒生效過** | 沒有任何症狀。不報錯、不失敗，只是每次都全額計費 | 後端刻意放「精簡版」`SYSTEM_PROMPT`（34 條術語，約 650 tokens），但 Haiku 的最小可快取前綴是 **4,096 tokens**，低於門檻時 `cache_control` **靜默失效**。同一個坑 userscript 早就踩過並寫進鐵則，後端卻沒套用。連帶第二個損害：擴充功能的翻譯 100% 走後端，所以快取沒命中時使用者拿到的是精簡術語表的譯文，比 userscript 差。修法：把 userscript 那份完整 prompt（273 條術語、16KB）整份搬進後端，兩個問題一起解決；新增 `tools/check-prompt.js` 擋日後漂移。**教訓：一條規則在某個檔案上學到，要主動去看它在別的檔案上成不成立** |

| 22 | **刪死碼時連帶刪掉還在用的宣告** | `ReferenceError: manifests is not defined` 洗版，連診斷都匯不出來 | 用行號區間刪掉 PLAY API 死碼時，區間起點算錯 8 行，把剛加上的 `const MANIFEST_MAX` / `let manifests = []` 一起帶走。**`node --check` 完全沒反應**——少一個宣告在語法上完全合法，要到執行期才炸。而且錯在 async `boot()` 裡，表現成 `Uncaught (in promise)`，頁面其他功能看起來還在動，更難察覺。修法：(a) 補回宣告；(b) 新增 `tools/smoke-extension.js`——在 Node 裡用假 DOM／假 `chrome.*` 真的把 content script 跑一遍並觸發實際路徑。**已驗證：拿掉宣告時這支會失敗，補回後會通過** |

| 23 | **bundle 記憶體快取沒有 TTL** | 同一支影片預抓完 836 句後重整，共用快取仍只回報 87 句，整支又重翻一次 | `sw.js` 的 `getBundle` 寫成 `if (cached) return cached;`——存了 `at` 卻從來不檢查，等於永久快取。重整頁面時 SW 沒被回收，回的是開場那份舊 bundle。**沒有任何徵兆**：畫面一模一樣，只是帳單多一份、伺服器的譯文永遠追不上。修法：(a) 90 秒 TTL；(b) `getBundle` 支援 `force`；(c) **預抓完成 20 秒後主動回頭查核後端有幾句**，本機與後端差太多就明確警告。與遠端設定那兩層快取是同一類錯——**寫了 `at` 就要有人去讀它** |

| 24 | **並行請求把彼此的譯文覆蓋掉** | 一輪翻了 363 句，後端只多 148 句（41%）。每個請求自己都成功，沒有任何錯誤 | `/v1/translate` 是「讀 bundle → 呼叫模型（1~2 秒）→ 整份寫回」。用戶端同時最多 3 個請求在飛，三個都讀到同一份舊快照再依序覆蓋，只有最後寫的活下來。**經典的 read-modify-write lost update**，而 KV 沒有 CAS。修法：寫回前**重讀再合併**（只補不覆蓋），把競爭視窗從「模型呼叫的 1~2 秒」縮到「讀+寫的幾十毫秒」。做不到零損失，但差了好幾個數量級 |
| 25 | **擴充功能每次都重抓整支，不會跳過** | 每個人開同一支影片都再向 CDN 發 383 個請求、花約 80 秒，而且幾乎抓不到新句子 | userscript 早有 `bundleSegCount === segs.length` 的跳過判斷（坑 #17），擴充功能完全沒有。原因是 `segCount` 只能由 `POST /v1/subs`（ADMIN_TOKEN）寫入，而使用者不該持有 ADMIN_TOKEN，所以這個欄位在擴充功能的世界裡永遠是 0。修法：新增 `POST /v1/complete`（CLIENT_TOKEN，**只能寫 segCount 且只准往上加**），收割完成且零失敗才回報。最壞情況只是別人略過預抓、退回 worker 攔截（仍有提前量），不會弄壞既有譯文 |

| 26 | **HTTP 快取讓剛寫進去的值一分鐘看不到** | 第一次成功「標記完整收割」，第二次開同一支片卻顯示「後端完整度：記錄 0 段」，照樣重抓 383 段 | `GET /v1/subs` 帶著 `cache-control: public, max-age=60`。v1.2 時 bundle 只有管理員會寫，一分鐘陳舊無所謂；現在每個使用者都會改它。**瀏覽器的 HTTP 快取是用戶端清不掉的一層**——`memCache.bundles.delete()` 清得掉自己的記憶體，清不掉底下的 HTTP 快取。時間軸完全吻合：01:03:06 查核存進快取（segCount 0）→ 01:03:11 標記完整 → 01:03:48 重開，42 秒仍在快取內。第二次的 bundle 只花 22ms 回來（真連線要 50~200ms）也證實是快取。修法：後端改 `no-store`，用戶端加 `cache: 'no-store'` 當第二道保險 |

| 27 | **疊字靠固定計時器消失，不跟著來源** | 使用者持續回報「擴充功能比 userscript 慢一拍」，但量測顯示我們只加 1ms | 疊字只靠 `holdMs` 計時器收起，**不管原生字幕還在不在**。F1 一句常在畫面上停 3.5~4 秒：`holdMs=3000` 會讓中文先消失、空白半秒才換下一句（讀起來是「慢一拍」）；`holdMs=7000` 則是講完很久還掛著上一句（讀起來是「對不起來」）。**兩種都像延遲，而且調參數只能二選一。** 使用者的擴充功能是 3000、userscript 預設 7000，差異就從這裡來。修法：輪詢每 100ms 就知道原生字幕清空了，那一刻收起疊字（350ms 寬限避開換句空窗）；`holdMs` 退化成純安全網 |

| 28 | **事件時間軸被同一行洗版** | 跑三小時後，400 筆的時間軸有 150 筆是「⚙ 遠端設定已更新 v1 → v1」，真正重要的紀錄全被擠掉 | `applyRemoteConfig` 每 60 秒判定「設定變了」並整組重掛觀察者、清空去重狀態，但版本號其實沒變。**根因未確認**——加了冪等性測試（同樣內容第二次必須回 `false`）卻無法重現，代表真實環境兩次拿到的設定內容確實不同（可能是 SW 快取層之間的漂移）。既然追不到單一來源，就改成**不論原因為何都不會再發生**：(a) `logEvent` 對連續相同訊息折疊成 `×N`；(b) 版本號沒變時降級成詳細日誌；(c) `window.__pitlingoBooted` 擋重複注入。**診斷報告是唯一的回報管道，被洗版等於瞎了——在出口擋一次，勝過追無窮盡的來源** |
| 29 | **翻譯失敗的句子被永久丟棄** | 沒有症狀。只有 `state.errors++`，畫面正常，共用快取默默缺一塊 | `flushPending` 的 catch 只做 `requested.delete(k)`，那批 20 句既沒放回 `pending` 也沒記錄，**永遠消失**。一次 429（後端每 IP 每分鐘 120 次，同一家用 IP 兩人同時收割就會逼近）或一次網路抖動就是 20 句沒了，下一個人得重新付費。更嚴重的是 `markComplete` 只看 `segFailed`（分段抓取），翻譯失敗完全不擋——**可能把殘缺的 bundle 標成「完整」，之後所有人都跳過預抓、永遠拿到缺角的譯文，而且沒有任何機制會去補**。修法：(a) 失敗排回佇列，每句最多 3 次（無上限會讓 429 更嚴重，鐵則 #3）；(b) 有放棄的句子就不標記完整 |
| 30 | **memo 淘汰會砍掉正在看的那支** | 連看幾支長影片後，畫面上突然開始重翻已經翻過的句子 | `MEMO_MAX` 淘汰時從最舊的砍，但 memo 跨影片共用——連看幾支之後，**正在看的這支的譯文可能就是「最舊的」**。不報錯，只是變慢又變貴。修法：淘汰時跳過 `sessionKeys`。尚未實際觸發（實測連看兩支才 4,308 句，上限 12,000），屬預防 |
| 31 | **診斷報告外洩 CDN 存取權杖** | 沒有症狀。報告看起來正常，只是多了一長串網址 | 字幕清單網址的 base64 解開含 `sessionId` 與 24 小時有效的存取權杖。**報告是設計來貼給別人看的**，原設計刻意不含任何權杖——v0.4.0 為了偵錯拿掉截斷時把這個原則一起破壞了，期間已產生 6 份以上外洩的報告。修法：遮蔽權杖但保留長度與頭尾，仍能判斷網址有沒有被截斷。**教訓：為了偵錯放寬輸出時，要回頭確認有沒有踩到既有的安全設計** |
| 32 | **加自訂網域時 workers.dev 被靜默關閉** | `f1zh-api.pitlingo.workers.dev` 突然回 `error code: 1042`，所有已安裝的擴充功能同時斷線 | wrangler 在 `wrangler.toml` 出現 `[[routes]]` 時，會**預設把 workers.dev 路由關掉**。部署輸出只有一行黃字警告，很容易滑過去。而所有已發佈的用戶端都指向那個位址——等於一次部署讓全部使用者離線。修法：明確寫 `workers_dev = true`，並在確認沒有任何用戶端使用舊位址前不得移除。**教訓：新增設定時要問「這會不會關掉某個預設值」，尤其是路由這種沒有中間狀態的東西** |

> **坑 #29／#30 的通則：跨物件共用的容器，不能拿單一情境的規則去管它。**
> `memo` 刻意跨影片共用（重複用語互相受惠），於是它同時被三種邏輯使用：
> 查核的分母、淘汰的對象、命中的來源。每一種都要問「這裡的『全部』指的是哪個範圍」。
> 回寫查核就是因為拿 `memo.size` 當單片分母而誤報過（本機 4,308／後端 1,946
> 看似只有 45%，真實回寫率是 99.7%）。

> **坑 #27 的通則：量測範圍不對，結論就是錯的。**
> 先前只量「偵測 → `render()` 返回」得到 1ms，就下結論「延遲不是我們造成的」。
> 那個數字沒錯，但**它不含版面計算與繪製，更完全沒涵蓋「什麼時候消失」**——
> 而消失的時機同樣構成使用者感受到的同步性。
> v0.5.1 起兩個產物都量到「兩次 rAF 之後真的上畫面」，數字可直接對照。
> **使用者說慢，就是慢；量不到只代表量錯地方。**

> **坑 #26 的通則：又是鐵則 #11（傳播延遲是所有快取層的總和）。**
> 這已經是**第三次**在同一件事上跌倒——先是遠端設定的 6 小時、再是 SW 的 bundle 永久快取，
> 現在是 HTTP 快取。每次都以為「已經處理過快取了」，每次都漏掉底下還有一層。
> **加任何快取時，先把該路徑上所有快取層列出來。**

> **坑 #25 的通則：權限模型會悄悄砍掉功能。**
> 「這個欄位只有管理員能寫」在 userscript 時代沒問題（你就是管理員），
> 到了擴充功能就等於「這個功能不存在」——而且不會有任何錯誤訊息。
> 把 userscript 的能力移植過去時，要逐項問「這件事在沒有 ADMIN_TOKEN 的世界裡還成立嗎」。

> **坑 #23 的通則：共用快取的貢獻端沒有任何使用者可見的回饋。**
> 存失敗、存到舊的、根本沒存——畫面全都一模一樣，只有下一個人要重新付費。
> 這類「靜默的利他機制」必須自己長出查核步驟，不能等使用者回報。

> **坑 #22 的通則：驗證強度要配得上改動性質。**
> 改邏輯時 `node --check` 加上眼睛還算夠；**大量刪除**時不夠，因為刪錯的東西不會留下語法痕跡。
> 凡是「按行號批次刪除」這類操作，一定要有能實際執行程式碼的驗證。

> **坑 #21 的通則**：這個專案已經有兩個「跨檔案複製、靠檢查工具維持一致」的契約——`normKey()`（三份）與 `SYSTEM_PROMPT`（兩份）。
> 兩者都是**靜默失效**型：壞掉不會報錯，只會讓品質或成本默默劣化。新增這類複製時，一併加檢查工具。

---

## 6. 成本演進（實測）

| 版本 | 機制 | 每句成本 | 2 小時正賽推估 |
|---|---|---|---|
| v2.2 | 純逐句翻譯 | $0.00083 | $1.66 |
| v3.2 | Worker 攔截 + 批次（平均 2 句） | $0.00050 | $1.00 |
| v3.3 | 聚合批次（平均 3.9 句） | — | $0.75 |
| v3.4 | 自適應批次（平均 5.1 句） | $0.000258 | $0.52 |
| **v4.0.1** | **整軌預抓（平均 14.2 句）** | **$0.000155** | **約 $0.31** |

**相對起點降低 81%。**

### 目前的成本結構（v4.0.1）

| 項目 | 佔比 |
|---|---|
| 輸出 tokens | **65%** ← 真正的地板 |
| 快取讀取 | 24% |
| 未快取輸入 | 11% |

**輸入端已無優化空間。** 輸出佔 65% 是物理下限——1,100 句中文就是要產生約 22,000 個輸出 token，除非讓翻譯變短變差。

---

## 6.1 Cloudflare 免費額度的真實邊界（實測觸發過）

| 資源 | 免費額度 | 實測消耗 |
|---|---|---|
| Workers 請求 | 100,000／天 | 寬鬆 |
| **KV puts** | **1,000／天** | **最先撞牆的就是這個** |
| KV reads | 100,000／天 | 寬鬆 |

**KV 寫入是唯一的瓶頸。** 初版設計一場正賽會寫 2,100 次，實測看 38 分鐘就把
當日額度用光，之後所有寫入回 429、用戶端連續拿到 HTTP 500。

修正後一場約 110 puts，也就是**每天約 9 場**的餘裕。若要超過，
Workers Paid 方案 US$5/月含 100 萬 puts——以商業化的規模來說這筆錢無論如何都要付，
但**架構上不該依賴「加錢解決」**，所以寫入量本身必須壓低。

**通則：免費額度的瓶頸往往不在最明顯的那個指標上。** 部署前要逐一算過每個
資源的消耗量，而不是只看請求數。

---

## 7. 商業化計畫

> **產品定位、品牌、定價、Landing Page、社群、Launch 時程以
> [`F1TV_即時繁體中文字幕工具_產品與Launch整合企劃.md`](./F1TV_即時繁體中文字幕工具_產品與Launch整合企劃.md) 為準。**
> 本節只記錄**技術架構決策**與**與企劃書不一致之處**。

### 7.0 已定調的商業決策（摘自企劃書）

| 項目 | 決定 |
|---|---|
| 品牌 | **PitLingo — 已定案**（2026-08-16）。網路搜尋未發現同名產品或商標衝突；正式註冊前仍須完成第 7.0.2 的查核清單 |
| 定位 | **F1TV 的繁體中文化工具**，不是「中文轉播」。不與緯來／愛爾達競爭 |
| 核心客群 | 已經在用或願意用 F1TV，但英文字幕造成障礙的人 |
| 定價 | Weekend NT$79、**Season NT$799（主推）**、Early Access 前 100 位 NT$599 |
| 月費 | 暫不做（避免與低價中文 F1 服務直接比價） |
| 免費層 | 每場 GP 前 15 分鐘，**後端記錄，不可只靠 localStorage** |
| 金流 | 綠界或藍新（台灣優先） |
| 社群 | Threads、Instagram、TikTok、Facebook |
| 法務 | 列為 P0，Launch 前請律師檢視 |

### 7.0.2 PitLingo 品牌查核清單

初步搜尋（2026-08-16）**未發現** 同名產品、公司或商標。但以下仍須逐項確認：

| 項目 | 狀態 | 備註 |
|---|---|---|
| 網路搜尋同名產品 | ✅ 未發現 | — |
| `pitlingo.com` / `.app` / `.tw` | ⬜ | 一併把 `.io` `.co` 買起來防搶註 |
| Instagram `@pitlingo` | ⬜ | — |
| Threads `@pitlingo` | ⬜ | 與 IG 帳號綁定 |
| TikTok `@pitlingo` | ⬜ | — |
| Facebook 粉專 | ⬜ | — |
| X `@pitlingo` | ⬜ | 即使不經營也先卡位 |
| Chrome Web Store 名稱 | ⬜ | 上架時才知道，但可先搜尋 |
| 台灣智慧財產局商標檢索 | ⬜ | 第 9 類（軟體）、第 42 類（SaaS） |
| USPTO 檢索 | ⬜ | 若打算賣海外 |

**一個要留意的點**：`-lingo` 字尾在語言／翻譯領域已有 Duolingo、Witlingo、Vlingo 等既有品牌。`lingo` 本身是英文通用字、單獨難以主張專屬，但 Duolingo 在商標維護上相當積極。**這不是不能用，而是列入律師檢視清單的項目之一**（企劃書已把法務列為 P0）。

**現在就該做的**：網域與四個社群帳號一次註冊完。加起來不到 NT$1,000，但被別人搶註的代價是整個品牌重來。

### 7.0.1 ⚠️ 企劃書與現況的三個落差

**① 「產品已經做好」是高估了。**
目前有的是 **Tampermonkey userscript**，不是 Chrome Web Store 上的 MV3 擴充功能。兩者差距是真實的工程量（見 7.5）：

| 項目 | 現況 | 上架需要 |
|---|---|---|
| 執行環境 | Tampermonkey | MV3 擴充功能 |
| `unsafeWindow` | 可用 | 改用 `"world": "MAIN"` |
| `GM_xmlhttpRequest` | 可用 | 改用 service worker `fetch` |
| `GM_setValue` | 可用 | 改用 `chrome.storage` |
| 狀態常駐 | 可以 | **SW 隨時被殺，狀態必須外置** |

**這是 P2，估 2 週。** 企劃書的 Week 1 商業化基礎、Week 2 Beta 都預設擴充功能已存在——**時程要往後推 2 週，或把 P2 併進 Week 1。**

**② 金流的地域範圍要先決定。**
綠界／藍新對台灣使用者體驗最好，但**只收得到台灣的錢**。若目標包含香港與海外華人，需要另一條國際金流（Paddle／LemonSqueezy 這類 Merchant of Record，稅務他們處理）。
**建議：第一版純台灣、用綠界；有海外需求再加。** 但要在 Landing Page 講清楚。

**③ 企劃書直接進入付費 Launch，跳過了免費驗證（P0）。**
這是可以的選擇，但要知道代價：**你會在不知道市場規模的情況下投入 4 週。**
現在其實有折衷方案——**發布免費版時附上公用後端**，一般使用者連 Anthropic key 都不用申請就能看熱門內容。安裝門檻極低，驗證訊號乾淨，而且那批人就是 Beta 名單與首批付費者的來源。**成本幾乎為零，我仍建議做。**

### 7.1 最關鍵的架構決策：伺服器共用譯文快取

### 7.1 最關鍵的架構決策：伺服器共用譯文快取

所有人看同一支影片拿到的是**同一份英文字幕**，所以譯文可以共用。

```
第一次有人看  → 翻譯一次 → 存進伺服器（key = contentId + 單句雜湊）
之後所有人    → 直接下載譯文，零 API 呼叫
```

| | 各自翻譯 | 共用快取 |
|---|---|---|
| 每用戶每月 API 成本 | $0.62 ~ $2.40 | **趨近 $0** |
| 整季全站 API 成本 | 隨用戶數線性成長 | **固定約 US$40** |

一季 24 站 × 5 個時段 = 120 個時段 × $0.30 = **全站總計約 US$36**，不管幾個用戶。

**沒有這個設計，商業模型不成立。**

### 7.2 直播怎麼處理（無法賽前預抓）

兩層機制：

**第一層｜以單句雜湊為 key 的快取 —— 自動成群**
直播觀眾的緩衝深度天生不一致，總有人比別人早幾秒。第一個碰到某句的人替所有人付錢。**架構自帶，不需要任何額外元件。**

**第二層｜專屬收割器 —— 讓「第一個」永遠是你**
不需要播放影片，只要：認證 → 取得字幕清單網址 → 每 3 秒重抓清單 → 下載新分段 → 小批次翻譯 → 寫入快取。

**HLS 直播的觀眾天生落後 live edge 約 20~40 秒**（播放器緩衝），收割器站在 live edge，因此有約 15~25 秒餘裕。

> 實際餘裕待測。有直播時用現有腳本量「提前量中位數」即可得知。

**直播的批次策略要反過來**：用小批次（3~5 句）換低延遲，因為成本已被全站均攤（一場約 $0.40，不管幾人觀看）。

### 7.3 擴充功能應該做什麼、不做什麼

**不要做**：worker 注入、網路攔截。理由：有伺服器快取就不需要；修改頁面 Worker 是審核紅旗；維護成本高。

**只要做**：讀 contentId → 向後端要譯文 → DOM 監聽 → 查表 → 疊字。

**「第一次收割」由你自己做**——現有的 userscript 當管理員工具，賽前跑一遍灌進快取。

### 7.4 技術選型

| 項目 | 選擇 | 理由 |
|---|---|---|
| 後端 | Cloudflare Workers + KV + D1 | 邊緣節點延遲低、免費額度夠、KV 適合寫少讀多 |
| 金流 | **Paddle 或 LemonSqueezy** | Merchant of Record，全球稅務他們處理 |
| 認證 | Email magic link | UX 遠優於授權碼 |

> **Chrome Web Store 的付費／應用內購買在 2020 年關閉且未恢復，必須自建收款。**

### 7.5 MV3 遷移要點

| Tampermonkey | MV3 |
|---|---|
| `unsafeWindow` | manifest 的 `"world": "MAIN"`（Chrome 111+） |
| `GM_xmlhttpRequest` | service worker 的 `fetch` + `host_permissions` |
| `GM_setValue` | `chrome.storage.local` |
| 常駐記憶體 | **SW 隨時被殺，狀態一律放 `chrome.storage`** |

**禁止遠端程式碼**：可以下載資料（譯文 JSON、選擇器設定），不可下載 JS 執行。

### 7.6 定價

| SKU | 價格 | 定位 |
|---|---|---|
| 月費 | NT$99 | 降低嘗試門檻 |
| **2026 賽季通行證** | **NT$799** | **主打，預期佔營收 80%** |

錨定：NT$799 ÷ 24 站 = 一場 33 元。

**免費層：每場比賽前 15 分鐘免費。** 比「7 天試用」好，因為每一場都是一次轉換機會，而且用戶正在看的當下動機最強。因 COGS 趨近零，可以大方。

300 位付費用戶的年淨利約 **NT$22 萬**，毛利率約 91%。

### 7.7 市場現實

繁中圈同時是 F1TV 訂閱者的人，樂觀估 5,000–30,000。附加服務轉換率 1–3% → **50–900 付費用戶**。

**300 人是中間偏樂觀，不是保守估計。** 獲客管道基本上只有 PTT F1 版、FB 台灣 F1 社團、Discord、YouTube 中文頻道合作。

---

## 7.7 擴充功能架構（P2）

`extension/`，MV3，`v0.4.0` — **三層架構已與 userscript 對等**。
詳見 [`extension/README.md`](./extension/README.md)。

### 最終架構：雙 world

| World | 檔案 | 職責 |
|---|---|---|
| **MAIN**（`document_start`） | `inject.js` | patch `URL.createObjectURL`，把 hook 注入 blob worker |
| **ISOLATED**（`document_idle`） | `main.js` 等 | 只有這裡有 `chrome.*` API：批次翻譯、共用快取、疊字顯示 |

需要兩個 world 是因為：**注入必須在頁面環境，但 `chrome.*` API 只存在隔離環境。**

資料流：

```
worker 內攔到 VTT
  → BroadcastChannel（不是 postMessage，避免污染播放器的訊息通道）
  → MAIN world
  → window.postMessage
  → ISOLATED world → 批次翻譯 → 共用快取 → 顯示
```

**實測驗證**：注入的 worker 腳本是 707,103 bytes，與 userscript 攔到的完全一致。

### ⚠️ PLAY API 這條路走不通（v0.1.0 → v0.2.9，八個版本）

原本的設計是「擴充功能不做注入，改自己呼叫 F1TV 的 PLAY API 取得串流位址」。
**這條路失敗了。** 完整的排查軌跡（負面知識同樣有價值）：

| 版本 | 伺服器回應 | 解決了什麼 |
|---|---|---|
| v0.2.2 | `400 Missing parameter … Token` | header 名稱錯 → 確認是 `ascendontoken` / `entitlementtoken` |
| v0.2.3 | `401 signature validation failed` | 送的是 URL-encoded 的 JWT，要先解碼 |
| v0.2.5 | 同上 | 兩個是**不同的權杖**，要各自配對來源一起送 |
| v0.2.6 | 同上 | 候選被 `ABTastyData`（18KB）擠掉，改成依可信度排序 |
| v0.2.7 | `500 Failed to evaluate stream rule` | **認證通過了**，改剔除不能當 header 值的候選 |
| v0.2.9 | 同上 | **原封不動重放播放器自己的網址仍然 500** |

**結論**：還缺播放器會帶的裝置／平台 header，而那個清單無法窮舉。停損。

### 後記：整軌預抓的入口其實一直都在（v0.4.0）

PLAY API 失敗之後，v0.3.x 認定「拿不到 master m3u8 就做不了整軌預抓」，
於是把整段預抓程式碼用 `USE_PLAY_API = false` 關掉——**但那個前提是錯的**。

**worker 本來就會抓 m3u8**（它就是播放器的網路層）。v0.3.x 的 hook 只是
`if (t.indexOf('-->') !== -1)` 一行就 return，只送 VTT 不送 manifest。
入口一直在手上，只是沒去拿。

v0.4.0 的修法：hook 也送 manifest，然後比照 userscript 用兩條路徑找字幕清單
（直接攔到 > 從 master 解析）。下游的分段抓取、批次翻譯全部沿用既有程式碼。

同時修掉三件事：

| 問題 | 說明 |
|---|---|
| **網址截斷復發** | `inject.js` 的 `url: String(u).slice(0,300)` —— 就是坑 #3。當時只拿來記錄所以沒事，一旦要用它去抓東西就會 400 |
| **manifest 未隨影片清空** | 換片後會拿上一支的字幕清單去抓，整支翻錯且不報錯（坑 #16 那類） |
| **收割鎖直接放棄** | 切換影片時前一次收割還在等待階段（最長 45 秒），新影片的 `startPrefetch` 撞上鎖就 return，**永遠不會再有整軌預抓**。改成有上限的重試 |

**順帶移除 PLAY API 的全部死碼（-339 行）與 `cookies` 權限**，商店審查面縮小。

> **教訓：一個被否決的方案會連帶埋掉它周邊的東西。**
> PLAY API 死的時候，整軌預抓被一起關掉了，而那兩件事其實沒有必然關係。
> 停用一條路徑時，要分清楚「真的依賴它的」和「只是剛好寫在一起的」。

程式碼保留，由 `USE_PLAY_API = false` 控制。

### ⚠️ F1TV 有 Imperva 機器人防護（重要，影響商業架構）

cookie 清單裡的 **`reese84` 是 Imperva Advanced Bot Protection**。
實測連續快速的 API 重試會開始被擋（表現為 `Failed to fetch`）。

**這對商品化的影響很大：**

1. **任何需要大量呼叫 F1TV API 的設計都不可行** —— 例如「每個用戶端各自去抓 manifest」
2. **共用譯文快取的價值因此更高** —— 全站一次收割，其他人只跟我們自己的後端說話
3. **Worker 注入反而是最安全的方案** —— 它搭播放器自己的請求便車，**零額外流量**

原本以為 Worker 注入的審核風險是缺點，實測後發現它在「不觸發對方防護」這點上
反而優於自行呼叫 API。

### ⚠️ 7.3 的「擴充功能只做 DOM」是錯的（v0.2.0 修正）

實測發現：**擴充功能跟不上語速，userscript 卻可以，即使兩者都沒預先收割。**

差別不在效能調校，在**提前量**：

| | 資料來源 | 提前量 |
|---|---|---|
| userscript | Worker 注入攔截 VTT 分段 | **47~53 秒**（實測） |
| 擴充功能 v0.1.x | 只讀畫面 DOM | **0 秒** |

播放器會提前約 50 秒下載字幕分段，userscript 的第二層攔截等於在提前翻譯。
DOM-only 沒有這個，逐句翻譯（1~3 秒來回 vs 每 3~4 秒一句）**物理上追不上**。
對直播與未收割的重播都不可用。

**解法不是 Worker 注入**：master 網址本來就來自主執行緒的 PLAY API，
擴充功能有 `host_permissions`，可以自己發同一個請求拿到串流位址，
再自行抓字幕清單與 VTT 分段。提前量拿回來了，而且完全不用注入任何東西。

```
PLAY API → master m3u8 → 字幕清單 → VTT 分段 → 批次翻譯 → 共用快取
```

重播一次抓完整支；直播用滑動視窗每 20 秒重抓（觀眾落後 live edge 20~40 秒，有餘裕）。

**新增 host permission**：`ott-video-fer-cf.formula1.com`（CDN）。

### 擴充功能的三個實測修正（v0.1.1）

| 問題 | 原因 | 修法 |
|---|---|---|
| **中英字幕並排而非上下** | 兩個子元素都是 `inline-block`，userscript 版本靠 `<br>` 分隔，移植時拿掉了 `<br>` 卻沒改排版 | 父層改 `display:flex; flex-direction:column`，比 `<br>` 乾淨且不受子元素 display 影響 |
| **HTTP 401 之後永不重試** | `loadBundle()` 只在換影片時呼叫。啟動時金鑰還沒填就 401，整場都走最慢的逐句路徑 | 指數退避重試 5 次；並監聽 `clientToken` 變更，一填好就立刻重抓 |
| **逐句翻譯跟不上語速** | 等 1.2 秒才送出，且一次只允許一個請求在飛，實際延遲疊到 4 秒以上 | 聚合縮到 300ms、允許 3 個請求並行、加「翻譯中…」暫時顯示。**但這條路本來就追不上 F1 語速，正解是預先收割** |

### ⚠️ `normKey()` 是三份程式碼的硬性契約

`extension/src/shared/normalize.js`、`backend/src/index.js`、`f1tv-zh-subtitles.user.js`
三處必須**行為完全一致**，否則同一句話算出不同的鍵，共用快取整個失效——
**不會報錯，只會默默重翻**。

已做成可重複執行的檢查：

```bash
node tools/check-normkey.js
```

**修改任何一處的 normKey 之後都要跑這個。**

---

## 8. 風險與防禦

| 破壞面 | 可能性 | 衝擊 | 防禦 |
|---|---|---|---|
| **DOM 選擇器改變** | 高 | 致命 | 遠端設定檔（選擇器陣列） |
| 網址結構改變 | 中 | 致命 | 同上（contentId pattern 也放設定檔） |
| 播放器整個換掉 | 低 | 致命 | 嗅探器自動找新選擇器 |
| CDN 網域改變 | 中 | 只影響收割器 | 設定檔 |
| API 版本升級 | 中 | 只影響收割器 | 設定檔 |
| **字幕改成加密** | 低 | 致命 | **無法防禦**，保留 DOM 路徑當底線 |
| **F1TV 自推繁中** | 中 | 產品歸零 | 賽季通行證讓現金提前入袋 |

### 防禦優先順序

| 順位 | 項目 | 說明 |
|---|---|---|
| 1 | **遠端設定檔** | ✅ **已實作並實測**（見 8.1）。選擇器用**陣列**依序嘗試（灰度推送期新舊版同時可用）。改設定幾分鐘內生效，不用等 1~3 天審核 |
| 2 | **主動驗證 + 自我診斷回報** | 見下方說明。**已在 v4.1.0 於本地實作，商品版需加上回報後端** |
| 3 | **雙顯示路徑 + 自動校準** | 快取同時存時間軸。平時用 DOM，同時偷量 DOM 顯示時刻與時間軸標記的偏移；DOM 失效時用時間軸接手並套用已校準的偏移。**把 DOM 改版從「致命」降為「幾乎無感」** |
| 4 | 嗅探器自動找選擇器 | 失效時跑有界限的文字變動嗅探，回報候選路徑 |
| 5 | 收割器賽前金絲雀 | 每時段開播前健康檢查，比用戶早幾小時知道 |
| 6 | 來源抽象層 | `SubtitleSource` 介面，F1TV 只是其中一個實作 |

**1、2、3 必須在上架前完成。**

### 8.1 遠端設定熱修 — 實測結果（2026-08-16）

**機制**

| 端點 | 用途 |
|---|---|
| `GET /v1/config` | 優先讀 KV，讀不到才用 Worker 內建預設 |
| `POST /v1/config` | 熱修入口（需 `ADMIN_TOKEN`），寫入 KV。**不用重新部署** |
| `DELETE /v1/config` | **還原鍵**，刪掉 KV 回到內建預設 |

用戶端：SW 快取 2 分鐘，content script 每 60 秒重讀並**就地套用**（重掛觀察者、
重套隱藏樣式、清除去重狀態）。選項頁有「立即重新載入設定」可跳過等待。

**已驗證**

```
[22:09:53] PitLingo v0.3.3 已啟動 | 設定版本 3
[22:10:27] ⚙ 遠端設定已更新 v3 → v1，選擇器已就地套用
```

- 後端 POST / DELETE 改得動設定 ✅
- 用戶端 **34 秒內**自動收到並套用 ✅
- 影片全程未中斷、不需重新整理 ✅

**行為層面也已驗證**：推 v2（`.pitlingo-does-not-exist`）之後字幕確實停止，
換回正確選擇器後恢復。**熱修機制完整成立。**

> 這次測試順帶逼出坑 #18：選擇器壞掉時畫面仍會「斷斷續續重複同一句舊字幕」，
> 那不是選擇器失效的正常表現，是補顯示邏輯的缺陷。

**踩過的兩個坑（傳播延遲比想像中難算）**

| 版本 | 問題 |
|---|---|
| 初版 | SW 快取 6 小時 —— 比賽日出事等於整場報銷 |
| v0.3.1 | 改成 10 分鐘，但 content script 的 60 秒重讀只會問到 SW 的快取，**實際傳播仍是 10 分鐘** |
| v0.3.3 | SW 2 分鐘 + 重讀 60 秒 → 最壞約 3 分鐘；另加手動立即套用 |

**通則：傳播延遲 = 所有快取層的總和，不是最短那層。** 診斷面板只寫「每 60 秒重讀」
會誤導自己，必須標明實際上限。

### 為什麼「主動驗證」值得排到第 2

v4.1.0 的「切換車手鏡頭後字幕消失」暴露了一個比 DOM 改版更陰險的失效模式：**時好時壞**。

| 失效模式 | 用戶行為 | 對生意的影響 |
|---|---|---|
| 完全壞掉 | 回報給你 | 你會知道，能修，用戶會等 |
| **時好時壞** | **默默流失** | **你永遠不知道發生過，也沒機會補救** |

**時好時壞比完全壞掉更傷。** 而且商品版的用戶不會看 Console，他們只會覺得「這東西不太可靠」然後在續訂時不續。

通則：**任何「註冊一次然後假設它持續有效」的機制，在 SPA 裡都必須有主動驗證的備援。** MutationObserver、事件監聽、播放器 API 掛勾——全部適用。

商品版要做的兩件事：
1. **主動輪詢驗證輸出**（已實作，見決策 4.6）
2. **靜默失效自動回報後端** —— 播放中 + 容器存在 + N 秒零字幕，就送一筆遙測（只需網域、播放器版本、失敗的選擇器）。**你要在用戶抱怨之前就知道。**

---

## 9. 路線圖

| 階段 | 內容 | 估時 | 狀態 |
|---|---|---|---|
| — | 個人自用版 | — | ✅ 完成 |
| **P0** | **免費開源驗證需求**（整理 README、發文、看裝機數） | 1 週 | ⬜ **仍建議補做** |
| P1 | 後端（Workers + KV 快取 + 上傳／下載 API） | 2 週 | ✅ **已部署並驗證**（見 10.1） |
| P2 | MV3 擴充功能 | 2 週 | 🟢 **功能面完成**：Worker 注入提前量、命中率 99.5%、遠端設定熱修已驗證 |
| P3 | 商業化（金流、登入、配額、免費層） | 3 週 | ⬜ |
| P4 | 上架（隱私政策、審核、著陸頁） | 1 週 | ⬜ |

**P0 是最重要且最便宜的一步。** 若免費版只有 50 人用，付費版不可能有 300 人買，該停在那裡。

---

## 9.1 下一步（接手時從這裡開始）

### 立刻要做（環境層面）

| # | 事項 | 說明 |
|---|---|---|
| 1 | **部署 backend v1.3** | `cd backend && wrangler deploy`。修的是 KV 寫入爆量（坑 #19），不部署會持續超額 |
| 2 | **等 KV 額度重置** | 每日 UTC 00:00（台灣早上 8 點）。在那之前寫入會失敗，但**讀取正常**，已翻好的內容照常能用 |
| 3 | **換掉 `ADMIN_TOKEN`** | 曾在對話中明文出現。`wrangler secret put ADMIN_TOKEN`。換完只需更新自己的 PowerShell 變數，一般使用者不受影響 |
| 4 | **`git push`** | 有 20 幾個 commit 未推送 |

### P2 收尾（剩下的都是形式要件，非技術風險）

| # | 事項 | 判準 |
|---|---|---|
| 5 | 完整看完一場正賽 | 命中率維持 95%+、無字幕重複、記憶體不持續成長、切換視角／全螢幕正常 |
| 6 | 確認坑 #20 的推測 | 若再出現「字幕停一下」，Console 應有 `⚠ 偵測到輪詢停擺 N 秒`。有那行就證實是瀏覽器節流 |
| 7 | 圖示 16/32/48/128 png | 上架必備 |
| 8 | 隱私權政策 | **必須說明 `cookies` 權限的用途**（目前只有 PLAY API 路徑用得到，而那條已停用——**可考慮直接移除該權限**，審核會更順） |

> ⚠️ **這一則已證實是錯的（2026-08-21 查證）。**
> 原文寫「`USE_PLAY_API = false` 之後 `cookies` 與 `ott-video-fer-cf.formula1.com`
> 都不再需要，可以移除」。**`cookies` 確實不需要（已移除），但那個 CDN 還在用**：
> **字幕軌的 m3u8 與 VTT 分段全部託管在 `ott-video-fer-cf.formula1.com`**
> （診斷報告裡 86 筆請求指向它）。移除那條 host permission 會讓**整軌預抓完全失效**，
> 而且不會報錯——只會退化成逐句翻譯，成本變成五倍。
> **不要移除。** 隱私政策裡已經有它的用途說明。

> **P3 的技術規劃見 [`P3-PLAN.md`](./P3-PLAN.md)**——分階段推送、成本後台、
> 金流比較（綠界 vs PayPal）、介面規劃、上線前檢查清單。
>
> **共用快取寫入遺失（約 30%）已決定採 B 案**：維持現狀不開放用戶端上傳。
> 理由是不願把「誰能寫共用快取」放寬到所有使用者（汙染風險）。
> 代價是成本不會歸零，但每多一個人看就補一點，會收斂。

### P3 之前必須先決定的事

| # | 決策 | 現況 |
|---|---|---|
| 9 | **網域** `pitlingo.com` 等 | **已購買**。Landing Page、Chrome Store 官網欄位、隱私政策託管都需要它 |
| 10 | 商標檢索（TIPO 第 9、42 類 / USPTO） | 未做。註意 `-lingo` 字尾已有 Duolingo 等既有品牌（見 7.0.2） |
| 11 | 金流地域範圍 | 綠界／藍新只收得到台灣的錢。若含港澳海外需另一條國際金流（見 7.0.1） |
| 12 | 法務檢視 | 企劃書列為 P0，Launch 前請律師看過 |

### 仍然建議補做的 P0

免費開源版 + 公用後端，成本幾乎為零，但能換到**真實的市場規模數字**。
目前所有推估（50~900 付費用戶）都是紙上談兵，安裝數是唯一可靠訊號。

---

## 10.1 共用快取實測結果（2026-08-16）

測試內容：`post-qualifying-show-japan`（cid 1000010205，407 個字幕分段）

**第一次觀看**（冷快取）：整軌預抓 407 段 → 全部翻譯 → 上傳 845 句

**第二次觀看**（重整後）：

| 指標 | 數值 |
|---|---|
| 從共用快取取得 | **845 句** |
| 命中率 | **100%** |
| 逐句呼叫 | **0 次** |
| 該次成本 | **約 US$0.01**（若無共用快取約 $0.13） |
| system prompt | 7,895 tokens（caching 生效） |

**共用架構驗證成功。** 第二位以後的觀看者成本趨近於零，這正是商業模型成立的前提。

---

## 10.2 整軌預抓與跳過收割實測（2026-08-16）

測試內容：`2026-monaco-gp-practice-3`（cid 1000010262，1,042 個字幕分段，約 1,855 句）

**第一次開啟（冷快取）**

| 指標 | 數值 |
|---|---|
| 收割 | 1,042 段成功、0 失敗 |
| 平均批次 | **14.3 句/次**（上限 15） |
| 逐句呼叫 | **0 次** |
| 增量上傳 | 82 → 371 → 669 → 947 → 1227 → 1511 → 1793 → 1823（每 60 秒） |
| 完整度標記 | ✅ `已標記完整：1042 段` |
| 成本 | **US$0.28** |

**第二次開啟（熱快取）**

```
⏭ 共用快取已涵蓋整支影片（1823 句 / 1042 段），跳過整軌預抓
```

**1 個請求取代 1,042 個。**

**新量到的數字**：整軌預抓下的提前量中位數為 **276,592 ms（4.6 分鐘）**，遠大於逐段攔截時的 47 秒——因為整支一次載入。自適應等待因此撞到 30 秒上限，屬預期行為。

---

## 10. 待驗證事項

- [ ] **直播情境**（三個產物都沒測過）：提前量、命中率、直播滑動視窗補抓
- [ ] 2026 車手陣容核對（腳本內名單需人工確認，我對部分席次沒把握）
- [ ] 整軌預抓在 2 小時正賽（約 1,200+ 分段）的表現與耗時
- [ ] 坑 #20 的推測（瀏覽器節流）需要 Console 出現 `輪詢停擺` 才算證實
- [ ] 譯文本地持久化（重看同一支影片免費）— 尚未實作
- [ ] 擴充功能的長時間記憶體表現（memo 上限 5,000 句，未實測過上限行為）

---

## 11. 更新紀錄

| 日期 | 版本 | 內容 |
|---|---|---|
| 2026-08-21 | **backend v5.5** | **五天代訂改為「Premium 單一比賽週末」**（NT$79 不變，鍵名 `svc_pro_5d` 維持不改）。⚠️ 這是唯一一個共用帳號商品，**四個限制**：只有該週末的五六日可用、僅限網頁端、高峰期有機率被擠掉、不附贈字幕授權。四點都放在**收合狀態的卡片上**——藏在展開後的說明裡等於沒寫。⚠️ 新增 `svcWeekend` 旗標，**結帳時必須指定場次**：不指定的話訂單進來時不知道要幫他開哪個週末的帳號，而那是人工作業，弄錯就整筆重來。已結束的場次同樣拒絕結帳。**一個月的代訂可以買多份，上限三個月**（`maxQty: 3`）。數量規則現在是三段：一場一份的商品（比賽週通行證、共用帳號代訂）夾成 1、有 `maxQty` 的照設定、其餘維持 5 的誤操作防護。使用條款與 FAQ 同步。 |
| 2026-08-21 | **backend v5.4 / ext v0.21.2** | **直播實測的第二輪後台稽核，抓到一個「安靜地付兩次錢」的漏洞。** 今日成本 $1.12，其中 `cid=misc` 佔 $0.13（287 句）。misc 是**所有影片共用的退路桶子**，把它的內容跟正片比對後發現 **98.1% 重疊**——同一場直播的同一句話被翻了兩次、付了兩次錢。成因：兩台裝置看同一場直播，一台的網址解析得到 contentId，另一台解析不到，於是後者整場的譯文都落進 misc。⚠️ **這個漏洞完全不報錯**：使用者照樣看得到字幕，只是這支影片的共用快取拿不到那些句子，下一位觀看者要重新付費。三管齊下：(1) cid 掃描擴大——查詢參數、`/detail/` 路徑、頁面的 `__NEXT_DATA__` 三種都找；(2) **沒有 cid 就先把句子留在佇列裡**，最多等 15 秒；(3) 等不到就照樣送出（沒有字幕比多付錢嚴重得多），但記 `PL-C08` 並寫進診斷，**把「安靜地多花錢」變成報告上看得到的一行**。另外這一輪也確認了修 bug 前的基線數字，供修好後比對：每句 $0.000519（文件記錄的整軌預抓是 $0.000155，**3.3 倍**）、共用快取命中率 25.9%（兩人同看同一場應該遠高於此）、一支影片 1010 次呼叫（每批只有 2.2 句）。**這三個數字就是驗收標準。** **商品變更**：五天代訂由 Pro 改為 **Premium 共用帳號**（NT$79 不變），鍵名 `svc_pro_5d` 維持不改（改了既有訂單的 plan 會對不上）。⚠️ 共用帳號的兩個限制（**僅限網頁端**、**高峰期有機率被擠掉**）必須出現在**收合狀態的卡片上**——寫在展開後的商品說明裡等於沒寫，而「買了才發現」是必然的客訴與退款。使用條款加了專屬條款，明列「被擠掉不構成服務瑕疵，亦不構成退款事由」。 |
| 2026-08-21 | **backend v5.3 / ext v0.21.1 / us v4.10.5** | **第一次雙人直播實測，抓出三個 bug，全部只有在「兩個人同時看直播」時才會現形。** ⚠️ **(1) `urgentKeys` 從來沒有任何地方 `add` 進去**——`isUrgent` 永遠是 false，於是每一批都被當成非急件，直播並發去重把**每一句**都讓給對方翻。實測回覆率只有 16~34%，字幕斷斷續續。單人測試完全看不出來（沒有人可以讓）。修法：畫面上正在顯示的句子標為急件，且**直播一律算急件**——並發去重的前提是「讓路的人有時間等別人翻完再讀快取」，而那個前提來自重播的整軌預抓（提前量約 47 秒），直播沒有提前量，讓路等於放棄那一句。⚠️ **(2) 直播的字幕清單在另一個 CDN**：重播是 `ott-video-fer-cf.formula1.com`，**直播是 `ott-video-cf.formula1.com`**（少了 `fer`）。`FETCH_ALLOW` 與 `host_permissions` 都只寫死重播那個，所以**直播的整軌預抓從來沒有成功過**——診斷裡只有一行「網址不在允許清單內，已拒絕」，功能靜默退化成只靠 worker 攔到的分段。改成樣式比對 `^(ott-video[a-z0-9-]*|f1tv)\.formula1\.com$`，manifest 收斂成 `*.formula1.com`，隱私政策同步更新。⚠️ **(3) 模型跳出角色，自言自語直接顯示在付費使用者的畫面上**：輸入是轉播畫面上的一個問句（且人名拼法與模型記憶不同），模型輸出「我沒有收到任何關於 Isaac Hadjar 的資訊。（注：根據我的知識庫……請提供完整的轉播內容片段，我會直接翻譯。）」。當時的守門是 `max(120, en×3)` ＝ 132 字，那段是 116 字，**差 16 字矇混過關**。三管齊下：`SYSTEM_PROMPT` 加規則 2a/2b/2c（輸入永遠是別人講的話不是對你說的、事實與拼字錯誤照原文翻、列舉禁止輸出的字串），守門長度收緊成 `max(60, en×2)`（中文比英文密，正常譯文不會比原文長），新增 `ROLE_BREAK` 樣式比對。**寧可那一句沒有譯文，也絕不能把模型的自言自語當字幕。** 另外新增後台「掃描可疑譯文／刪除」——**守門是後來才收緊的，之前放行的垃圾還躺在共用快取裡**，而快取是所有人共用的。 |
| 2026-08-21 | **backend v5.2 / ext v0.21.0** | **「這支影片沒有官方字幕」現在偵測得到、說得出口、也統計得到。** ⚠️ 判定刻意做成**三態**（has／none／unknown），最重要的是分出「確定沒有」與「還不知道」：只有**看過 master m3u8** 且裡面完全沒有 `TYPE=SUBTITLES` 才算 none。把 unknown 當成 none 會造成最糟的誤報——注入失效時對每一支影片都說「這支沒有字幕」，使用者會以為涵蓋範圍很差而退訂，而真正的問題（注入壞了）反而被那句話蓋掉。也刻意**不看使用者有沒有開 CC**：CC 關著但字幕軌存在時判定仍是 has，那種情況交給 `checkNoCaption` 去提醒開 CC。**畫面上會主動告知**，但**不給錯誤代碼**——影片本來就沒字幕是 F1TV 的內容問題不是我們的故障，發代碼等於訓練使用者把正常現象當故障回報。**後端新增 `nosub:<cid>` 統計**（`/v1/complete` 帶 `noSubtitles`），⚠️ 每支最多寫三次（KV 寫入是天花板，熱門影片被幾百人看過就寫幾百次的話光這一項就吃掉整天額度），用回報次數當信心值。後台翻譯清單多一組「無官方字幕」，與「收割失敗」**刻意分開**——前者我們無能為力，後者是我們的問題，混在一起看真正要處理的會被淹沒。**這個數字是決定「要不要自己做語音辨識」的唯一依據**，在有數字之前不做那個決定。FAQ 與購買頁同步說明涵蓋範圍以 F1TV 提供字幕的內容為準。 |
| 2026-08-21 | **backend v5.1 / ext v0.20.1** | **賽事場次改用真實時間**（f1calendar.com，原 Europe/London，已換算 UTC）。⚠️ 先前那版「標準時刻表推估」有**兩個實際錯誤**：把比利時標成衝刺賽（實際不是）、漏掉英國與荷蘭（實際是）。依 F1 官方 2026 Sprint Calendar 核對，衝刺賽是**中國、邁阿密、加拿大、英國、荷蘭、新加坡**六站。衝刺賽週的場次組成完全不同，錯了會讓商品卡列出不存在的場次。`est` 旗標已全部拿掉。**舊的 `weekWindow()` 整個移除**（連同 `MIN_PASS_SEC`）——兩套區間算法並存是漂移的來源，改了一邊忘了另一邊完全不會報錯，只會讓某些人的通行證早幾天失效。`planExpiry` 的退路也改走 `gpWindow`。**後台發碼可以指定大獎賽**（`gpIds`），發出的碼與 webhook 發的**欄位完全一致**（windows／gpIds／gpName／gpCount／nextLabel）——少一個欄位，使用者的設定頁就少一塊，而那不會報錯。**升級折抵補上多站測試**：5 張比賽週通行證仍夾在 NT$80、拿別人的碼證明所有權會被拒、代訂贈送的那張不能折抵。**擴充功能的剩餘時間會走了**（每分鐘重畫）。**著陸頁補上方案與價格**——先前只有標題與導覽列，從搜尋進來的人連「這是多少錢的東西」都看不出來。⚠️ **`ott-video-fer-cf.formula1.com` 的 host permission 不可以移除。** handoff 舊註記說「`USE_PLAY_API = false` 之後就不需要了」，查證後**那是錯的**：字幕軌的 m3u8 與 VTT 分段全部託管在那個 CDN（診斷報告裡 86 筆請求指向它）。移除會讓整軌預抓完全失效，而且不報錯，只是成本變五倍。舊註記已就地更正。 |
| 2026-08-21 | **backend v5.0 / ext v0.20.0** | **「一週通行證」改為「比賽週通行證」，而且一個 Grand Prix = 一個商品。** 這是架構級改動：商品**不再保存任何時間**，只綁一個場次代碼（`gpId`），效期由賽事資料**動態計算**——所以賽程延期、改時間、加減場次時，**已售出的通行證效期會自動跟著變**，不需要動商品也不需要重新發碼。⚠️ 區間定義為 ［本場第一場賽事 − 1 天, 下一場第一場賽事 − 1 天）。往前一天是為了涵蓋比賽週四的 warm-up 直播；不這樣做的話，荷蘭的暖身會落進匈牙利的通行證裡。**23 段完美接合**（測試會驗），任何時間點都只屬於一場，不會出現「兩張票都能看」或「都不能看」。⚠️ **跳著買不可以送中間那幾站。** 做法不是把區間塞進通行證簽章（那要改格式、已發出的通行證會全部失效），而是利用「通行證最長 14 天、到期一定回來續」這個既有性質：**續期時只簽到「現在這一段」的結束**，落在空檔就不簽。格式一行都不用改。**賽事資料大幅擴充**：國家、國旗、賽道、時區、每個場次的 UTC 時間、衝刺賽週、待定、狀態。⚠️ 場次時間目前是**依 F1 標準時刻表推估**（標 `est: 1`），購買頁明確標示「時間待官方確認」——**不可以假裝是官方時間**。**每天 04:00 UTC 自動同步賽程**（`SCHEDULE_URL`），⚠️ 嚴格驗證（站數、日期遞增、場次落在比賽週內），不合理就**整份拒絕並保留原賽程**——外部來源壞掉時若直接套用，購買頁會空掉而那看起來像我們倒了。**購買頁重做**：三個可收合的最高層級分類，比賽週通行證預設只顯示最近兩場（23 站全開會把頁面淹掉），已結束的收起來。每張卡有國旗、賽道、比賽週日期、第一場／排位賽／正賽時間（**依使用者瀏覽器時區換算**）、狀態、效期說明。購買前的四項提醒（需 F1TV 訂閱、需 VPN、僅桌面 Chrome/Edge、無七日鑑賞期）放在分類最上方。**後台新增「賽事資料」分頁**，與商品資料分開。**擴充功能顯示**改成「涵蓋場次／有效至（下一場第一場賽事前）／剩餘 N 天 M 小時」。 |
| 2026-08-19 | **backend v4.9 / ext v0.19.4** | **通行證效期改成「到下一個比賽週開始的前一天」**（使用者：比賽週星期四就有 warm-up 直播，所以切在那之前）。⚠️ 有兩站是**星期四開賽**（亞塞拜然、拉斯維加斯），所以規則寫成「下一站 start 的前一天」，不能寫死星期三。實際效期 10~16 天。⚠️ **正賽當天購買**的處理：賽程只有日期沒有時間，伺服器分不出「正賽剛開始」與「正賽已結束」——與其猜錯，改成**購買頁明講**涵蓋哪一站、到哪一天，正賽當天再加一句「若您要的是下一站請明天再買，或直接把站數改成 2」。**升級折抵加上限 NT$80**（使用者決定，兩站 78 直接算 80）。⚠️ 沒有上限的話，通行證可以買多站之後，12 站（NT$468）的折抵會超過賽季票本身。**授權清單的方案下拉復原**（使用者決定），只列 issuable、二次確認並明講「訂單金額不會跟著改」。**全專案一致性稽核，新增 `tools/check-consistency.js`**（11 項），當場抓出五個真實問題：(1) `paid.html` 的 CSS/JS 完全沒有 `?v=`，改版後使用者永遠吃舊檔；(2) `?v=` 三個版本並存，buy.html 停在 4.4——**我剛改過 site.css，那頁的站數選單會完全沒有樣式**；(3) FAQ 殘留「一週通行證用完七天就結束」；(4) **購買頁寫「將直接為您升級，不另行收費」，但後端照樣送去綠界收款——畫面在說謊**，而且沒有任何程式碼在兌現那個承諾（改成誠實說明並導向客服，同時加 E14 擋住小額送進金流）；(5) **`options.js` 寫死方案對照表，裡面有一個根本不存在的鍵 `weekend`**（真正的鍵是 `week`），所以買一週通行證的人在設定頁看到英文鍵名——改成由伺服器回 `planLabel`，用戶端不再自己維護一份。**買多站現在使用者與後台都看得出來**（`gpName`／`gpCount` 一路接到授權清單、帳號視圖與擴充功能設定頁）。⚠️ 寫檢查工具時踩到：正則不可以寫在樣板字面量裡，`\b` 會被當成**退格字元**而不是 word boundary——檢查會永遠是綠的，注入假 bug 也抓不到。**Google 一鍵登入的實作方式已定調**（Google Identity Services + 後端 JWKS 驗簽，不在擴充功能裡跑 OAuth），寫進 CLAUDE.md。 |
| 2026-08-19 | **backend v4.8** | **通行證改成錨定賽程，不再是「購買起算七天」。** 舊版在背靠背的兩站之間會讓一張票看兩場（2026 的 23 站有 9 站會漏）；上一版夾了上限，但那讓效期變成「有時七天有時三天」，名字開始說謊。現在明確賣的是**接下來 N 個比賽週末**：效期到最後一站結束的隔天（加一天是因為各站時區不同——拉斯維加斯的正賽當地週六深夜，換算 UTC 已是週日）。⚠️ 加上**保底 72 小時**：正賽快結束時買，若只算到該站結束等於花 39 元買十分鐘。72 是算過的上限——背靠背兩站之間有 5 天，保底 3 天安全、4 天有風險；改這個數字前先重算。保底之後**仍然要夾**「範圍外的下一站」，安全優先於慷慨。**通行證可以一次買多站**，上限是本賽季剩餘比賽週（使用者決定：想一次買到季末就讓他買）。買 N 張＝涵蓋接下來 N 站，不是同一站買 N 份——購物車會列出站名，不講清楚的話買 3 張的人會以為自己重複買了。⚠️ 數量必須從購物車讀回 webhook：買 3 站卻只發一站的效期，是安靜地少給使用者付過錢的東西。**移除購買頁主動算給買家看的價格對照**（使用者決定）：那句話會讓正在看賽季方案的人突然要做一次算術比較，多一個比較就多一批放棄結帳的人；剩餘比賽週末數仍然顯示，想算的人算得到。**授權清單的方案下拉復原**（使用者評估後決定保留——客服現場確實會遇到「補償時直接升級」）；保留的前提是下拉只列 issuable 的方案、每次二次確認，並在確認視窗明講「訂單金額不會跟著改」。**Google 一鍵登入的四個坑已寫進 CLAUDE.md**：sub 才是主鍵（email 會變）、email_verified 一定要驗、購買 email ≠ Google email 必須留「用授權碼認領」的路、不要在擴充功能裡做 OAuth。⚠️ 過程中用 `sed -i` 改後端，把整個檔案的 CRLF 換成 LF，`check-prompt` 立刻紅字（SYSTEM_PROMPT 是位元組級契約）——**這個專案不要用 sed -i**。 |
| 2026-08-19 | **backend v4.7 / ext v0.19.3 / us v4.10.4** | **定價改成兩張獨立的卡**：本賽季通行證永遠只賣本賽季（上半季 599／夏休起 299，季末不再自動換商品），下一賽季通行證獨立成 `season_next`，**平常鎖住、剩不到 8 個比賽週末才開放預購**。鎖住不等於隱藏——整張消失會讓人以為賣完了，留著並說明「什麼時候開放」本身就是預告。⚠️ 使用者明確決定**賽季尾聲不再自動下架本賽季票**（剩幾場值不值得由他自己判斷），代價是那一段可能比單買貴，所以購買頁會主動算給他看「一週票買滿是多少」——「自己決定」的前提是看得到另一個選項的總價。**修掉一張 NT$39 的票涵蓋兩個比賽週末的漏洞**：「購買起算七天」在背靠背的兩場之間，正賽當天買會蓋到下一場的正賽日。實測 **2026 的 23 場裡有 9 場會漏**。改成效期再夾一次「下一場比賽週開始前」。**免費層的影片判斷搬到伺服器**：那條規則以前只存在於用戶端，改一下用戶端就繞得過去（**那是疏漏，不是取捨**）。slug 優先用收割時寫入的 `bundle.slug`（使用者改不到），退回用戶端送的；兩者都沒有時放行，因為值得白嫖的熱門影片一定被收割過，為此擋掉新影片會弄壞獲客漏斗。**授權管理只留客服真的會用到的動作**，移除「換方案」——買錯的正解是退款重發，就地改方案會讓訂單金額與授權內容對不起來，而 13 個方案的下拉在壓力下很容易選錯且不報錯。發碼下拉也只列「發了有用」的方案（代訂方案本身發出去是一張立刻到期的碼）。**六項新建設**：(1) 每位使用者的 token 用量（獨立的 `ustats:` 日鍵，跟 stats 同一次 flush 落地，不增加 KV 寫入次數）(2) 錯誤代碼系統 `PL-Cxx`／`Exx`，⚠️ **兩種「沒有字幕」刻意不給代碼**（影片本來就沒字幕、中途長時間沒旁白），給了就是訓練使用者把正常現象當故障回報 (3) 固定測試資料 `tools/fetch-fixtures.js` + `check-fixtures.js`，離線檢查不變式（三份 normKey 用真實字幕實測、空譯文、整句未翻、簡體字），`--live` 才重翻比對 (4) 成本後台：每次翻譯／每支影片／每人日／付費者／免費者的成本，以及 CAC／LTV／毛利率，⚠️ **量得到的與估出來的分開呈現**，續訂季數沒資料之前一律 1 (5) FAQ 16 題 (6) **帳號模型定調**：`Account(email) → Purchases / Licenses / Entitlements`，每筆訂單與授權碼都寫 `acct`，`check-backend` 會掃所有發碼路徑漏寫就變紅；後台的「查一個客戶」就是帳號系統的預演。 |
| 2026-08-19 | **backend v4.6 / ext v0.19.2 / us v4.10.3** | **修掉一個會白送永久授權的漏洞。** 購物車裡全是代訂時，`quoteCart` 的 `primary` 會取到代訂方案本身，而代訂方案沒有 `days`／`weekBound`／`untilSeasonEnd`，`planExpiry` 回 `null` ＝ **無期限**——買一次 79 元的五天代訂，拿到的是一張永遠不會過期的字幕授權。不報錯，只是白送。修法：新增兩個內部方案，`week_svc`「一週通行證（代訂）」與 `svc_none`「代訂服務（不含字幕授權）」，全代訂購物車依有無 bundleWeek 發其中之一；另外在 `planExpiry` 加最後一道——**代訂方案一律回「已過期」**，日後任何新路徑發錯方案，最壞結果是「立刻過期」而不是「永久授權」。要壞就壞在安全的那一邊。`week_svc` 帶 `fromService`，**不列入升級折抵**（拿我們送的東西折我們的錢）。後台因此分得出這一週是代訂送的還是使用者自己買的。**「單句長度不可超過 1000 字元」的成因找到了**：擴充功能有這道防線、**userscript 完全沒有**（同一條規則只套在一個產物上，坑 #21 那一類）。F1TV 偶爾把整段節目介紹塞進字幕容器，那一句就讓整批被退回。兩邊都修：userscript 補上 `MAX_LINE_LEN`，後端改成**只丟掉超長的那幾句、不整批退回**——防成本放大的效果一樣，但不會再讓同批 199 句正常字幕陪葬。**播放器關閉（網址不變）現在會停止字幕偵測與顯示**：收割端早就有這個判斷（坑 #15），顯示端沒有，於是疊字會留在畫面上擋著。兩邊改用同一個 `playerGone()`。**營運設定跟著伺服器的方案走**：分組顯示、標出「此刻實收」（賽季分段價與牌價不同時），下架改成勾選——填代碼時打錯會被伺服器靜默忽略，你以為下架了、購買頁上還在賣。 |
| 2026-08-18 | **backend v4.5 + 後台** | **後台新增「訂單」分頁，並修掉後台與定價脫節的問題。** (1) **訂單在按下結帳的當下就建立**，不是付款成功才建立——所以「有人結帳卻沒付成功」現在看得到。之前那些訂單在系統裡完全不留痕跡，使用者回報付款失敗時後台查無此人。狀態機 `created → awaiting → paid / failed`，⚠️ **只准前進**：付款結果的導回頁是使用者的瀏覽器送的、內容可偽造，若允許倒退，任何人都能拿別人的訂單編號把那筆標成失敗（`check-backend.js` 已把這條寫成測試）。導回頁要改訂單狀態前還會先驗綠界簽章。(2) 列表**只讀 KV metadata、不逐筆 get**，所以 KV 操作數與訂單數無關（坑 #10 的預算天花板）。(3) **新訂單通知**：後台每 45 秒打一次 `?feed=1`（同一次 list，回應幾十位元組），有新的已付款訂單就跳頁內橫幅 + 桌面通知 + 嗶聲，分頁標題掛紅點。⚠️ 要求通知權限與解鎖音訊**必須在使用者按鈕的那個手勢裡**做，寫在載入時會被瀏覽器靜默忽略。(4) **補寄授權碼信**與**代訂結案**：寄信會失敗（網域沒驗、信箱打錯），沒有補寄按鈕就只能請買家自己去查；代訂沒有結案欄位就沒人知道處理了沒。⚠️ **刻意不做「手動標記為已付款」**——那等於在後台開一個發碼後門。(5) **後台的方案下拉不再寫死**。之前寫死成「早鳥 399／正式 599」，定價改成「上半季 599／下半季 299／一週 39」之後後台還在發早鳥碼，賣的東西與網站上是兩套且不會報錯。改由 `/v1/admin/plans` 提供**此刻真正的售價**（含後台覆寫與賽季分段），`check-admin.js` 改成反過來擋「寫死的方案或價格」。(6) 後台發碼補上 `weekFrom` / `paid` / `items`，與 webhook 發的碼欄位一致——少了它們，一週通行證不知道七天從哪天算、日後升級也折抵不到，兩者都不報錯。 |
| 2026-08-17 | **backend v4.4 + 網站** | **修「付款成功卻看不到任何訂單資訊」。根因：綠界用 POST 導回 `OrderResultURL`，而 `/paid` 是 Cloudflare Pages 的靜態頁——靜態頁讀不到 POST body**，所以頁面上什麼都沒有。（我改寫 `buy.js` 時又弄丟了 `sessionStorage` 的訂單編號備援，兩個一起失效才完全看不到。）改成 `OrderResultURL` 指向 Worker 的 `/v1/payment/result`：接住 POST、取出訂單編號與 `RtnCode`，再 302 轉到 `/paid?no=…`。⚠️ **這個中繼端點不發碼、不改任何狀態**——瀏覽器導回是使用者可以偽造的，發碼只能留在伺服器對伺服器的 webhook。**付款失敗改為導回並提供重試路徑**（顯示金流回覆原因、按鈕回購買頁）——只說「失敗」而把人留在死路上，那筆生意就結束了，而多數失敗換個付款方式就能成功。**新增寄信**（Resend，免費層 3,000 封/月）：授權碼、訂單編號、購買項目、啟用步驟。⚠️ 寄信失敗**絕不影響發碼**——授權碼此刻已在資料庫，寄不出去補得回來；但若因寄信拋例外讓 webhook 回非 200，綠界會重送而重送被去重擋掉，結果是買家永遠收不到且查不出原因。失敗會寫 `mailfail:<訂單>` 供追查。**後台移到 `legal/admin.html`**，隨網站部署到 `/admin`，平板手機都能用；唯一的門仍是 `ADMIN_TOKEN` |
| 2026-08-17 | **userscript v4.10.0** | **收割加速：翻譯改為並行。** 實測 390 段 / 815 句的一支影片，抓分段只花 91 秒，**翻譯卻花了 192 秒**——`drainPrefetch()` 的 `while` 迴圈裡是 `await translateBatch(batch)`，57 批嚴格排隊。**翻譯才是收割慢的主因，不是抓取。** 改成 N 條並行（預設 4）：批次之間彼此獨立、結果各自寫進 memo，沒有順序需求。並發數刻意不開大——超過 Anthropic 的速率限制會開始回 429，重試反而更慢。單一批次失敗改成不拖垮其他工作者（原本一個例外會中斷整個迴圈）。`fetchConcurrency` 3 → 6（收割時無人觀看，但仍顧慮 Imperva 不開太大）。兩個並發數都列進診斷報告，否則調了不知道有沒有生效 |
| 2026-08-17 | **backend v4.2 + extension v0.19.1 + userscript v4.9.5** | **後台新增「翻譯清單」分頁**：依比賽週分組，看得出哪幾場已經備妥、哪幾場還缺。⚠️ **只讀 KV metadata，不讀 bundle 本體**——一份兩千句的 bundle 約 200KB，一百支就是 20MB 的讀取，一次列表就會逾時而且把讀取額度吃光；`list()` 本來就會回傳 metadata，那正是為這種用途設計的。`writeBundle()` 起同時寫入摘要（句數／分段數／時間／slug），metadata 有 1KB 上限所以只放摘要不放內容。**舊資料沒有 metadata 會列在「待補資料」，下次被寫入時自動補上——刻意不做回填**，回填要把每一份讀出再寫回，代價正是要避免的那件事。分組靠 **slug**：我們只存 contentId（一串數字，人看不出是澳洲正賽還是賽後訪問），所以三個用戶端（`/v1/subs`、`/v1/complete`、擴充功能）都改成順手帶上 `location.pathname`。⚠️ 賽程的 `name` 是中文而 F1TV 的 slug 是英文，直接比對永遠對不上，因此另建 `GP_SLUG_HINTS` 英文關鍵字對照表（**改賽程時要記得一起改**）；關鍵字刻意選夠獨特的（用 `united-states`／`austin` 而不是 `usa`，後者會誤中 `usual`）|
| 2026-08-17 | **userscript v4.9.4** | **自動收割完全不會前進——`hqWatch()` 裡的 `path` 是未定義的。** 上一輪把追蹤鍵從 contentId 改成完整路徑時用字串替換改名，**漏掉了這個函式**：宣告的是 `const cid = currentContentId()`，底下卻用 `path`。第一行 log 就拋 `ReferenceError`，而它在 `setTimeout` 的回呼裡——**例外被吞掉，Console 也不一定看得到**。表現出來是「收割明明完成了，佇列就是不動」：實測診斷顯示 390/390 段抓完、815 句已上傳、`翻譯完成`，一分鐘後還停在同一頁。**新增 `tools/smoke-userscript.js`** ——擴充功能早在坑 #22 就有 `smoke-extension.js`，userscript 卻一直沒有，**同一條規則沒有套用到另一個檔案上**（鐵則 #13）。它在假瀏覽器環境裡實際執行整份腳本，並**一路跑完巢狀排出來的 setTimeout 回呼**（`hqWatch` → `tick` 是兩層），判定標準只有「不可有未捕捉的例外」。⚠️ 第一版的測試抓不到那個 bug，因為 `hqWatch` 開頭就 `if (!d.running) return`——**佇列狀態必須在執行腳本之前就設成 running**，否則那條路徑等於沒被測到。已用注入原始 bug 驗證會變紅燈（`❌ 排程回呼拋例外：path is not defined`）|
| 2026-08-17 | **userscript v4.9.3** | **兩個修正，都是使用者從診斷報告看出來的。** (1) **共用快取已涵蓋整支影片時，自動收割不會跳到下一部**：`hqDone()` 的結束條件是 `stats.segFetched > 0`，但這種情況**根本不會去抓分段**（`harvestSkipped++` 後直接閒置），segFetched 永遠是 0 → 判定「還沒好」→ **白等 15 分鐘逾時後記成失敗**。診斷擺明了資料齊全（`已跳過 1 次`、`清單/已抓 1203/0 段`、`快取完整度 1203 段 / 2216 句`），佇列卻卡住。加上 `harvestSkipped > 0` 或 `bundleSegCount && bundleLineCount` 就立刻算完成——這其實是**最常見也最該快的路徑**（第二次跑同一份佇列、或別人已收割過）。(2) ⚠️ **`@version` 與 `const VERSION` 早已漂移**：metadata 是 4.9.2，而診斷報告與事件時間軸一路顯示 **v4.7.2**。後果是**診斷報告會說謊**——「使用者跑的是哪一版」是排查任何問題的第一個依據，那個數字錯了後面全部被誤導，而且兩個數字各自都很合理，只有並排看才看得出來。這與 `normKey`／`SYSTEM_PROMPT` 是同一類問題（**同一件事寫在兩個地方**），已加入硬性契約表並新增 `tools/check-userscript-version.js`，用注入漂移驗證過會變紅燈 |
| 2026-08-17 | **userscript v4.9.2** | **自動收割的第二個網址錯誤：舊格式資料留在儲存裡。** v4.9.1 修好了「刮取時保留 slug」，但**沒有處理已經存進 GM storage 的舊資料**（只有 contentId）。那些拿去組網址會變成 `f1tv.formula1.com1000010530/?action=play`——**連網域都拼錯**，瀏覽器回 `ERR_NAME_NOT_RESOLVED`，看起來像網路壞掉，使用者會去查 DNS 而真正的原因在一行字串拼接裡。**舊資料留在儲存裡而程式碼假設它是新格式，是最難查的一種錯**：程式碼本身是對的，測試也會過，只有升級過的使用者會踩到。改成 `hqLoad()` 讀取時就用 `HQ_VALID = /^\/detail\/\d+\/[A-Za-z0-9\-_]+$/` 過濾並記錄清除筆數（狀態選單看得到），`hqNext()` 導覽前再驗一次當最後防線。**與其要使用者記得清空佇列，不如讓資料自我修復**——依賴使用者做正確的事，等於沒有防護。slug 無法從 id 反推（`/detail/<id>` 不會轉址，只回 404），所以沒有 slug 的連結一律不進佇列 |
| 2026-08-17 | **userscript v4.9.1 + backend v4.1** | **修自動收割的 404。** 第一版只把 `contentId` 存進佇列，導覽時組成 `/detail/<id>?action=play`——但 F1TV 的網址是 `/detail/<id>/<slug>`，**少了 slug 一律 404**，每一支都跳到「This page does not exist」。改成刮取時保留完整路徑；只抓到編號沒有 slug 的連結會被略過並提示（那種網址一定失敗）。新增**分類略過**（使用者指定）：記者會、F2、F3、Shows & Analysis（`weekend-warm-up` 除外）、2024 年以前的內容——判斷依據是網址 slug，那是列表頁唯一拿得到的分類資訊。略過不是為了省事，是**這些內容翻譯價值低而收割成本一樣高**，一場記者會照樣要抓上千個分段。**背景分頁節流**：Chrome 會把背景分頁的計時器節流到每分鐘一次，收割會幾乎停住；啟動時明講兩種不受影響的做法（獨立視窗保持可見／`--disable-background-timer-throttling`），並說明「影片播放通常能豁免部分節流但不保證」。**代訂取消「自備帳號／無自備帳號」的區分**：實際作業沒有差別，只是讓買家多做一次不必要的選擇；統一成一種、只需 email。⚠️ **鍵名保留 `_own` 後綴不改**——改了會讓既有訂單與授權記錄裡的 `plan` 對不上，而那是查不回來的資料；命名難看勝過資料對不起來。條款與隱私政策同步更新（不再索取 F1TV 帳號密碼、不持有任何帳號憑證）|
| 2026-08-17 | **userscript v4.9.0 + `LIVE-TEST.md`** | **自動收割佇列**：把 F1TV 的重播內容在賽前一次跑完，讓第一位觀眾的成本由管理員在離峰付掉。⚠️ **刻意不寫成爬蟲或無頭瀏覽器**——F1TV 有 Imperva 機器人防護，任何「不是真人在真瀏覽器操作」的模式都有被擋的風險，而被擋之後連自己都看不了。做法是**讓同一個分頁自我導覽**：載入 → 收割 → 換下一個網址；對 F1TV 而言與人工一支支點開沒有區別，因為就是同一個瀏覽器、同一份登入狀態。狀態存 GM storage，跨導覽與關閉瀏覽器都不遺失。⚠️ **結束條件有三個，缺一不可**：收割不在進行中、待翻佇列清空、已上傳完畢——`harvestInFlight` 變 false 只代表分段抓完，那時翻譯可能還有幾百句在飛，太早換頁等於抓了 1,000 個分段卻只上傳一半，下一個人還是要付費。單支上限 15 分鐘，逾時跳過（已翻的部分早就寫進共用快取，不是災難）。新增 **`LIVE-TEST.md`**：直播實測計畫。**一台電腦用三個 Chrome 設定檔就能模擬三個獨立使用者**（各自有獨立 installId）。五個要量的數字：提前量（**最關鍵，< 3 秒的話前瞻預譯在直播上不成立，去重設計要重做**）、DO 去重是否生效、KV 寫入增量、免費層計時（直播用累計秒數，暫停不可計時，從未實測）、播放品質。另修 `wrangler.toml`：**帶 DO migration 的第一次部署必須用 `wrangler deploy`**，`wrangler versions upload` 會被 Cloudflare 以 code 10211 拒絕（migration 是全域一次性狀態變更，與漸進式部署語意衝突）|
| 2026-08-17 | **backend v4.0（Durable Objects 步驟 1~3）** | **直播並發去重改用 DO，實測模型呼叫從 10 次降到 2 次。** Cache API 與 KV 都沒有 CAS，`claimLine` 的 check-then-act 在同時抵達時完全失效（實測 10 並發 → 10 次呼叫）；DO 是**單執行緒**，同一個 cid 的請求排隊進入，認領因此是原子操作。新增 `export class SubtitleRoom`（每支影片一個實例）與 `claimViaDO()`。三個刻意的設計：(1) **DO 只做協調，絕不做翻譯**——它是單執行緒且運算時間計費，把 Anthropic 呼叫放進去會讓第 2 個人等第 1 個人的 API 來回，那 1~3 秒還要付 GB-秒；(2) 認領記錄**只放記憶體不寫 storage**，壽命只有 25 秒，寫進去等於為短暫旗標付儲存費，實例回收最壞只是重複翻一次；(3) **`locationHint: 'apac'`**——DO 實例位置在第一次建立時決定，不指定可能建在美國，台灣使用者每次認領多一趟跨太平洋往返。**DO 不可用時一律退回 Cache API 版**：它是新的單點，第一次上線就全部依賴是不必要的風險。⚠️ `[[migrations]]` **不可逆**，class 部署後不能隨便改名或刪除——這是本專案第一個「部署錯了不能單純回滾」的東西。兩支檢查工具要一併剝掉 `export class`（只剝 `export default` 的話沙箱會在那行語法錯誤，而訊息指向 index.js 的行號，看起來像後端壞掉）|
| 2026-08-17 | **extension v0.19.0** | **擴充功能加上購買入口。** 三個位置：(1)「授權」區塊在**沒有授權碼時**顯示「前往購買」按鈕，放在輸入框下方——順序本身就是引導，有碼的人往上填、沒碼的人往下買；(2) **免費額度用完的狀態卡直接給連結**，那是使用者最有意願購買的一刻，只說「已結束」等於把人擋在門口；(3) 頁尾補上官方網站／購買／聯絡我們／隱私權政策／使用條款五個連結。用 `<a>` 不用 `<button>`：這是離開擴充功能的導覽，使用者也才能中鍵開新分頁。另修事件訊息裡寫死的「購買 Season」——一週通行證上線後那句話就是錯的，且會隨定價調整而漂掉，改成指向購買頁（那裡的價格永遠是對的） |
| 2026-08-17 | **網站 v4.3** | **首頁與購買頁加上 30 秒實際運作示範影片。** 原始錄影 `test_video.mp4` 是 1080p30、**18.8 Mbps、66.7 MB**——**超過 Cloudflare Pages 單檔 25 MB 上限**，而且對網頁本身也太重（手機使用者還沒決定要不要看就先被吃掉流量）。用 ffmpeg 轉成 720p／CRF 23／AAC 96k → **9.4 MB**，並抽一張有雙語疊字的畫面當 `poster`。`preload="none"` 是必要的：不加的話一進站就開始下載那 9 MB。原始檔已加進 `.gitignore`（不進 git、不進 `legal/`；`legal/` 是唯一會被部署的資料夾，整個目錄目前 9.8 MB）。重新編碼指令留在此處備查：`ffmpeg -i test_video.mp4 -vf scale=1280:-2 -c:v libx264 -profile:v high -crf 23 -preset slow -pix_fmt yuv420p -movflags +faststart -c:a aac -b:a 96k legal/demo.mp4` |
| 2026-08-17 | **網站 v4.2** | **方案卡片改成可展開。** 12 個方案每張都攤開完整說明時整頁變成幾千字，標題與價格被擠成一團——使用者連「有哪些方案」都看不出來，更不用說比較。收合時只有名稱＋價格＋一行重點，細節放進 `<details>`（用原生元素而不是自己寫展開邏輯：本來就支援鍵盤與螢幕閱讀器，也不會因為 JS 出錯就永遠打不開）。⚠️ **選取與展開必須綁在不同元素上**，否則使用者想看說明卻把方案加進購物車。新增 **Pro／Premium 對照**：官方截圖（`legal/f1tv_pro_plan.jpg`、`f1tv_premium_plan.jpg`）與文字表格**並存**——截圖是佐證讓買家對照官方原文，表格才是主要內容（可搜尋、可翻譯、螢幕閱讀器讀得出來、手機不會糊）。順帶修兩處顏色錯誤：`.compare .yes` 與 `.cartRow.adj` 原本用 `var(--accent, #16a34a)`，而 `--accent` 是**品牌紅 `#e10600`**，綠色後備永遠不會生效——勾勾會變紅勾、跟旁邊的紅叉同色，折抵金額也會看起來像錯誤。改用 `--ok` |
| 2026-08-17 | **網站 v4.0 + 新檢查工具** | **坑 #36：正規表示式字面量是延遲編譯的，所有靜態檢查都放行。** 注入商品說明時反斜線被 shell 吃掉，`/\*\*(.+?)\*\*/g` 變成 `/**(.+?)**/g`（`*` 沒有可重複的對象），`buy.js` 整個壞掉。實測 **`node --check`、`new Function()`、`vm.Script`、`esbuild` 四者全部放行**——它只在那一行真的被執行到時才爆炸，而錯誤被最外層 try/catch 吃掉，使用者看到的是「目前無法取得方案資訊」，一個與真正原因完全無關的訊息。新增 `tools/check-regex.js`：找出正則字面量逐一 `new RegExp()` 真的編譯一次（168 個），並用注入原始 bug 驗證會變紅燈。⚠️ 第一版的偵測樣式有 `(?![*/])` 守衛（為了跳過 `/*` 註解），**正好把要抓的目標排除掉**——改成強前導（`.replace(` 等，該位置必定是正則）不加守衛、弱前導（`=`、`,`）保留守衛。另修：`[hidden]` 屬性被 `.field { display: flex }` 蓋過導致授權碼欄位在未勾選時仍顯示（HTML `hidden` 最常見的失效方式），已加 `[hidden] { display: none !important }` |
| 2026-08-17 | **backend v3.9** | ⚠️ **修升級補差價的所有權漏洞。** 第一版是「填 email 就查得到可折抵金額」——但 **email 是公開資訊，不是憑證**。任何人填別人的 email 就能拿到那個人的折抵，而新授權碼會在付款完成頁上直接顯示給付款的人看，**等於用受害者的錢買自己的授權**。改為必須同時提供一組**屬於自己的一週通行證授權碼**：授權碼是隨機 12 碼、只寄給買家本人、猜不到，且那組碼登記的 email 必須與這次填的相符。另外**折抵失敗一定要說出原因**（`need_key` / `key_not_found` / `email_mismatch` / `not_week_pass` / 查無可折抵）——只看到金額沒變，使用者會以為系統壞了，或更糟，以為自己被坑了。**12 個方案全部加上商品詳細說明**，含 F1TV Pro 與 Premium 的實際差異（Premium 多出 Multiview 多視角、4K UHD／HDR、六台裝置同時觀看；其餘內容兩者相同）。說明顯示在卡片內而非彈窗——買家比較方案時資訊必須同時看得到 |
| 2026-08-17 | **backend v3.8 + 後台營運設定** | **價格與營運參數改為後台可調，不必重新部署**（使用者要求「整個網站我要能自訂」）：新增 KV 的 `ops:config` 與後台「營運設定」分頁，可改各方案價格、早鳥限量、下架方案、綠界付款方式、購買頁公告。覆寫**只接受已知的方案鍵與 1~99999 的整數**——填 0 或負數一律忽略，那種錯誤直接等於金錢損失；後台儲存後回報的是「**實際生效的值**」而不是「你送出的值」，只說「已儲存」會讓人以為填 0 也有效。**綠界 10300023「本次交易未提供任何付款方式」**：`ChoosePayment: ALL` 的語意是「顯示商店已開通的全部方式」，商店在綠界後台一項都還沒開通時，過濾完就一種都不剩；改為後台可設定，填 `Credit` 可繞過。**購買頁顯示舊價格的原因是瀏覽器快取**——線上 API 早已回傳正確資料，是 Pages 長期快取了舊的 `buy.js`；已為 JS/CSS 加上 `?v=` 查詢字串。另修三個檢查工具自身的缺陷：`check-links` 的 `for (const h)` 無法重新指派、`check-backend` 剝 `export default` 的正則貪婪吃到檔尾、以及 **`String.replace` 的替換字串裡的 `$` 加單引號是特殊語法**（代表「匹配之後的內容」），用它注入含 `NT$` 的程式碼會被靜默吃掉——改用函式當替換值 |
| 2026-08-17 | **backend v3.7.1** | **坑 #35：`node --check` 對 ESM 檔案有盲點。** CORS 重構把 `export default { async fetch() }` 拆成獨立函式時，尾端的 `},
};` 沒改乾淨。`backend/src/index.js` 是 ES module 但副檔名是 `.js`，於是 `node --check` 用 **CommonJS** 解析並**放行**，而 wrangler 用的 esbuild 直接爆掉——**check-all 全綠卻部署不了，比紅燈更浪費時間**。修法：複製成 `.mjs` 再檢查一次（`.mjs` 強制 ESM 解析，實測 `.js` exit 0、`.mjs` exit 1），已加進 `check-all.js` 的語法區段，並用注入壞法驗證這支檢查確實會變紅燈 |
| 2026-08-17 | **backend v3.7 + 網站** | **修 CORS——購買頁「Failed to fetch」的真正原因。** 後端曾**刻意拿掉**一般端點的 `Access-Control-Allow-Origin`，註解寫著「我們根本不需要 CORS」，而那句話在當時是對的：只有擴充功能（service worker + host_permissions）與 userscript（GM_xmlhttpRequest）在打 API，兩者都不受 CORS 限制。但**購買頁是一般瀏覽器頁面**，它的 fetch 一定受 CORS 管——購買頁改成向後端要價格之後，那個前提就不成立了。症狀是使用者按結帳看到 `Failed to fetch`，真正的原因只在 Console 裡。**這是坑 #13 的又一次：某個時期成立的規則，要主動確認它在新情境下還成不成立。** 修法是只開 `pitlingo.com` / `www.pitlingo.com` 兩個來源 × 五個網站實際會用的端點（`/v1/plans`、`/v1/quote`、`/v1/checkout`、`/v1/contact`、`/v1/order`），**不回到 `*`**（那會把原本消除掉的攻擊面整個放回來），並加 `Vary: Origin` 避免 CDN 把 A 的允許標頭回給 B。結構上把 `fetch` 拆成 `handleRequest` + 外層加標頭，避免散落各處的 `return json(...)` 都要改。**法律文件全面正式化**：第二人稱統一為「您」、口語句子（「先講三件最重要的」「我們的錯不該讓你看不到字幕」「一句話總結」）改為書面語、管轄法院由臺北改為**臺中**、隱私權政策補上準據法與管轄一節。**兩個「聯絡我們」統一成一個**——首頁原本同時有 mailto 與工單頁兩個同名連結，同名不同行為是最讓人困惑的介面；改成工單為唯一入口並做成按鈕樣式，email 降為工單頁內的備援 |
| 2026-08-17 | **backend v3.6 + 網站改版** | **購買頁重寫、購物車、客服工單、隱私政策補完。** (1) **購買頁不再自己算任何金額**：上一版把 599/399 寫死在 `buy.js`，只從 `/v1/plans` 讀早鳥剩餘數，後端改成分段定價後**畫面顯示 599、綠界實收 299，線上真的發生過**。現在方案與牌價來自 `/v1/plans`、購物車金額來自 `/v1/quote`（與 `/v1/checkout` 走同一個函式）。(2) **購物車**支援字幕方案 + 9 種代訂混買，顯示折抵明細（代訂附贈一週 −39、升級抵扣）、VPN 提醒、人工服務工期。報價請求用**序號擋過期回應**——連點時先送後到會讓畫面顯示上一次的金額，而結帳收的是正確的錢，兩個數字不一樣就是糾紛。(3) **升級補差價 UI**：勾選後以 email 查本賽季已付的一週通行證，改 email 會重算。(4) **`/v1/contact` 客服工單**：與 `/v1/report` 刻意分開——後者需要安裝權杖，但網站訪客可能根本還沒裝擴充功能；改用 IP 速率限制防濫用，寫進同一份 `report:` 儲存，後台不必做第二套介面。使用者原本要即時對話窗口，改成工單制：一個人的規模做不到有人在線，而**掛著一個沒人回的聊天視窗比明確說「一個工作日內回覆」更傷信任**。(5) ⚠️ **隱私政策抓到一則不實陳述**：原本寫「不記錄 IP 位址（僅用於即時的速率限制，不寫入儲存）」，但 v3.4 加的 IP 額度**確實有寫入 KV**。已改為據實說明「IP 經 HMAC-SHA256 單向雜湊後取前 16 碼、原始 IP 不寫入、每日失效」。另補訂單與購物車、代訂服務（含自備帳號者的憑證處理）、工單三節。**政策寫「不做 X」而程式碼在做 X，是下架理由也是個資法下的不實陳述，而且沒有任何技術徵兆** |
| 2026-08-17 | **backend v3.5 + extension v0.18.2** | **直播的並發去重。** 直播時所有觀眾在同一秒看到同一批新 VTT 分段，於是同時發現快取沒有、同時送同一批句子來翻——**10 個人在線就是 10 倍的錢翻同樣的內容**，而且 10 份寫回同一個 bundle（KV 沒有 CAS，還會互相覆蓋）。伺服器端新增 `claimLine()` 認領機制，**用 Cache API 不用 KV**：KV 寫入是免費方案的天花板（1,000/天，坑 #19），不能為了去重再加寫入；而 Cache API 不計額度且**是 per-colo 的**，正好符合需求——同一場直播的觀眾多半連到同一個機房，去重就發生在他們之間。TTL 25 秒，認領者斷線或超時最多卡住別人 25 秒。⚠️ **認領失敗一律放行**：去重是省錢的優化不是正確性要件，寫成「失敗就不翻」會讓一個機房的 cache 故障變成那裡所有使用者完全沒字幕——用省錢機制製造功能性故障是不能接受的。用戶端配套兩項：直播的 flush 加 **0~1.2 秒抖動**（提前量有 50 秒，完全吸收得掉），以及收到 `pendingElsewhere` 時**把那些句子排回佇列**並清掉 `requested` 標記——沒有這段的話讓路的句子會被當成「已送出」而永遠不再要，直播時只有第一個到的人看得到字幕，安靜地壞掉 |
| 2026-08-17 | **backend v3.4** | **方案模型重構：一週通行證、代訂服務、購物車、升級補差價。** (1) `weekend` → **`week`（一週通行證）**：F1TV 非比賽週也有大量重播，只給週四到週日太窄。生效規則刻意做成**不需要使用者決定**——購買當下立刻可用（贈送），正式七天從**下一個比賽週的星期一**起算，贈送期上限 14 天（夏休期間買才不會變成白送一個月）。使用者原本想做「讓買家選延遲啟用」，我建議不要：那是需要解釋的決定，「我以為我選了延後」會直接變成沒有乾淨裁決依據的退款爭議。(2) **9 種代訂方案**進 `PLANS`，標 `manual: true`（人工服務，發碼只代表已收款待處理，後台看得到 `services[].status`）、`vpn: true`、除 5 天外 `bundleWeek: true`。(3) **`quoteCart()` 統一報價**——`/v1/quote` 與 `/v1/checkout` 走同一個函式，前端一律不自己算價，否則畫面與實收必然漂掉。綠界 `ItemName` 用 `#` 分隔多項；折抵不列成項目（綠界沒有負數金額），明細記在自己的 `pending` 裡。(4) **升級補差價**：`weekCreditFor()` 以 email 為鍵、只算本賽季、用 `lic.paid` 實付金額（不是牌價，否則折抵過的訂單會被重複抵）、**無上限**、`creditedAt` 標記防重複、差額 < 30 元免收（超商代收一筆 25 元，收 20 元淨得是負的）。標記時機刻意在**發碼之後**——反過來的話發碼失敗會讓使用者既沒新授權、舊額度也被吃掉。(5) **代訂附贈一週 + 另買一週 → 折抵 39**。(6) **免費層加上 IP 維度**（`FREE_IP_MULTIPLIER = 3`，只存 HMAC 不存 IP 原文）：單靠 installId 擋不住重裝，加上 IP 後要換網路才繞得過。倍率刻意放寬，同一 IP 後面可能是整個宿舍的 NAT，誤傷是靜默的。使用條款補上代訂價目、VPN 必要性、履行期間、帳號憑證不留存、升級折抵規則——`check-legal.js` 攔下了「後端有價格但條款沒寫」，那正是它存在的理由 |
| 2026-08-17 | **backend v3.3 + extension v0.18.1** | **剩餘週末改為包含待定場次**（使用者決定，方向與前一版相反）：卡達與阿布達比是 F1 官方排定的賽程，取消與否是他們的決定不是我們的；事先自己打折扣等於替別人的不確定性買單。2026-08-17 的剩餘從 10 變成 **12**。**新增中途缺漏偵測**——舊版 `checkNoCaption` 有一行 `if (everSawCaption) return`，看過一次字幕之後就再也不警告，所以**比賽中途的字幕缺漏完全是隱形的**：使用者看到字幕消失一段，診斷報告卻什麼都沒有。新的 `checkCaptionGap` 每 1.5 秒檢查，超過 20 秒無字幕就**當場重讀 DOM 分辨四種成因**：`untranslated`（原生有、我們無譯文）、`render`（有譯文卻沒畫出來）、`container_gone`（找不到字幕容器＝選擇器失效或播放器重建）、`silent`（原生也是空的＝賽後訪問等，正常）。只有前三種計入 `state.gaps`。⚠️ 呼叫順序必須排在 `checkNoCaption` **之前**，否則後者會先把 `lastNonEmptyAt` 重置，缺漏永遠偵測不到。**免費用量改為跨分頁持久化**：舊版是 content script 的區域變數，開新分頁就從 0 開始——伺服器每日 800 句仍擋得住成本，但畫面顯示「剩餘 15:00」等於在告訴使用者這招有效，而**讓人以為找到漏洞比真的有漏洞更糟**。改存 `chrome.storage.local`，每支影片各自記、上限 50 支。預抓節奏放回並發 2／間隔 40ms（退讓已是自適應的，卡頓時自動退到 800ms 單線） |
| 2026-08-17 | **extension v0.18.0** | **免費使用者不再執行整軌預抓——花錢之前先確認身分。** 舊行為：`startPrefetch` 完全不看授權狀態，於是免費使用者一進未收割的影片就把**整支**（實測 1,203 段、上千句）送去翻譯，而他最多只看得到 15 分鐘。更糟的是可以反覆觸發：進一支沒人翻過的影片、等收割開始、立刻退出、換下一支。伺服器端每日 800 句擋得住金額，但擋不住「免費使用者的成本結構本身就是錯的」。改成免費模式只用 **worker 攔到的分段**（前瞻預譯，提前量約 50 秒，零額外請求），整軌預抓留給付費使用者。另補上翻譯佇列的**三道額度防線**（`ingestVtt`／`handleCaption`／`flushPending`）——舊版只有 `handleCaption` 有擋，而預抓那條路根本不經過它，額度用完後使用者看不到字幕、我們卻還在替他翻後面的內容。**免費剩餘時間改為顯示在狀態卡最上方**（`剩餘 12:34`、暫停時標示「不計時」），不再只寫在診斷報告裡——那是使用者決定買不買的唯一依據。**修上一版造成的收割餓死**：退讓寫成硬性閘門（緩衝 < 8 秒就停、最多等 30 秒），實測 57 秒的觀看有 30 秒在等、1,203 段只抓到 4 段，照這速度要四小時，等於整軌預抓實質上不會完成——收割不完就沒人把譯文灌進共用快取，成本反而比卡頓那版更糟。改成三段式**減速而非停止**（健康 120ms／緩衝偏低 800ms／真的在等資料才暫停且上限 5 秒），並修 `bufferAheadSec` 取涵蓋目前播放位置的那一段而不是最後一段（拖過進度條後 buffered 會有多段）。診斷新增播放器緩衝秒數與目前節奏 |
| 2026-08-17 | **extension v0.17.0** | **坑 #34：整軌預抓跟影片搶頻寬，導致播放卡頓與自動降解析度。** 使用者 A/B 驗證：開擴充功能就一直當、關掉就正常。成因是收割要抓 1,020 個 VTT 分段，而那些分段跟影片**來自同一個 CDN 主機**——HTTP/2 之下共用同一條連線，我們每秒打十幾個沒有優先權標記的請求，播放器的 ABR 量到吞吐量下降就會降解析度。**我們要的字幕是 40 分鐘後才用得到，影片是現在就要看的，優先權完全相反。** 三道退讓：(1) `sw.js` 的 `fetchText` 加上 **`priority: 'low'`**，直接告訴瀏覽器排在影片後面；(2) 並發 3 → **1**、間隔 60 → **120ms**；(3) 新增 `playbackHealthy()`／`waitForPlayback()`，**每抓一段前**確認緩衝存量，低於 8 秒或 `readyState < 3` 就暫停收割（最多讓 30 秒，避免完全收割不到；暫停播放時視為健康，那時頻寬是空的）。診斷報告新增「讓路給影片：N 次，共 X 秒」——全 0 代表根本沒讓過。另修 `scanContentIdFromPerformance` **每 1.5 秒對 1,000 筆資源計時各跑一次正則**（F1TV 的網址帶 400 字元 token，特別長），而**播放器的 ABR 決策也跑在主執行緒上**；網址本身有 contentId 時直接跳過 |
| 2026-08-17 | **backend v3.2** | **定價簡化成三個價格**（使用者決定）：夏休前 NT$599／夏休起 NT$299／單場週末 NT$39。上一版的四段階梯每一段都算得出道理，但**選項太多會讓人猶豫，而猶豫的結果不是選便宜的，是不買**。分界用 **F1 的夏休**而非月份或剩餘場次——那是每個看 F1 的人本來就知道的界線，「夏休前 599、夏休後 299」一句話講得完。⚠️ 夏休的位置**必須人工標記**（`afterSummerBreak`）不能自動抓最長間隔：2026 最長的空檔在四月（3/29 → 5/01，33 天，因巴林與沙烏地取消），比真正的夏休（7/26 → 8/21，26 天）還長，自動偵測會把分界放在四月而且不報錯。**新增絕對底線**：整季票不可比「單場 × 剩餘週末」貴——2026 賽季被削短後，夏休前最後幾週只剩 11 個週末，599 元會比買 11 張單場票（429）貴 40%，使用者按計算機就會發現。守門的做法是**提早套用下半季價**而不是新增第三個價格。切換賣下一賽季的門檻改為剩餘 **8** 個週末（299 ÷ 39 ≈ 7.7，再少就會比單買貴）。順帶抓到一個 TDZ：`WEEKEND_PRICE` 宣告在 `PLANS` 之後會 `ReferenceError`，而那要到執行時才炸——`check-backend.js` 的懸空常數檢查擋下了 |
| 2026-08-17 | **backend v3.1** | **Season Pass 定價依據從「月份」改為「剩餘比賽週末」。** 月份原本是剩餘量的代理指標，2026 把這個代理打壞了：四月兩場取消後，「6~8 月 = 399」這一段從 6/1 的 16 個週末掉到 8/17 的 10 個週末，**同一個價格帶橫跨的價值差了快一倍**——8/17 用舊規則收 399 元賣 10 個週末，比單買 Weekend Pass（39 元）還貴，整季變成負價值商品。新價目表 `≥20 → 599／15–19 → 399／10–14 → 259／5–9 → 129`，門檻是照「每個週末永遠明顯低於單買」解出來的，實測最貴 NT$28.5（便宜 27%）。**單位用「比賽週末」不用「場次」**：一個 GP 週末含三場練習、可能有衝刺賽、排位賽、正賽，再加上一堆節目，使用者心裡的單位是週末。⚠️ 最高一級的門檻是 **20 不是 22**——2026 全年 23 個週末扣掉兩場待決只剩 21，門檻設 22 的話 599 今年一次都不會出現，旗艦價變成沒有徵兆的死碼。**剩不到 5 個週末時改賣下一賽季**（全價 599、效期到再下一年 1/31、本季剩餘週末附贈），而且 `nextSeason` 旗標必須顯示出來。**Weekend Pass 改綁比賽週末**（該週星期四 → 正賽隔日），不再是「發碼後 4 天」——舊做法在提前一週購買時**會在比賽開始前就過期**，付了錢什麼都看不到且不報錯；授權記錄新增 `startsAt` 與 `gpName`，`licenseProblem` 對尚未生效的通行證會明講從哪一天起可用。**早鳥價一旦不低於當期 Season 價就自動從 `/v1/plans` 隱藏**（用比價不用比日期，不必每年維護截止日）。`tools/check-backend.js` 的定價檢查整段重寫成驗新語意，並新增 Weekend Pass 綁定的三項檢查 |
| 2026-08-17 | **extension v0.16.3** | **坑 #33：注入層把媒體分段當成文字攔截，造成播放卡頓。** 使用者回報「影片時不時轉圈圈」，同一份診斷還有 `翻譯後端回報錯誤：單句長度不可超過 1000 字元 ×2`——**兩個症狀是同一個根因**。`inject.js` 的 `interesting()` 最後一條規則是 `n>0 && n<300000`，**只看大小不看型別**，而 HLS 的音訊分段與低位元率影片分段幾乎全部落在 300KB 以內。實測 8 種媒體分段樣本**舊版 8/8 全部洩漏**：每個分段都被 `clone()` 一份、用 `TextDecoder` 整個解成字串、再對那串二進位垃圾做 `indexOf` 掃描，最後才丟掉。後果三個且都不報錯：(1) 每個分段記憶體加倍 → 播放卡頓；(2) clone 的分支要被讀完，對播放器下載造成背壓；(3) **二進位垃圾偶爾湊出 `-->`**，於是被當成 VTT 送進翻譯佇列，`parseVtt` 產出幾萬字元的假字幕 → 後端退回。註解一直寫著「只 clone 小的文字類回應」（坑 #7），但程式碼的第三條規則根本沒檢查型別——**註解對、程式碼錯，而且已經在那裡很久了**。改為「先排除媒體、再納入文字、最後才是保守的 64KB 規則」，並新增 `looksText()` 在解碼後做廉價的二進位偵測。第二道防線：用戶端加上與後端一致的 `MAX_LINE_LEN = 1000`，超過就略過並計入 `state.badLines`（診斷報告可見，**不是 0 就代表攔錯東西**）。新增 **`tools/check-inject.js`**——注入的 worker 程式碼是**字串**，`node --check` 只檢查外層檔案，字串裡的語法錯誤會讓字幕完全不出現且不報錯；這支會實際編譯它，並用真實的 URL／content-type 樣本驗證「媒體一定排除、字幕清單一定攔得到、二進位不得判成文字」 |
| 2026-08-17 | **backend v3.0 + extension v0.16.2** | **修使用者回報的「後台刪除授權碼，擴充功能仍顯示已啟用」。兩個獨立缺陷疊在一起：** (1) 用戶端 `licenseRenew` 只把 **403** 當成確定的否定答案，刪除授權碼回的是 **404**，程式一路走到「不是 ok 就什麼都不做」，本機通行證原封不動 → 最長 14 天持續顯示已啟用，不報錯也沒日誌；(2) 更嚴重的是**伺服器端也還在放行**——通行證是簽出去的獨立憑證，`entitlementGate` 只驗簽章與期限，刪掉 KV 裡的授權記錄對它毫無影響。原始碼註解還寫著「那是刻意的，避免誤刪立刻把人踢下線」，但對「刪除」這個動作而言那是錯的，誤刪的保護應該留在「有裝置就必須帶 force」那一層。新增 **`entvoid` 通行證作廢清單**，與既有的 `revoked` **刻意分開**：`revoked` 封鎖整個安裝（連免費層都沒有，用於濫用盜用），`entvoid` 只讓通行證失效而**退回免費層**——刪掉一組發錯的碼不該讓那個人連每場 15 分鐘都不能看。刪除、停用、清空裝置三個動作都會寫入；**啟用時會清除**（否則「清空裝置後請使用者重新啟用」這條客服流程會變成啟用成功但伺服器照擋）；後台「恢復」走反向清除（否則畫面說成功、使用者仍不能用）。另修 `handleLicensePatch` 先清空 `lic.devices` 才讀 installId 的順序錯誤。設定頁開啟時改為**強制向後端確認**，不再顯示最多過時 24 小時的本機狀態。**2026 賽程寫進遠端設定**（`REMOTE_CONFIG.schedule`，可熱更新不必送審）：中東戰事使巴林與沙烏地的四月兩場取消、巴林移師馬來西亞 Sepang 於 10/02–04 舉行、西班牙站在馬德里 Madring（不是巴塞隆納），全季 **23 場且四月完全無賽事**；卡達與阿布達比標為 `tentative`，**計算剩餘場次時不計入**。使用者可見文案改為正式用語（移除「不會再花錢」這類從開發者視角寫的句子） |
| 2026-08-17 | **extension v0.16.1** | **修使用者回報的「字幕顯示出來後馬上消失、有時完全不顯示」。** 成因是**延後只延後了「顯示」，沒有延後「收起」**——收起仍然跟著原生字幕即時走，於是每一句的顯示時間都被硬生生砍掉 N 毫秒：原生 1200ms + 延後 1000ms 只剩 550ms（閃一下），原生 800ms 的短句在排程時間到達時原生早已清空，舊版的 DOM 比對判定「畫面已換句」而**整句不顯示**。轉播字幕每 3~4 秒換一句，延後 3 秒幾乎必然落在這兩種情況裡。改成把延後當作**整條時間軸的平移**：顯示與收起進同一個延後佇列，使用者看到的就是原生字幕往後挪 N 毫秒、長度完全不變。模擬 8 句真實形狀的時間軸實測：**舊做法在延後 3 秒時 8 句掉 5 句、其餘全被砍短；新做法 0 句遺漏、長度誤差 0ms**。舊版那個 DOM 比對是為坑 #18 加的，但那個坑屬於「翻譯慢回來才補顯示」的路徑（仍保留檢查）——這裡是排程當下譯文就在手上、延遲量固定、順序不會亂，不該套同一條規則。佇列另有換影片作廢、停用作廢、拖進度條作廢與 40 筆上限 |
| 2026-08-17 | **extension v0.16.0** | **字幕時機重做，重點是讓它「可被驗證」而不只是「有做」。** 上一版的提前顯示取 8 個樣本的中位數就直接相信，**沒有任何辦法分辨 VTT 的 cue 時間是絕對值還是分段內相對值**——前者成立、後者會在隨機時刻噴出隨機句子，而兩者都不報錯。新增**離散度（IQR）閘門**：樣本擠在一起才啟用提前，攤開就判定時間軸不可用並退回只延後；判斷結果寫進診斷報告與設定頁，不必再靠感覺猜。同時修四個實際缺陷：(1) **提前顯示會被自己的輪詢在 350ms 後收掉**（原生字幕還沒出現，輪詢判定「已清空」）；(2) 提前顯示只畫中文，英文要等原生字幕追上才跳進來，**畫面會抖一下**——那個抖動看起來像時機不對，其實是內容變了；(3) **提前顯示不經過 `handleCaption`，完全繞過免費層閘門**；(4) `tickEarlyDisplay` 排在停用判斷之前，killSwitch 期間仍會把句子標記成「已顯示」，恢復後再也不提前。延後上限 ±2 秒 → **延後 3 秒／提前 2 秒（刻意不對稱**：延後是純計時器，提前依賴校準精度）。**換影片自動回復預設**（字級與位置保留）。**「字幕停留上限」從設定頁移除**改為固定 7 秒——疊字改成跟著原生字幕收掉之後那個計時器永遠不會觸發，留著只是一個調了沒反應的旋鈕。另修 `Number(true) === 1` 讓布林值變成「提前 1 毫秒」、`Math.min(NaN,…)` 讓 `setTimeout` 收到 NaN 而立刻收字幕。**`tools/chaos-extension.js` 的 `setInterval` 假時鐘原本排在 `now + 1e9`＝永不觸發**，等於字幕輪詢、換影片偵測、遠端設定重讀、免費額度計時全部從未被測到（測試綠燈但沒測到，比紅燈危險）——已修正並新增第 15 個情境 |
| 2026-08-17 | **backend v2.9 + extension v0.15.0** | **字幕時機微調**（±2 秒、0.2 為單位）。延後＝單純 `setTimeout`，零風險；提前＝需要 VTT 時間軸，但**不解析 X-TIMESTAMP-MAP**（決策 4.1 避開的那條路），改用**自我校準**：記錄「DOM 顯示時的 `currentTime` − VTT cue 時間」取中位數推出基準，樣本不足就自動退回只能延後。**直播一律夾成 0**（滑動視窗基準會變）。**賽季中分段定價**（1~5 月全價／6~8 月 70%／9~10 月 45%／11~12 月 25%，折扣價 floor 取整到 10 元）＋ Weekend Pass NT$39。修：訂單編號秒級時間戳 + 6 碼隨機在 20000 次仍會碰撞，改為分級 + 8 碼；折扣取整用 round 會讓 599 變 600 比牌價貴。新增 `BUSINESS.md` |
| 2026-08-17 | **backend v2.8** | **金流串接完成**：`/v1/checkout` 建單、綠界 `CheckMacValue`、ATM／超商取號通知（**不發碼**——取號不等於付款）、訂單查詢、公開方案資訊。webhook 改為**從自建訂單讀方案並比對金額**，不再從商品名稱猜字串——使用者結帳當下看到什麼價格就該拿到什麼方案。網站新增 `/buy`（含法定的同意勾選）與 `/paid`。修一個會直接讓使用者付不了錢的缺陷：**訂單編號碰撞**（時間戳只到秒，隨機碼用 base36 截斷導致分布傾斜，實測 3000 次撞 12 次；綠界收到重複編號會直接拒絕）——改為字元表均勻取樣加拒絕取樣，20000 次全異 |
| 2026-08-17 | **extension v0.14.1** | 網站上線：`legal/` 部署到 Cloudflare Pages（專案 `pitlingo`）。**架構修正**——`pitlingo.com` 原本被掛在 Worker 上，導致整個網域回 `{"error":"not found"}`；正確配置是網站在 `pitlingo.com`、API 在 `api.pitlingo.com`。踩到坑 #32（加 `[[routes]]` 會靜默關閉 workers.dev，所有安裝同時斷線）。`BACKEND` 改為 `api.pitlingo.com`，舊位址保留相容。新增 `tools/check-links.js`（站內連結、互連路徑、host_permissions 涵蓋、可加 `--live` 實際驗證線上狀態） |
| 2026-08-16 | **extension v0.14.0** | `pitlingo.com` 已購入，**隱私權政策與使用條款完成**（`legal/`）。台灣法規的兩個關鍵：七天鑑賞期排除必須依《通訊交易解除權合理例外情事適用準則》第 2 條第 5 款明示（沒明示 = 仍可主張無條件退貨），以及與 Formula 1 無關聯的聲明。新增 `tools/check-legal.js` 確保**文件與程式碼行為一致**（權限、保存期限、價格、免費層秒數、統計白名單）——政策寫「不蒐集 X」但程式碼在蒐集，是下架理由也是不實陳述，且沒有任何技術徵兆。另：洗掉 16 份舊診斷報告裡的 77 處 CDN 權杖（坑 #31 的殘留；全部 24 小時內到期且 repo 為 private，故不重寫歷史） |
| 2026-08-16 | **backend v2.7 + extension v0.13.1** | 全面審查的第二批修正：**早鳥會賣超**（`讀 count → +1 → 寫回` 在 KV 上是 lost update，兩筆同時付款都讀到 19 就會發出 21 組；且計數器會與實際發出的碼漂移。改為直接數實際的碼，來源即事實，並在達標時提早結束掃描避免 O(n)）、**manifest 最壞佔用 14 MB 常駐記憶體**（份數上限擋不住「每份都很大」，改為總位元組上限，且取得字幕清單後立即釋放 body） |
| 2026-08-16 | **backend v2.6 + extension v0.13.0** | **產品數據**（彙總計數、維度白名單收斂、**不含 cid／IP／任何識別碼**）。審查中查出四個實際缺陷並修正：(1) `noteFreeUsage` 取樣數學錯誤——`n≥5` 時機率恆為 1，**每批都寫一次 KV**（一支影片 100 puts，走回坑 #19 老路）且固定 `+5` 導致免費額度低估 4 倍；(2) **userscript 收割器會被免費額度擋死**，且 legacy 全體共用一個桶；(3) 早鳥「限量 20」根本沒實作，`earlyRemaining` 是死碼；(4) 用戶端讀 `site.freeTier` 但它掛在**設定根層**，遠端推的免費層永遠不生效且不報錯 |
| 2026-08-16 | **backend v2.5 + extension v0.12.0** | **伺服器端授權閘門**（用戶端只是 UI，這裡才是真正的牆：未授權者每日 800 句上限，超過退回只讀共用快取——已翻過的影片仍完全可用）。**免費層閘門**（四種正式場次前 15 分鐘；重播用播放位置、直播用累計觀看秒數）。方案定案：GP Trial 免費／Early NT$399 限 20 名／Season NT$599，**取消永久方案**。後台補上**換方案下拉**（上一版只做了後端，UI 漏掉——已用 `check-admin.js` 防止再犯）、**永久刪除**（有裝置啟用時需二次 force）、**工單標示已解決／重開／刪除**、**封鎖安裝**。回報表單前加上「字幕慢半拍是正常的」說明 |
| 2026-08-16 | **backend v2.4 + extension v0.11.0** | **金流自動發碼**（驗簽 + 防重放；未設定驗證方式時拒絕處理）、**免費層定義**（四種正式場次的前 15 分鐘，用網址 slug 分類且放在遠端設定可熱修）、後台授權清單可就地改方案／延期／清空裝置且**已驗證不會弄丟 email 與已啟用裝置**。修「回報無法使用」——真正原因是後端未部署，但 `api()` 把所有失敗都寫成「無法連線到伺服器」，使用者去檢查網路是白費工；現在錯誤帶著 status 與伺服器訊息。新增**懸空引用檢查**（第二次踩到：路由呼叫了不存在的函式，`node --check` 完全過得去，要到有人打那個端點才 500） |
| 2026-08-16 | **backend v2.3 + extension v0.10.0** | 修**停用不生效**（通行證 14 天有效但只在到期前 3 天才續，停用最久 11 天才生效——對開發者毫無保護。改成每 24 小時主動回報一次，14 天有效期只當離線緩衝）。方案期限改由伺服器計算：試用 14 天／賽季到隔年 1/31（F1 賽季橫跨年底）／永久無期限。後台改成**完整授權清單**（搜尋、狀態過濾、就地延期／清空裝置／停用恢復）並新增**問題回報**分頁。擴充功能：拿掉一般使用者看得到的「詳細日誌」；「匯出診斷」改成**傳送給開發者並回工單編號** `PL-YYMMDD-XXXX` |
| 2026-08-16 | **backend v2.2 + extension v0.9.0** | **設定頁整個重做**：狀態卡（打開就看得到現在如何，每個錯誤都說「現在能做什麼」）、首次使用引導（CC 沒開是最常見卡關點）、授權區（輸入授權碼、裝置管理、在這台停用）、設定分成顯示／進階／疑難排解三層。修管理端 CORS——第一版把 `*` 收得太乾淨，連自己的 `file://` 後台都擋掉了。後台加上**發碼／查詢／停用**，不必再用 curl。新增 `tools/check-options.js`：檢查 JS 取用的 id 都存在、HTML 的互動元件都有人接、沒有行內 script/style（MV3 審核硬性要求）、設定欄位三邊對得起來 |
| 2026-08-16 | **backend v2.1 + extension v0.8.0** | **修正 v2.0 的授權設計錯誤**：把「證明付過錢」做成綁裝置的，換一台電腦就失效——付費軟體不能這樣。改成可攜帶的授權碼 `PL-XXXX-XXXX-XXXX`（任何裝置貼上即可、最多 3 台、可自行解除、可用 email 補發、不需密碼與註冊）。新增 `tools/chaos-extension.js`：**14 個亂操作情境**（反覆開關、預抓中連切五支影片、後端回 401/500/亂格式/不回應、MAIN world 送畸形訊息、遠端推壞設定、設定塞不合理值、容器數 0/1/5 跳動、節流恢復、關分頁時佇列有東西⋯），判定標準是「不可有未捕捉的例外」，並已用注入 bug 驗證這支測試抓得到問題。新增 `sanitizeSettings()` 清洗設定值——`fontSize: 'big'` 不會炸，只會畫出 CSS `NaNpx` 讓字幕整個不見 |
| 2026-08-16 | **backend v2.0 + extension v0.7.0** | **資安、分階段推送、成本後台三塊一次到位。** 資安：每個安裝一枚匿名權杖（取代等同公開的共用 CLIENT_TOKEN，S6）、拿掉 CORS `*`、常數時間比對、速率限制改以 installId 為鍵、譯文合理性檢查擋 prompt injection 汙染共用快取、每日成本熔斷、撤銷端點。推送：`rollout` 百分比分流（伺服器依 installId 雜湊決定，可單方面收回）、`killSwitch` 總開關、`minClientVersion`、**套用新設定後 30 秒自我檢查，抓不到字幕自動退回上一版**。後台：`GET /v1/admin/stats` + 單檔 `backend/admin.html`，看得到每日／每片成本、共用快取命中率、**prompt 快取命中率**（掉下來就是坑 #21 復發）。新增 `tools/check-backend.js`，21 項行為驗證全過 |
| 2026-08-16 | **backend v1.7 + extension v0.5.4** | 後端補上 userscript 的坑 #9：**單句不再走批次編號協定**（實測回覆率只有 50%——送 2 句回 1 句，且完全不報錯，沒對上的下次再翻一遍白花錢）。批次解析加上「行數相同就按位置對應」的備援。另修正誤導訊息：「開始批次預譯」改成「已從播放器的字幕分段取得提前量」——使用者掛著幾小時後看到它再次出現，會誤以為整支要重翻 |
| 2026-08-16 | **extension v0.5.3** | 修坑 #28（事件時間軸被同一行洗版三小時）。加上**圖示 16/32/48/128**（由 `logo.png` 縮圖產生），P2 的形式要件只剩隱私政策。煙霧測試新增遠端設定冪等性檢查 |
| 2026-08-16 | **extension v0.5.2** | 補齊 userscript 的防呆機制：**播放中沒字幕的提示**（只在 video 真的在播、且從未看過字幕時才提示，五分鐘一次）、`memo` 上限 12,000／`pending` 上限 3,000、`pagehide` 把還沒送出的句子用 keepalive 做最後一搏。實測批次效率 19.8 句/批（v0.4.3 是 4.8，userscript 是 14.9）、到上畫面中位數 4.3ms |
| 2026-08-16 | **extension v0.5.1 + userscript v4.8.0** | 修坑 #27（疊字靠固定計時器消失，不跟著原生字幕走——這才是「比 userscript 慢一拍」的真正成因）。兩個產物都加上**「到上畫面」量測**（兩次 rAF），數字可直接對照，不用再各說各話。驗證：跳過整軌預抓已生效（後端記錄 383 段、本次已抓 0 段） |
| 2026-08-16 | **extension v0.5.0 + backend v1.6** | 修坑 #26（HTTP 快取讓 segCount 一分鐘看不到，跳過判斷永遠不成立）。補上 userscript 的坑 #10（收割期間停用計時器 flush——實測批次 4.8 句/批 vs userscript 14.9）與坑 #15（收割中止偵測，播放器返回鍵不改網址）。**新增測試工具 `__pitlingo.t.*`**，集中在一個區塊，上線前整段刪除；`__pitlingo.help()` 列得出來的東西正式版一律不該存在 |
| 2026-08-16 | **extension v0.4.3 + backend v1.5** | 修坑 #24（並行請求互相覆蓋，實測只有 41% 的譯文存活）與坑 #25（擴充功能不會跳過已收割的影片）。新增 `POST /v1/complete` 讓一般用戶端也能標記完整度。**量測結果：擴充功能替顯示延遲加了 1ms**（observer 佔 100%、繪製中位數 1ms），使用者感受到的 0.5 秒全部來自 F1TV 自己畫字幕的時機 |
| 2026-08-16 | **extension v0.4.2** | 修坑 #23（bundle 快取無 TTL，重整後共用快取永遠停在舊值）。新增**預抓後回寫查核**，主動比對本機與後端句數。新增**詳細日誌模式**（設定頁勾選或 `__pitlingo.debug(true)`），逐句與逐批都印。顯示延遲改為**量測而非猜測**：診斷報告新增「偵測來源」（observer vs 輪詢）與繪製耗時，用來分離「我們的延遲」與「F1TV 自己的延遲」。輪詢 250ms → 100ms，停擺門檻改用絕對值 2500ms（原本是 POLL_MS 的倍數，調快輪詢會連帶誤報） |
| 2026-08-16 | **extension v0.4.1** | 修坑 #22：v0.4.0 清死碼時誤刪 `manifests` 宣告，實測一開頁就 `ReferenceError` 洗版、診斷無法匯出。新增 `tools/smoke-extension.js`（假 DOM 執行 content script 並觸發實際路徑），已驗證能抓到這類 `node --check` 抓不到的錯 |
| 2026-08-16 | **extension v0.4.0** | **整軌預抓補上，三層架構與 userscript 對等。** 入口不是 PLAY API 而是 worker 本來就會抓的 m3u8——v0.3.x 的 hook 只送 VTT 不送 manifest，所以一直以為拿不到 master。同時：修好 inject.js 裡重新出現的網址截斷（坑 #3）；換影片時清空 manifest（防坑 #16 式污染）；收割鎖改成重試而非直接放棄。**移除 PLAY API 全部死碼（-339 行）與 `cookies` 權限**，減少商店審查面 |
| 2026-08-16 | **backend v1.4** | 修坑 #21（後端 prompt 快取從未生效）。把 userscript 的完整 `SYSTEM_PROMPT` 整份搬進後端：術語 34 → 273 條，token 數跨過 4,096 門檻，`cache_control` 才真的啟動。連帶修正擴充功能「快取未命中時譯文品質較差」的問題。新增 `tools/check-prompt.js` 擋兩份 prompt 漂移 |
| 2026-08-16 | **extension v0.3.5 + backend v1.3** | 修坑 #19（KV 寫入爆量，一場正賽超額 2 倍，實測已觸發 Cloudflare 封鎖）與 #20（分頁被節流時輪詢停擺）。38 分鐘穩定性測試：命中率 99.5%、VTT 218 份、零字幕重複 |
| 2026-08-16 | **extension v0.3.4** | 修坑 #18（過時的譯文會重新彈出）。遠端設定熱修的行為層面驗證完成——壞選擇器確實讓字幕停止、換回即恢復 |
| 2026-08-16 | **extension v0.3.3 + backend v1.2** | **遠端設定熱修完成並實測**（見 8.1）。後端設定移到 KV，`POST`/`DELETE /v1/config` 可熱修與還原，不用重新部署。用戶端 34 秒內自動套用、影片不中斷。過程修掉兩個傳播延遲的坑（6 小時 → 10 分鐘 → 3 分鐘），並修正「事件日誌第二個參數被靜默丟掉」。新增 `tools/check-logcalls.js` |
| 2026-08-16 | **extension v0.3.0** | **P2 核心障礙解除**。放棄 PLAY API（八個版本、詳見 7.7），改用 MAIN world 注入 blob worker 取得提前量——與 userscript 相同的機制，實測注入的腳本同為 707,103 bytes，命中率 83%。發現 F1TV 掛有 Imperva 機器人防護（`reese84`），這反而讓 Worker 注入成為最安全的方案（零額外 API 流量） |
| 2026-08-16 | **extension v0.2.0** | **推翻 7.3 的 DOM-only 決策**。實測擴充功能跟不上語速而 userscript 可以，根因是提前量 50 秒 vs 0 秒。改為直接呼叫 PLAY API 取得 master m3u8 → 字幕清單 → VTT 分段預抓，拿回提前量且不需注入。直播用滑動視窗。移除會分心的「翻譯中…」佔位字。新增事件時間軸與「匯出診斷」按鈕 |
| 2026-08-16 | **extension v0.1.0（P2 起步）** | MV3 擴充功能骨架。關鍵決策：**不做 Worker 注入，因此不需要 MAIN world，跑在 ISOLATED world**；contentId 改用 `performance.getEntriesByType('resource')` 取得（回溯、免 webRequest 權限）；所有網路請求集中在 service worker；`/v1/config` 遠端設定讓 F1TV 改版可熱修不用送審。新增 `tools/check-normkey.js` 驗證三份 `normKey()` 一致 |
| 2026-08-16 | **v4.7.2** | 修坑 #17（手動 🎯 會默默重抓整支）。收割完成時登記本機覆蓋率，讓重新進入自動走「跳過」路徑；按鈕在已完整時要求確認；強制觸發留下時間軸紀錄 |
| 2026-08-16 | **v4.7.1** | 修坑 #16（跨影片 memo 污染導致共用 bundle 缺角，從診斷報告的時間軸發現）。加 `prefetchDoneForCid` 以 contentId 為鍵的預抓決策保險。診斷面板改顯示目前觀察中的容器數 |
| 2026-08-16 | **v4.7.0** | **可觀測性**：新增事件時間軸（所有狀態變化都印到 Console 並存入環形緩衝）、階段指示、收割與翻譯的 10% 進度回報、**「📋 匯出完整診斷」一鍵複製完整報告**。修坑 #14（無旁白片段誤判為故障）、#15（收割中止只看 contentId，播放器返回鍵無效） |
| 2026-08-16 | **v4.6.0** | **韌性：使用者亂按也不會出錯**。修坑 #11（contentId 只認一種網址，導致 `/page/` 進來的影片完全不上傳）、#12（手動觸發無併發鎖，1056 段抓了 2112 次）、#13（只在全部翻完才上傳，中途關掉全白費）。新增 `harvestGen` 世代中止、`uploadedKeys` 增量上傳、60 秒定時上傳、`pagehide` keepalive 收尾 |
| 2026-08-16 | **v4.5.0 + backend v1.1** | **快取完整時跳過整軌預抓**：bundle 新增 `segCount`（僅在「整軌抓完且零失敗」時標記），用戶端比對字幕清單分段數，相同就完全跳過收割——省下 400+ 次對 F1 CDN 的請求。品牌定案 PitLingo，腳本 `@name` 更新，新增品牌查核清單（7.0.2） |
| 2026-08-16 | v4.4.0 | 修正坑 #9（單句批次編號協定失效）與坑 #10（收割期批次被計時器切碎）。記錄共用快取實測結果（10.1 節） |
| 2026-08-16 | **v4.3.1** | 修正「掛在非播放頁也會跳前瞻字幕警告」——改為累計實際播放秒數才判定。新增 `CLAUDE.md`（跨機器 AI 接手）、`README.md`（repo 門面）。整合產品與 Launch 企劃書，記錄三個落差。git repo 建立 |
| 2026-08-15 | **v4.3.0 + backend v1** | **共用譯文後端（P1）**：Cloudflare Workers + KV。`GET/POST /v1/subs`、`POST /v1/translate`、`GET /v1/config`（遠端設定，防禦第 1 名的實作）。userscript 加上換片自動下載、收割後自動上傳、後端選單與統計。連不上時全自動退回本機翻譯 |
| 2026-08-15 | v4.2.0 | **術語表大幅擴充**：新增歷代車手、車隊與賽事人物、2026 完整賽道清單、各賽道彎角俗名（含歷史賽道）、纏鬥與駕駛動作、天候與賽道狀況、賽季與合約用語。防禦順位調整：主動驗證＋自我診斷提到第 2 |
| 2026-08-15 | v4.1.0 | **韌性強化**：顯示偵測改為輪詢為主、observer 為輔；節點集合變動整組重掛；字幕清空重置 `lastRaw`；contentId 切換偵測與狀態重置；疊字層自動重掛；字幕長時間缺席提示。修正「切換車手鏡頭後字幕不再出現」 |
| 2026-08-15 | v4.0.1 | 建立本文件。修正網址截斷、無限重試、manifest 截斷；改用直接攔到的字幕清單優先 |
| 2026-08-15 | v4.0.0 | 整軌預抓（重播）。成本降至約 $0.31/場 |
| 2026-08-15 | v3.4.0 | 自適應批次等待、manifest 擷取 |
| 2026-08-15 | v3.3.0 | 聚合批次（最長等待而非 debounce） |
| 2026-08-15 | v3.2.0 | blob worker 注入成功，命中率 95% |
| 2026-08-15 | v3.0.0 | VTT 攔截架構（主執行緒，未成功） |
| 2026-08-15 | v2.2.0 | 修正 `innerText` 讀不到隱藏元素 — 字幕首次成功顯示 |
| 2026-08-15 | v2.0.0 | 2026 賽季擴充術語表 + prompt caching |
| 2026-08-15 | v1.0.0 | 初版 |
